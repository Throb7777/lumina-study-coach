# 项目 Agent 执行规则

本文件定义在本项目中进行分析、设计和实现时必须遵守的工作方式。产品需求见 `learning_flow_coach_spec.md`，动态实施状态见 `IMPLEMENTATION_PROGRESS.md`。

## 1. 信息优先级

发生冲突时，按以下顺序执行：

1. 用户在当前对话中的最新明确要求。
2. `learning_flow_coach_spec.md` 中的产品规格。
3. `IMPLEMENTATION_PROGRESS.md` 中已经确认且未被更新的技术决策。
4. `design-references/` 中的视觉参考图。
5. Agent 自己的推断。

不得使用低优先级信息覆盖高优先级要求。发现冲突时，应先说明冲突，再按高优先级要求处理；如果会明显改变产品行为，则先向用户确认。

## 2. 开始实施前

凡是要写代码、修改页面、变更数据结构、调整文件读写或实现功能，开始前必须：

1. 阅读本文件。
2. 首次进入项目时完整阅读 `learning_flow_coach_spec.md`；后续至少阅读总则和与当前任务有关的章节。涉及跨模块行为时重新完整检查规格。
3. 阅读 `IMPLEMENTATION_PROGRESS.md` 的当前阶段、功能状态、当前任务、技术决策、阻塞项和最近验证结果。
4. 检查项目中的实际代码和文件。进度文档只能用于定位，不能代替代码检查。
5. 明确本次范围、不会处理的内容和可验证的完成条件。
6. 检查是否存在未完成任务、用户已有改动、阻塞问题或规格冲突。
7. 对多步骤任务给出简短计划，并在执行过程中更新状态。

纯讨论、方案设计、解释和只读检查不属于实施，不要求为了形式修改进度文档。

## 3. 实施原则

- 只实现当前要求，不增加推测性功能。
- 优先完成可运行的最小闭环，再扩展次要能力。
- 修改范围必须能直接追溯到当前任务。
- 不重构无关代码，不清理既有但与当前任务无关的问题。
- 遵循项目已有结构、命名、组件和数据访问方式；没有既有模式时选择最简单、可验证的方案。
- 数据结构、API 和文件格式应有明确边界，避免使用临时字符串约定代替结构化数据。
- 不把个人学习数据、SQLite 数据库、Obsidian 路径、导出内容或本地配置写入开源仓库。
- 第一版范围以产品规格中的 MVP 和“暂不实现”列表为准。
- 遇到不明确但不影响当前最小实现的事项，记录到进度文档的待确认项，不擅自扩大范围。

## 4. 参考图使用规则

`design-references/` 中的图片是视觉参考，不是逐像素验收标准。

- 参考其暖色基调、轻量工作台气质、信息层级、留白、字体尺度和完成状态表达。
- 可以根据真实数据、组件复用、交互效率、内容长度和响应式约束调整布局。
- 产品功能、可用性和真实状态优先于参考图中的局部细节。
- 不照搬参考图中的示例课程、日期、题目、统计数据或其他演示内容。
- 不为了贴近图片而增加无意义卡片、图表、装饰或营销式内容。
- `06-obsidian-note-editor.png` 中左侧“文档结构”栏不实现；笔记页以 Markdown 编辑器和预览双栏为准。

## 5. 验证要求

每项实现都要先定义完成条件，再进行与风险相称的验证：

- 业务逻辑修改应有对应的单元测试或可重复检查。
- API、数据库或文件读写修改应验证成功路径和关键失败路径。
- 页面和交互修改应至少验证主要流程、空状态、错误状态和典型内容长度。
- 涉及前端布局时，应检查常用桌面视口，确保文字、按钮、面板和进度控件不重叠。
- 构建、类型检查、测试和实际页面检查应分别记录，不能互相替代。
- 无法执行某项验证时，必须明确记录原因，不能把未验证状态写成已完成。

只有实现满足验收条件且必要验证通过后，功能状态才能标记为 `DONE`。

## 6. 完成实施后

每次产生实际代码、数据结构、配置或产品文档变更后，结束前必须更新 `IMPLEMENTATION_PROGRESS.md`：

1. 更新当前阶段和最后更新时间。
2. 按真实结果更新功能状态。
3. 更新当前任务、下一步和阻塞项。
4. 记录本次修改的主要文件。
5. 记录实际执行的构建、测试和页面检查。
6. 明确记录未执行或失败的验证。
7. 记录会影响后续实现的新技术决策。
8. 在实施记录中追加一条简洁记录。

不要把每条命令写进进度文档。进度文档只保留下一次运行需要了解的事实。

## 7. 进度状态定义

进度文档只使用以下状态：

- `NOT_STARTED`：尚未开始实现。
- `IN_PROGRESS`：已经开始，但尚未满足完整验收条件。
- `BLOCKED`：存在明确阻塞原因，当前无法继续。
- `DONE`：实现完成，并通过约定的必要验证。

“已有设计稿”“已有部分代码”或“页面能够打开”都不等于 `DONE`。

## 8. 进度文档维护方式

- “当前阶段”“功能状态”“当前正在进行”“下一步”和“最近验证结果”维护为最新快照，可以直接改写。
- “实施记录”采用追加方式，保留日期、完成内容、修改文件、验证结果和遗留问题。
- 已失效的决策应在当前快照中替换，并在实施记录中说明原因；不要让新旧决策同时作为有效规则存在。
- 进度文档与代码不一致时，以实际代码为事实，并立即修正文档。

