# Lumina：实现进度

本文件记录项目当前真实实施状态。产品需求以 `learning_flow_coach_spec.md` 为准，执行规则以 `AGENTS.md` 为准。

## 当前阶段

- 项目阶段：第一版阶段 44，`v0.1.3` 已进入实际使用反馈修复阶段。
- 当前目标：图片作答多模态批改、页面定位、公式收尾与未保存状态修复已完成实现、验收和 `v0.1.3` 同版本发布刷新。
- 最后更新时间：2026-09-02。
- 当前结论：后端 133 项、前端 109 项测试及启动器 17 项发行检查通过；图片作答会把原图作为 Codex `localImage` 输入并以 OCR 文字辅助，PDF 继续使用文本提取。新章节进入顶部、收尾公式紧凑化、今日完成/生成练习前自动保存和幽灵未保存状态清理均已回归覆盖。功能提交 `c9b62e8` 已推送，`main` 与 `v0.1.3` 标签均指向该提交，两个 GitHub Actions 运行均成功；同版本 prerelease 的 5 个资产已经刷新并与本地大小及 SHA-256 逐项一致。正式学习数据未被读取、迁移、清理或修改。

## 功能状态

| 模块 | 状态 | 当前情况 | 完成验证 |
| --- | --- | --- | --- |
| 产品规格 | `DONE` | 已形成完整产品规格，并加入 Lumina 品牌、视觉和字体规则 | 已检查规格文件可读取，核心章节存在 |
| 视觉参考 | `DONE` | 已保存 6 张界面参考图及 Lumina 三版图标候选、正式主图和尺寸检查图；参考图不是逐像素要求 | 正式 PNG/ICO 尺寸、透明角和 16-256px 图标目录已验证 |
| 内置完整示例 | `DONE` | 基于 MIT OCW 18.06 Lecture 1 官方课程页和逐字稿 PDF 生成脱敏静态只读示例；作为课程首页特殊课程行显示，可经两次确认写入本机隐藏偏好；不写数据库、不请求 API/模型、不参与统计/搜索/导出 | 前端回归覆盖打开、零 API、两次确认隐藏和隐藏后深路由返回课程页；干净发布副本及 390/1440/2560/3840px 实际页面验收通过 |
| 项目基础结构 | `DONE` | 已建立 Vite 8 + React 19 + TypeScript 前端和 Python 3.12 + FastAPI 后端；生产 HTML 禁止缓存、哈希资源长期缓存、缺失资源返回 404，懒加载构建失效时只自动刷新一次并提供中文恢复页 | `npm ci`、`uv sync --locked`、前端完整检查、后端 Ruff/pytest、开发代理、生产 SPA 刷新和双构建替换恢复均通过 |
| 数据库与迁移 | `DONE` | 已使用 SQLAlchemy 2、SQLite 和 Alembic 建立课程结构、每日记录、六步流程、AI 交互、结构化练习单题与作答、引导回顾/重构、作答附件、错题、衔接问题、本地设置、AI 运行记录、三层学习记忆、结构化交接、AI 每日摘要、参考材料、PDF 解析元数据、版本快照、刷新状态、材料会话、课程完成摘要、模型运行快照和稳定笔记相对路径 | 最新且唯一迁移头为 `20260808_0016`；从空库升级后 `integrity_check=ok`，`alembic check` 无漂移 |
| 课程管理 | `DONE` | 已实现课程创建、列表、关键词搜索、真实小节进度摘要、详情、编辑和删除；进行中课程按最近学习倒序，未开始课程按创建时间倒序，已完成课程按完成时间倒序并置底 | API 排序测试及隔离生产端点 `active > not_started > completed` 实测通过；课程完成约束和级联删除回归通过 |
| 章节与小节 | `DONE` | 已实现章节、小节增删改查、排序字段和小节三态流转；课程阅读区默认隐藏新增表单，章节标题行和各章节操作区按需展开对应入口，草稿恢复时自动重开 | API CRUD、父对象 404、状态流转和级联删除测试通过；前端交互测试与隔离浏览器已验证默认隐藏、展开、聚焦和取消 |
| 每日学习记录 | `DONE` | 已实现按日期幂等打开、最近记录与上一组衔接问题回看，以及六步节点所需内容和状态保存；课程详情使用单行摘要收纳小节历史，按需展开最近 3 条或全部记录，并区分开始/继续学习。历史展开只属于当前页面，离开再返回时自动收起 | API 历史上下文和注入日期的跨自然日测试确认上一日 3 条衔接问题进入次日回顾提示词；前端回归和隔离页面实测覆盖展开、进入记录、返回后收起 |
| 固定学习流程 | `DONE` | 六个固定节点均可展开/收起；普通完成或跳过后自动收起并推进。结构化练习完成会进入批改；批改结果保持展开并由用户明确完成后进入收尾；今日摘要生成后在当前页面保持展开 | 状态约束、旧节点迁移、普通/结构化练习推进、批改结果阅读确认、今日摘要留存、跳过确认和小节完成通过 |
| 内置 AI 协作 | `DONE` | Codex App Server 完成全部学习任务，运行记录驱动真实阶段、耗时和页面恢复；Antigravity 模型发现兼容旧版单列显示名、单列 ID 和 `1.1.12` 的 `ID + 显示名` 双列输出；长任务可取消且拒绝重复请求 | UTC 序列化、Codex `turn/interrupt`、Gemini 子进程终止、三种模型列表格式和真实 `agy 1.1.12` 状态回归通过 |
| 参考材料 | `DONE` | PDF、多作用域 URL、视频字幕和页面内嵌逐字稿可解析；Windows 安装包内置锁定版本的 Tesseract 与 `chi_sim+eng`，原生文本优先，扫描页自动 OCR；单页失败保留其他页的解析结果和警告。同一作用域允许多个解析成功的重点材料，章节/小节材料从课程页弹窗管理 | 真实中英文扫描 PDF、原生文本、加密文件、部分页失败、OCR 缓存、多重点材料和材料弹窗回归通过 |
| 练习与作答 | `DONE` | 一次练习包含 12 个结构化单题和逐题作答；非选择题可用紧凑入口附加最多 5 个图片/PDF（单个 10 MB）。图片原件以 Codex `localImage` 多模态输入批改，OCR 只作辅助并在界面标明；PDF 继续使用文本提取。附件与作答进入完整备份，单选/多选题仍由本地答案键确定；题目卡片不显示“依据”小字 | API 覆盖类型/大小/数量、上传、原图输入协议、OCR 辅助、PDF 文本、仅附件作答、批改和删除；前端回归确认选择题无入口、非选择题入口紧凑且可上传，并显示实际批改输入方式 |
| 错题与统计 | `DONE` | 批改中的错误或部分正确题目提供“整理本题”；表单纵向展示对应原题和全宽正确答案，注意点与紧凑错误类型同组；索引详情以完整原题、正确作答和注意点为主，并标记无法关联单题的旧版记录 | API 验证已批改状态、题目归属、错误结论和重复整理；前端回归及隔离页面覆盖字段简化、题答对应、旧版标记与响应式布局 |
| 今日收尾 | `DONE` | 已实现固定提示词、恰好 3 条结构化衔接问题保存、今日完成及同小节下一条记录自动显示 | API 校验/上下文/下一条记录测试和页面保存流程通过 |
| Obsidian 笔记 | `DONE` | 显示标题与稳定存储路径分离；Windows 非法字符、保留名、长标题和冲突由后端安全化并持久化。生成笔记正文不再要求显示材料引用，同组保存按钮保持等高等宽和单行文字；原子写入、外部修改确认和双栏预览保持不变 | 提示词契约、路径迁移、冲突、重命名稳定、Markdown/GFM、按钮桌面与移动端布局回归通过；后端结构化交接仍保留来源引用 |
| 搜索与导出 | `DONE` | 笔记搜索和 Markdown 导出保持不变；材料候选块默认使用 SQLite FTS，用户可在设置中下载约 220 MB 多语言模型并启用语义混合排序。全文/向量索引是可重建 sidecar，不进入业务数据库或备份 | FTS 中文排序、语义设置成功/失败/关闭、模型下载和 252 MB 实际缓存、中文改写查询排序、向量失败回退及归档排除通过 |
| 学习记忆 | `DONE` | 课程页可编辑用户维护的课程记忆，并查看独立自动课程脉络、章节记忆和小节记忆；每日完成由一次 Codex 调用生成紧凑摘要并同时更新小节和章节记忆，手动整理作为修复入口 | 三层记忆 API、AI 每日摘要、全部学习来源聚合、课程概览不覆盖、结构化生成和持久化测试通过 |
| 设置 | `DONE` | 模型连接、Vault、字号、材料库和混合检索保持不变；新增完整备份下载、导入预检和桌面重启恢复，SQLite、材料、作答附件与受管笔记可迁移到新机器；Markdown 保留为独立内容导出 | 归档哈希/路径/数据库校验、旧版兼容、替换回滚、笔记冲突和跨机器 Vault 改绑由测试覆盖；设置页桌面与 390px 入口、预检弹窗和禁用边界已验收 |
| Windows 安装与启动 | `DONE` | 安装包命名为 `install_Lumina-版本号.exe`，内部卸载组件位于 `uninstall_Lumina`；启动器校验健康接口与本机前端构建一致后用构建标识 URL 打开课程页，运行中卸载仍先等待 PID 对应服务完全退出 | 启动器 28 项测试覆盖构建匹配和 URL；QA 运行中卸载、用户文件边界、默认数据保留及生产安装器编译通过 |
| 公开发布 | `DONE` | `v0.1.3` Public Preview 已于 2026-09-02 再次同版本刷新，包含 Windows 安装器、源码 ZIP、发布清单、CycloneDX SBOM 和 SHA-256 文件；`v0.1.2` 历史 Release 保留不变 | 本地全量检查、主分支与标签 GitHub Actions 均通过；5 个远端资产的大小和 digest 与本地逐项一致 |

## 当前正在进行

- 当前任务：图片多模态作答、页面顶部定位、收尾公式紧凑化、未保存状态自动收敛、自动化测试、Windows 构建和 `v0.1.3` 同版本发布刷新已完成。
- 最近完成：隔离浏览器验证长课程页从 `scrollY=1739` 进入每日记录后回到 `scrollY=0`，并验证“今日完成”先自动保存可见表单再显示未完成项提示；最终自包含 EXE 和安装位置程序均在隔离数据目录健康启动。
- 本轮范围：只修改作答附件到 Codex 的多模态输入链、对应输入方式展示、公式/Markdown 规范化、路由滚动和表单保存状态，以及规格与回归测试；未新增生产依赖、数据库迁移、真实模型内容调用或备份恢复。功能提交、标签和同版本 Release 已发布。
- 验收结论：发布包 `Lumina.exe` SHA-256 为 `87BB9E83F1395B0DE8CD85845661B0846421C6DAFB215DEA56DF3B4C7A4137AD`，安装器 SHA-256 为 `7DC9E93FB5AF799D87AC99274C3AF65671DF1534654E326FE1F35A06FA3968F6`；隔离数据目录启动约 2.55 秒后健康，内置 Tesseract `5.5.0.20241111` 及中英文语言包可用。正式安装位置和 `%LOCALAPPDATA%\Lumina` 均未在发布整理中改动。

## 下一步

1. 通过 GitHub Issues 收集 `v0.1.3` 的安装、备份恢复、练习附件和学习流程使用反馈。
2. 后续公开版本优先配置可信 Windows 代码签名，消除 SmartScreen 未知发布者提示。
3. 只在确认真实需求或缺陷后进入下一阶段，不扩大第一版范围。

## 阻塞与待确认

- 课程大纲自动生成是否进入第一版仍待确认；在确认前不应阻塞手动创建章节和小节。
- 真实模型生成仍受 Codex/ChatGPT 和 Antigravity 官方账号权限、会员规则及服务可用性约束；长笔记已改为后台运行和可恢复，但模型本身仍可能超时，需要从原入口重试。
- YouTube 等远端来源仍可能返回 HTTP 429；已有成功版本会继续可用并显示刷新错误，首次导入失败则需稍后同源重试。
- Windows 安装包内置 Tesseract 与 `chi_sim+eng`；识别结果可能存在字符误差，仍需用户复核。普通文本 PDF 不执行 OCR，源码构建者需按开发文档准备锁定的 OCR 运行时。
- 语义混合排序是本机可选能力，首次启用需下载约 220 MB（当前模型缓存实测约 252 MB）；关闭、缺失或失败时自动退回全文与既有匹配。
- Windows 下不得优先直接调用 Microsoft Store 的 WindowsApps Codex 路径；后端通过 npm `codex.cmd` 定位随包原生 `codex.exe`。当前生产服务已使用该路径验证 Codex 连接成功，不需要管理员权限或扩大 ACL。
- 提示词模板仍为内置固定版本；后续模板编辑器需单独确认。
- 当前本地构建未进行代码签名，直接分发时可能触发 Windows SmartScreen；功能与安装生命周期已通过，但正式公开发布前应配置可信证书。

## 已确认技术与产品决策

- 对外品牌名为 `Lumina`，公开项目标识为 `lumina-study-coach`，完整描述名为“Lumina · 学习流程教练”。紧凑界面只显示 Lumina，“学习流程教练”继续用于类别和 AI 角色描述。
- 品牌图标使用 ImageGen 生成的暖色打开书本与三束光方向；正式 1024px 主图生成 Windows 16-256px ICO、Web favicon 和 Apple Touch Icon。操作按钮仍使用 Lucide 图标。
- 品牌迁移不重命名 `runtime-data/learning-flow-coach.db`、`learning-flow-coach.*` 浏览器存储键、服务健康身份或既有 AI 内部标识，避免已有数据、偏好和会话失联。
- 源码型卸载默认只移除入口；生成环境清理只允许 `.venv`、`node_modules` 和 `dist`，完整清理额外允许 `runtime-data` 且必须双重确认。项目源码和 Obsidian Vault 永不进入删除清单。
- 前端使用 Vite 8、React 19、TypeScript 和普通 SPA 路由。
- 普通 SPA 路由使用 React Router 数据 loader 预取目标页数据；站内切换期间保留当前页面，超过 150ms 才显示顶部 2px 进度线。直接打开深层路由使用同款细进度线作为初始 fallback，不渲染独立骨架页。
- 侧栏“课程”在 `sessionStorage` 中记录当前会话最后访问的课程列表、课程详情、每日记录或流程内笔记路由；设置中的第一次点击恢复该位置，恢复后或已经位于深层课程工作区时再次点击返回全部课程列表。独立 `/notes` 与 `/notes/:sectionId` 始终归属“笔记”且不覆盖最近课程位置。位于全部课程时保持当前页面，不使用双击计时；新的浏览器会话默认进入课程列表。
- 展开侧栏默认和最大宽度为 `236px`、最小宽度为 `144px`，支持窗口级指针拖拽、方向键及 Home/End，并把宽度保存在浏览器本地；不大于 `168px` 时使用紧凑留白保证内容完整，折叠宽度继续为 `64px`，移动端不显示拖拽条。
- 后端使用 Python 3.12、FastAPI、Pydantic、同步 SQLAlchemy 2 和 Alembic。
- 业务数据库使用 SQLite；第一版不引入异步数据库驱动或外部搜索服务。材料 FTS 和向量使用同目录独立 SQLite sidecar，属于可重建派生索引，不进入业务迁移、备份或事实来源。
- 课程、章节和小节显示标题不再直接作为可变笔记地址。后端在小节创建或迁移时持久化一次安全三层相对路径，Windows 非法字符使用可读全角字符，保留名、截断和冲突追加稳定 ID；后续重命名不移动文件，已有旧版两层路径继续按显式迁移规则处理。
- 前端使用 npm 和 `package-lock.json`；后端使用 uv、`pyproject.toml` 和 `uv.lock`。
- 开发时前后端分开运行；本地正常使用时由 FastAPI 同时提供 API 和构建后的 SPA 静态资源。
- FastAPI 启动时从 `index.html` 计算前端构建标识并验证 module 入口存在；HTML 使用 `no-store`，哈希 `/assets/` 使用 immutable 缓存，其他静态文件重新验证，缺失的静态文件返回 404。启动器必须匹配该构建标识后再打开页面，前端只对已识别的动态 import/chunk 错误执行一次受保护刷新。
- 前端样式使用 CSS 自定义属性和普通 CSS/CSS Modules，不使用 Tailwind 或大型 UI 组件库。
- 第一版不引入 Next.js、Electron/Tauri、Redux、Axios、Docker 或通用动画框架。
- Node.js 最低版本为 `22.12`，当前使用 Node.js 22 LTS；Python 固定为 3.12。
- 依赖补丁版本由 `package-lock.json` 和 `uv.lock` 固定，只在实现对应功能时添加依赖。
- 产品形态是本地 Web 工具，第一版不做登录、云同步、Electron/Tauri 桌面壳和移动端专项适配；Windows 自包含安装包仍在浏览器中提供界面。
- 第一版不调用模型 API；ChatGPT 使用 Codex App Server，Gemini 使用 Antigravity CLI，提示词复制粘贴作为回退。
- Codex 登录使用 App Server 的 `account/login/completed` 通知记录成功或失败，Web 按登录标识轮询真实结果，不再以 provider 是否连接代替登录结果；令牌交换网络失败会在设置页显示站内错误。
- Codex 运行时返回刷新令牌撤销或认证失效时，后端转换为明确中文错误并将本进程提供方状态标记为未连接；前端在发起任务的位置显示“前往设置”，不只在页面顶部显示全局错误。
- Codex 默认使用 App Server 账号可用列表中的 `gpt-5.5` 和 `Medium`；设置页从 `model/list` 实时读取模型及其支持的思考强度并保存本机首选项。模型或强度不可用时明确提示更新 CLI 或检查账号权限，不尝试静默回退。
- Gemini 登录和生成使用官方 Antigravity CLI；Web 启动可见交互窗口完成 Google 登录，默认使用 `Gemini 3.5 Flash (High)` 的非交互 `--print` 模式。设置页从 `agy models` 实时读取 Gemini 模型与强度变体，不展示其他提供方模型，不读取或复用登录令牌。
- 每次 AI 任务在启动时固定模型与思考强度快照并写入运行记录，运行中修改设置只影响后续任务。材料基座按 Codex 模型隔离；切换模型后使用该模型对应的基座，不把其他模型的会话锚点混入新任务。
- 课程、章节、小节、参考材料、旧版练习和错题属于持久化删除对象，统一经过两层站内确认；只有第二层最终确认可以调用删除 API，两层都禁止点击遮罩关闭。
- 本地日常服务必须由当前用户桌面或开始菜单中的安装包启动器或源码 `pythonw` 启动器运行；启动器从当前用户环境重新加载代理配置，并在健康检查通过后打开浏览器。`start-local.cmd` 仅作为显示终端日志的诊断入口。由 Codex 沙箱等受限自动化环境启动的长期服务会阻止外部 CLI 创建子进程，应用在 Windows `WinError 5` 时给出普通用户启动恢复指引，不扩大 ACL、不注册 Windows Service，也不要求管理员权限。
- 安装/修复脚本优先复用现有 Python 3.12 虚拟环境，其次使用系统 Python 3.12，均不存在时才联网安装；缺少 uv 或 Node.js 时必须先征求用户确认再调用 WinGet。每次安装或升级在迁移前使用 SQLite Backup API 创建一致性备份，最多保留最近 5 份。
- Windows 发布包使用 PyInstaller `onedir` 内置 Python 运行时，并由 Inno Setup 提供中文标准安装向导和目录选择；安装包命名为 `install_Lumina-版本号.exe`，内部卸载文件收纳在 `uninstall_Lumina`。普通安装不依赖 Node.js、Python、uv 或 npm。程序目录与 `%LOCALAPPDATA%\Lumina` 数据目录分离，默认卸载保留数据，交互删除需要二次确认和成功备份。
- 安装包停止与卸载优先读取数据目录中的 `service.pid`，在关闭请求后等待对应进程真正退出；PID 缺失时才使用健康接口和端口作为退路。停止超时必须中止卸载，不能继续删除被占用的程序文件。卸载只递归清理明确拥有的 `_internal`，不递归清空用户选择的整个安装目录。
- 桌面服务使用稳定的 Windows 命名互斥和健康接口识别重复启动；安装包运行数据写入 `%LOCALAPPDATA%\Lumina`，源码运行数据写入被忽略的 `runtime-data`。启动器始终打开课程首页；欢迎框只负责进入产品，不强制配置模型、Vault 或个人描述。网页关闭仅在同源 JSON 请求、明确确认且确实由桌面启动器管理时可用，普通诊断 Uvicorn 返回 409。
- Antigravity 登录任务在可见交互窗口运行期间轮询官方 `models` 命令，账号可用后自动结束登录进程；设置页使用不可误关的站内进度框并支持取消。账号连接与 `Gemini 3.5 Flash (High)` 可用性分别表示，High 不可用时保持已连接并显示警告，不静默降级。
- AI 子进程和 URL 材料抓取都会检查继承的本机代理：仅当 HTTP/HTTPS 代理不可达且存在可达的 `ALL_PROXY` 时回退，不硬编码个人代理地址，也不改写远程或企业代理。
- 当可达代理只存在于 `ALL_PROXY` 时，AI 子进程需要同时补齐大小写 HTTP/HTTPS 代理变量，覆盖浏览器授权成功但 CLI 令牌交换直连失败的场景；只修改子进程环境，不修改 Windows 用户或系统代理。
- 每个小节维护独立材料 revision、稳定块索引和只读工作区。系统不为“确认已读”额外调用模型；首个业务节点使用相关原文块并可按需读取完整文件，成功后成为该 revision 的锚点，后续节点从锚点 fork。新增材料或扩围继承旧锚点并暴露新增文件，内容替换、刷新、删除、停用或缩围建立新 revision。
- 材料 revision 锚点必须使用可跨进程恢复的持久 Codex 线程；旧版短生命周期锚点返回 `no rollout found` 时，只清理当前失效锚点并重建一次，不影响材料版本和历史学习记录。
- 内置示例是前端构建中的脱敏静态只读内容，在课程首页作为特殊课程行并通过懒加载 `/example` 提供；它不创建业务对象、不请求 API 或模型、不读写 Obsidian，也不参与课程统计、搜索和导出。两次确认删除只写入本机浏览器隐藏偏好，安装器仍不向数据库写入演示数据。
- 生成中、成功和失败反馈紧邻发起任务显示，只有当前按钮进入忙碌状态；重新生成后必须以最新服务端结果替换当前阅读区，预习问题编辑框也同步重建。
- 长时间 AI 任务以持久化 `ai_runs` 为唯一恢复依据，API 中所有运行时间按带时区 UTC 输出，前端把旧无时区时间按 UTC 兼容解析并显示真实阶段和耗时，不虚构百分比；页面离开再返回继续观察同一任务，服务进程重启时把遗留 `running` 记录转换为可重试的失败状态。
- 材料解析失败保留原来源和错误，用户只能对同一 PDF 或 URL 执行重新解析；系统不自动选择替代材料。来源依据分别展示任务可用材料和模型实际声明引用，revision 与哈希折叠为技术详情。
- 前端页面组件按普通 React Router 路由懒加载，loader 继续负责切换前预取数据；Markdown/KaTeX 作为按需共享块，不回退到会产生中间页面的页面级加载逻辑。
- 共用 Markdown 渲染链固定为 `react-markdown`、`remark-math`、`remark-gfm` 和 `rehype-katex`。GFM 表格使用可聚焦的局部横向滚动容器，不能造成整页横向溢出；任务列表复选框需要覆盖全局表单输入尺寸，保持只读紧凑显示。
- Codex 使用项目专用且被忽略的 `runtime-data/ai/codex-home`，不修改全局 Codex 配置；Antigravity 使用 CLI 自身的用户级登录状态。
- 参考材料按课程、章节或小节作用域保存；课程材料向下继承，章节材料只在本章可用，小节材料只在当前小节可用。同一作用域允许多个解析成功的重点材料；首个成功材料默认标记为重点，失败材料不能标记。
- 每日学习记录的“添加材料”固定创建当前小节材料，不显示范围选择；课程级和章节级共享材料继续由课程详情或设置材料库管理。
- 课程详情不横向平铺全部每日记录：默认显示记录次数、最近日期和最近状态的一行摘要；展开后先显示最近 3 条，可继续查看全部，同一时间只展开一个小节。展开状态仅保存在当前页面组件中，离开课程详情再返回时默认收起，不进入 `sessionStorage` 或业务数据库。
- 同一作用域的第一份材料由后端自动设为主材料，添加弹窗不展示额外主材料勾选；已有材料时仍通过列表操作显式切换主材料。
- PDF 和 URL 快照保存在被忽略的 `runtime-data/materials/<material-id>/`；删除材料、课程、章节或小节后同步清理对应本地文件。PDF 原生文本优先，仅对文字极少且含图像的页面使用安装包内置 Tesseract `chi_sim+eng`；页级缓存按来源哈希、页码、语言、DPI 和解析器版本失效。
- 课程列表由后端提供 `course_state`、`last_study_at` 和 `created_at`：进行中按最近学习时间倒序，未开始按创建时间倒序，已完成按完成时间倒序并置底；示例行不参与 API 排序和数量。
- 材料检索始终先建立本地 FTS；用户明确点击后才下载 Apache-2.0 多语言嵌入模型并启用语义混合排序。模型、OCR 缓存、全文/向量 sidecar 都是可重建数据，不进入备份；完整备份保留业务数据库、原材料、作答附件和数据库登记的 Lumina 小节笔记。
- 同一任务和业务范围已有运行记录时拒绝重复启动；页面显示真实已用时间并允许取消。取消会调用 Codex `turn/interrupt` 或终止 Antigravity 子进程，运行记录保留可重试失败原因。
- 单选和多选题由结构化 `answer_key` 在本地确定正确或错误，模型只生成解释；问答、推导和计算题继续由模型按 rubric 评价。
- 每日记录默认启用所有可用且解析成功的继承材料，用户可以关闭单项或保存本次页码/章节范围。Codex 获得相关原文块和完整材料工作区的只读访问权，材料正文始终标记为不可信参考；每次运行记录材料 revision、清单哈希和经过当前候选块校验的实际来源定位。
- 课程是顶层对象，课程下包含章节和小节；学习按小节组织，活动按每日记录保存。
- 每日学习流程使用固定节点，节点顺序以产品规格为准；节点均可手动折叠，记录打开时默认展开首个未完成节点。普通完成或跳过后自动收起并推进；AI 新生成的评阅、逐题批改和今日摘要先保持展开，结构化批改由用户明确完成后再推进。
- 学习流程按钮使用“评阅回顾”“检查重构”“生成练习”“批改答案”“生成预习问题”等任务名称，不重复添加“AI”前缀；提供方名称只在连接设置和任务运行反馈等需要区分模型的位置出现。
- Obsidian Markdown 是小节结构化笔记的主来源，Web 负责编辑、索引和流程。
- 一小节对应一个 Markdown 文件，第一版路径为 `课程名/章节名/小节名.md`；旧版两层路径可读取，并在下一次保存时迁移。
- 笔记不强制固定文档结构，笔记页不实现独立的文档结构侧栏。
- 视觉采用暖白、陶土橙和鼠尾草绿的轻量工作台风格。
- UI 使用自托管的 `Nunito Sans Variable` 和 `Noto Sans SC Variable`；字体包采用 OFL-1.1 许可，不依赖 CDN 或本机预装字体。
- 编辑和确认操作统一使用基于 HTML `<dialog>` 的站内 `AppDialog`，不使用 `window.prompt`、`window.confirm` 或 `window.alert`。单字段标题编辑复用 `TextEditDialog`，删除、跳过和继续学习复用 `ConfirmDialog`；课程编辑使用中等宽度站内表单。对话框覆盖完整视口，桌面宽度为 460px/620px，移动端左右各保留 16px，并统一处理遮罩、初始焦点、`Esc`、安全取消、请求中禁用、错误反馈和关闭后焦点恢复。数据输入弹窗禁用遮罩关闭；确认弹窗仍可将遮罩和 `Esc` 视为安全取消。创建章节/小节、状态选择和学习内容仍保持内联。
- 应用根部维护单一未保存状态注册表；每日学习记录、小节笔记、创建/编辑课程、章节/小节标题、课程新增章节/小节草稿和设置手动 Vault 路径通过同一提供器使用 React Router `useBlocker` 拦截站内导航。离开弹窗提供继续编辑、不保存离开和保存并离开，禁用遮罩关闭和冗余关闭图标。保存并离开只持久化当前内容，不修改节点完成状态；保存失败或 Obsidian 外部修改冲突时继续停留。浏览器原生 `beforeunload` 监听器仅在任一来源为 `dirty` 时注册，保存或放弃后移除；提示内容与是否展示由浏览器控制。受保护文本输入同时写入版本化 `localStorage` 草稿，服务器基线一致时才恢复，保存或放弃后清除。
- 草稿恢复反馈不再复用成功横幅：使用无填充的紧凑状态行显示恢复来源，并在 2.6 秒开始淡出、2.9 秒移除；同一行的未保存数量保持陶土橙并持续到保存或放弃。保存和复制成功反馈使用内容宽度的鼠尾草绿提示并在 3 秒后清除，错误提示仍保持整行。
- 学习节点完成前按字段配置检查实际内容，并通过共用站内对话框只列出未完成字段。软性字段可确认后完成；练习全部未完成时使用既有跳过语义；预习问题必须保持三条完整内容；今日完成会检查前置节点状态。任何确认完成操作都保持“先保存成功、再更新节点状态”的顺序。
- 完成前对话框的取消操作统一命名为“继续完成”，点击后保留输入并恢复原完成按钮焦点。
- 所有按钮、导航链接、折叠标题、课程条目和选择控件统一提供指针、悬停、按下和 `focus-visible` 状态；按下以轻微缩放或位移表达，不改变稳定布局尺寸，并遵守“减少动效”偏好。
- 课程详情和每日记录的返回入口统一使用共用 `PageBackBar`；工具栏覆盖左侧导航以外的整个主工作区，总高度固定为 44px，返回按钮不再跟随居中正文宽度，桌面距主工作区左侧 16px，移动端距视口左侧 12px。组件使用 1px 定位标记和 `IntersectionObserver` 判断是否真正吸顶：顶部状态使用 `#f8f5ef`/`#eee7de`，吸顶活动状态使用 `#f6f2ec`/`#e8dfd4`，唤醒颜色以 420ms `cubic-bezier(0.22, 1, 0.36, 1)` 过渡；滚动停止 1.5 秒后，栏背景和底线以 600ms `cubic-bezier(0.16, 1, 0.3, 1)` 变为透明，滚动、悬停或聚焦返回按钮时恢复。按钮统一只显示“返回”，保持 40px 点击高度、500 字重、16px/1.7 线宽图标和轻边框，并在空闲状态继续可见可操作。
- 每日学习主区域在 1920px 及以下逻辑视口最大宽度为 `1040px`，`2200px` 起扩展到 `1280px`，`3200px` 起扩展到 `1440px`；课程详情和笔记等上下文宽页面可对应扩展为 `1080px`、`1320px` 和 `1520px`。课程、错题、笔记和设置等顶层管理页使用 `1080px`、`1320px` 和 `1520px` 三档工作区，确保同一视口下切换时左边界和宽度稳定。移动端继续保留左右各 `18px`。
- `design-references/` 只作为视觉和布局参考，不作为逐像素验收标准。
- 默认 SQLite 数据库位于仓库内被忽略的 `runtime-data/learning-flow-coach.db`；测试始终使用 pytest 临时目录中的独立数据库。
- FastAPI lifespan 启动时执行 `alembic upgrade head`，schema 变化只通过 migration 管理，不调用运行时 `create_all`。
- 数据模型保持规范化：章节关联课程，小节关联章节，每日记录关联小节；课程和章节信息通过关系读取，避免冗余外键漂移。
- 同一小节同一天最多一条每日记录；首次创建时初始化 6 个固定节点，并将未完成小节转为进行中。小节笔记和小节完成不属于每日节点。
- 闭卷回顾、材料学习和主动重构使用每日记录上的明确文本字段，避免在第一版引入通用动态表单系统。“遗忘与卡点”和“自我讲解”旧数据库字段仅为兼容已有数据保留，不在当前页面显示，也不进入新生成的固定提示词。
- 闭卷回忆评价、主动重构评价、出题和批改提示词由后端固定模板生成，并保存生成时的完整文本和用户粘贴的 AI 反馈。
- 一次 AI 出题结果仍是一组练习，但内部规范化为 12 个单题和一一对应的作答；固定 4 道选择题，整组一次生成、一次批改，不在批改阶段自动追问。
- 只有练习节点允许跳过，且 API 与页面都要求显式确认；今日完成独立标记每日记录和“今日收尾”节点。小节通过笔记页的独立操作完成。
- 错题规范化关联到整次练习，并可选关联到具体单题；通过练习和每日记录关系获得小节、章节和课程上下文。第一版仍由用户确认后整理，不自动持久化所有 AI 判错。
- 错题汇总不新增统计表：同一只读接口分别返回完整课程层级和错题上下文条目；筛选选项来自课程层级，结果与统计来自错题条目，避免零错题课程从筛选器中消失。
- 今日收尾按每日记录保存一组固定提示词和恰好 3 条结构化衔接问题；下一条同小节记录读取最近一组完整问题。
- Obsidian vault 路径保存在被忽略的本地 SQLite 设置表中；笔记正文不进入数据库，Markdown 文件始终是主数据源。
- 笔记索引只遍历数据库中已有课程、章节和小节，并逐个解析对应的管理路径；不会递归扫描 Vault，也不查询或返回每日学习记录 ID。单个路径冲突作为索引问题返回，不阻断其他可读笔记。
- Markdown 导出由后端根据用户选择在内存中生成 ZIP，不在仓库、运行目录或 Vault 中创建导出副本。压缩包按课程/章节/小节分层，课程概览、每日记录、AI 评阅、练习与批改、错题和小节笔记分别保存；第一版不提供日期范围、JSON 或 CSV 导出。
- 完整备份与 Markdown 导出严格区分：完整备份使用版本化清单、逐文件 SHA-256 和 SQLite 完整性检查，可由桌面启动器恢复；恢复前自动创建安全备份并在失败时回滚。受管笔记写入用户明确选择的新 Vault，遇到同路径不同内容时停止而不覆盖。
- Obsidian Vault 候选只读取 Obsidian 自身的 `obsidian.json`，不递归扫描磁盘；Windows 原生目录选择只返回候选，前端必须显示路径、`.obsidian` 和可写状态并由用户确认后再复用现有设置 API 保存。非 Windows 或选择器不可用时保留手动绝对路径入口。
- 桌面侧栏默认展开为 `236px`，可平滑收起为 `64px`，状态保存在浏览器 `localStorage`；720px 以下继续使用完整顶部导航，不应用桌面收起宽度。
- 界面字号、笔记编辑字号和减少动效属于设备级 UI 偏好，保存在浏览器 `localStorage`，不进入业务数据库；字号使用 CSS 自定义属性分级调整，不使用页面整体缩放。小/标准/大正文为 `10px/14px/18px`，页面标题为 `27px/31px/35px`，笔记编辑器为 `10px/14px/18px`；分段选项比同档正文小 `1px` 且最低 `9px`。
- 课程创建复用中等宽度 `AppDialog`，课程列表不再常驻创建表单；创建成功后直接进入新课程以继续建立章节和小节。
- 笔记文件只允许 `课程名/章节名/小节名.md`：拒绝 Windows 非法路径片段和同章节同名小节，不同章节允许同名；所有解析后的目标路径必须位于已配置 vault 的课程目录内。旧版 `课程名/小节名.md` 仅在归属唯一时读取和迁移，新旧文件并存时拒绝静默覆盖。
- 笔记写入使用同目录 UTF-8 临时文件原子替换；Web 打开时记录纳秒修改时间，保存前发现外部变化则返回冲突，并通过站内确认框显式强制覆盖，不做内容合并。
- Markdown 实时预览使用锁定在 `package-lock.json` 中的 `react-markdown`、`remark-math`、`rehype-katex` 和 `katex`。模型输出移除全文代码围栏并将 `\(...\)`、`\[...\]` 规范为 `$...$`、`$$...$$`；既有用户笔记只在预览时兼容，源文件必须由用户显式转换。
- 旧式公式兼容同时覆盖列表项中分行的 `- \[`、公式正文和 `\]`，以及同一行定界符；规范化结果保持列表结构并交给 KaTeX 渲染，不把 Markdown 标题或定界符显示为普通文本。
- AI 生成内容默认进入渲染阅读模式，用户显式点击后再编辑；运行中显示任务名称、加载动画和耗时，不使用虚假百分比或逐字流式渲染。
- 桌面笔记页固定为视口剩余高度，编辑器和预览各自独立滚动；720px 以下使用编辑/预览分段切换并只显示一个面板，不实现文档结构侧栏。

## 最近验证结果

- 文件检查：已确认产品规格和 6 张参考图存在。
- 图片检查：6 张 PNG 均可正常读取，尺寸为横向桌面构图。
- 规则检查：已完整读取并核对 `AGENTS.md` 和本文件；实施前阅读、完成后更新、状态定义、验证要求和参考图边界保持一致。
- 环境检查：Node.js `22.20.0`、npm `11.6.1`、Python `3.12.x`、Git、SQLite CLI 和 uv `0.11.28` 已安装。
- uv 验证：已从 WinGet 实际安装路径成功执行 `uv --version`，用户级 PATH 已包含该目录；当前已运行的 Codex 进程需要重启后才能直接解析新 PATH。
- 依赖安装：uv 已锁定 SQLAlchemy `2.0.51`、Alembic `1.18.5` 及其传递依赖；前端锁定 `@fontsource-variable/noto-sans-sc` `5.2.10` 和 `@fontsource-variable/nunito-sans` `5.2.7`。
- 前端检查：最终 `npm run check` 完成 ESLint、TypeScript、60 个 Vitest 测试和 Vite `8.1.4` 生产构建；覆盖局部任务反馈、重新生成同步、材料固定小节范围、Markdown 数学规范化、KaTeX 渲染、阅读/编辑切换，以及 Antigravity 登录进度和取消。加入 KaTeX 后单入口脚本约 825 kB，Vite 给出非阻塞 chunk 提示；第一版暂不为此扩大到路由拆包。
- 后端检查：Ruff 输出 `All checks passed!`；pytest 收集并通过 40 个测试，覆盖 Codex 登录失效、受限启动引导、Antigravity 交互进程未退出时完成登录和首选模型不可用状态。FastAPI TestClient 仍产生 1 条上游 `StarletteDeprecationWarning`，不影响功能；本轮未修改数据库 schema，因此未重复执行 Alembic 漂移检查。
- 开发模式：`http://127.0.0.1:5173/api/health` 经 Vite 代理返回健康 JSON，前端页面显示“后端已连接”。
- 生产模式：`http://127.0.0.1:8000/daily-records/1/note` 返回 200 HTML，验证新增笔记深层路由的 FastAPI SPA fallback。
- 监听边界：`netstat` 确认 Vite 仅监听 `127.0.0.1:5173`，FastAPI 仅监听 `127.0.0.1:8000`。
- 浏览器检查：实际完成课程创建对话框、侧栏展开/收起、字号即时切换、减少动效、Vault 自动发现和选择确认；原生浏览能力已返回可用，但未替用户更换或保存真实 Vault 路径。
- 响应式检查：课程、运行状态、设置在 1440、2560 和 3840px 下主区域宽度、左边界和标题纵向位置一致，最大宽度均为 1080px且无横向溢出；课程工具栏中的搜索与创建入口保持相邻。上下文页面仍保留高分辨率扩展规则。
- 导航加载检查：数据路由延迟请求测试确认目标数据完成前保留当前课程页且不渲染骨架；浏览器直接打开和站内切换均未出现新警告、错误横幅或骨架元素，真实 404 才显示“不存在”。
- Obsidian 路径检查：真实笔记页只读验收显示 `阶段验收课程/第一章/第一节.md`；未在浏览器验收中保存或修改笔记文件。
- 导航与字体检查：两个页面的按钮文案均为“返回”；顶部状态为 `#f8f5ef`/`#eee7de`，滚动活动状态为 `#f6f2ec`/`#e8dfd4`。滚动停止 1.5 秒后出现 `page-back-bar--idle`，淡出使用 600ms `cubic-bezier(0.16, 1, 0.3, 1)`；栏背景和底线最终透明，返回按钮保持 1.0 不透明度和可交互状态，悬停或再次滚动会恢复活动状态。
- 生产检查：开发代理 `/api/health`、生产笔记深路由和设置 API 均返回 200；笔记页在 1440×900、2560×1440 和 3840×2160 下无重叠或横向溢出。浏览器中的唯一 409 控制台记录来自验收时主动制造的外部文件冲突，页面正确转为站内确认框。
- 仓库边界：文本扫描未发现个人绝对路径；`.gitignore` 排除了环境文件、虚拟环境、构建产物、数据库、本地数据和测试产物。
- 验收数据：阶段 7 使用 `output/playwright/` 下隔离数据库和临时 vault，验收后已验证路径并完整删除；阶段 6 创建的临时课程也已从正常运行库删除，源码扫描未发现个人绝对路径。
- 未保存保护检查：实际浏览器在 1440×900 测得三个操作均为约 `134×38px`，390×844 下均为约 `316×38px` 且改为纵向排列；遮罩点击后弹窗保持，课程草稿、每日记录和设置路径均能拦截站内导航。本轮进一步确认每日记录刷新后以紧凑单行显示“已恢复上次草稿 · 1 处内容未保存”，2.9 秒后仅恢复文案消失，未保存状态继续保留；临时内容通过“不保存离开”清理，控制台 0 error/0 warning，未调用保存接口或修改真实学习数据。
- 完成前检查：实际浏览器在材料学习节点确认提示框显示“请检查：学习范围”，仅提供“继续完成/仍然完成”，不出现输入字符说明；选择继续完成后提示关闭并保留当前输入，确认前没有发送保存请求，未修改真实学习数据。
- 阶段 8/9 页面检查：使用隔离 SQLite 和临时 Vault 由 FastAPI 提供生产构建，无头 Chrome 验证错题页 2 条记录可按未解决状态筛为 1 条并展开详情，笔记页 2 篇可按正文关键词筛为 1 篇，Markdown 下载返回附件并包含课程与错题。1440/2560/3840/390px 均无横向溢出，顶层工作区实测为 1080/1320/1520px，移动端筛选为单列，控制台 0 error。
- 最终运行状态：生产构建已由 FastAPI 在 `http://127.0.0.1:8000` 启动；`/api/health`、`/mistakes` 和 `/notes` 均返回 200，深层页面使用 SPA HTML 回退，`netstat` 确认只监听 `127.0.0.1`。
- 错题筛选与分层导出检查：隔离生产库包含 3 门课程，其中 2 门没有错题；页面仍显示全部课程、完整带上下文的章节和小节，选择空范围后结果为 0 并显示明确空状态。导出对话框在桌面和 390px 下无横向溢出，可选择 1 门课程和 6 类内容；实际下载 ZIP 包含导出说明、课程概览、每日记录、AI 评阅、练习与批改、错题和小节笔记共 7 个文件，未选课程未进入压缩包，控制台 0 error。
- 模型连接检查：旧 FastAPI 由 Codex 受限环境启动时，Codex 和 Antigravity 均复现 `WinError 5`；切换为普通用户环境后，同一接口实机返回 Codex ChatGPT Plus 已连接、Antigravity CLI `1.1.3` 已连接且 `Gemini 3.5 Flash (High)` 可用。设置页刷新状态正常、两个连接点均为成功色，1281×720 无横向溢出且控制台 0 error/0 warning。
- 本机模型连接修复检查：Codex CLI 已由官方 npm 更新到 `0.145.0`，Lumina 解析到 npm 平台包中的原生 `codex.exe`；Google Antigravity CLI 已由官方安装脚本安装为 `1.1.8`。未登录状态下 `/api/ai/provider-snapshot` 实机约 1.05 秒返回，Codex 模型列表可读取，Antigravity 只显示“等待完成 Antigravity 登录”。设置页不再显示“正在读取连接状态”，浏览器控制台 0 error/0 warning。
- v0.1.1 干净环境检查：先删除 `node_modules`、Python 虚拟环境、构建输出和测试缓存，再以 npm `11.6.1` 和 uv `0.11.32` 从锁文件恢复。普通 `npm ci`、前端 88 项、后端 94 项、启动器 13 项测试、全部静态检查、空库迁移和发布元数据通过；清理后的正式应用重新启动后 Codex 与 Antigravity 均保持已连接。
- 依赖审计限制：npm 官方审计中的 `brace-expansion` 已通过非强制锁文件修复；剩余 2 条高危报告属于 React Router RSC Action 模式，Lumina 未启用 RSC。官方自动修复要求强制降级 `react-router-dom` 到 `7.11.0`，本补丁不引入该范围外变更。
- 材料闭环检查：生产构建中添加公开 URL 后生成 1 个正文片段和本地快照，课程级材料在新建小节记录中默认选中，学习范围保存并刷新后保持，生成练习提示词包含材料正文。1440×900 和 390×844 无横向溢出，最终新浏览器会话控制台 0 error/0 warning；验收课程、记录、材料行和本地文件已全部删除。
- Markdown 与笔记工作区检查：生产页面中既有练习默认显示为约 1000px 宽的阅读区；真实旧笔记在不改写源文件的前提下渲染 63 个 KaTeX 公式。1440×900 下页面本身无纵向或横向溢出，左右面板均为约 504px 高的独立滚动区；390×844 下编辑/预览一次只显示一个面板且无横向溢出。浏览器控制台 0 error/0 warning，验收未触发保存、转换或 AI 生成。
- 本轮生产页面检查：每日记录新增材料弹窗在 1440×900 下约 `620×467px`，390×844 下约 `358×509px`；PDF 选择区完整，两种视口均无横向溢出，不显示课程/章节范围选择，并明确说明材料只用于当前小节。既有批改显示 91 个 KaTeX 公式且无原始 `\[`/`\]` 定界符；设置页显示“模型连接”且无应用错误横幅。验收未保存或修改真实学习数据、材料或 Obsidian 文件。
- 最终运行状态：生产构建由更新后的 FastAPI 在 `http://127.0.0.1:8000` 提供；健康接口返回 `status=ok`，课程页可打开且无横向溢出，`netstat` 确认只监听 `127.0.0.1:8000`。
- 阶段 15 页面检查：设置页通过“打开材料库”显示站内工作区弹窗，不再占用侧栏或独立页面；1440×900 和 2560×1440 下筛选栏、材料列表和关闭操作正常，页面无横向溢出。每日记录中的旧版练习显示明确删除入口，站内确认框可安全取消且未删除真实数据。提供方状态分别显示 `GPT-5.5 · Medium` 与 `Gemini 3.5 Flash · High`，Codex 和 Gemini 均提供断开入口；控制台 0 error/0 warning。
- 阶段 24 自动化检查：前端 `npm run check` 通过 ESLint、TypeScript、79 项 Vitest 和 Vite `8.1.4` 生产构建；主入口为 313.96 kB，Markdown 430.01 kB 和各页面继续按路由拆包。后端全仓库 Ruff、80 项 pytest 和 Alembic 漂移检查通过；`httpx2` 锁定后原 TestClient 弃用警告已消失。启动器 Ruff、13 项 unittest 和 3 个 PowerShell 脚本解析通过。
- 阶段 24 安全与恢复：跨站 POST 在隔离生产服务返回 403；同源和无浏览器来源头路径由测试覆盖。完整归档回归覆盖数据库、嵌套材料、哈希篡改、路径穿越、已有数据拒绝、替换恢复和日志保留；未读取或修改真实学习数据库、材料、登录状态或 Vault。
- 阶段 24 页面检查：生产 `/status` 深层路由刷新返回 200 且展示 `Lumina 本地服务 · v0.1.0`，不显示内部服务名；390×844、1440×900、2560 和 3840px 均无横向溢出，390px 顶部导航保持单行五项，品牌图实际加载 192px 资源，控制台 0 error。隔离服务、页面和数据库已全部关闭或清理。

## 实施记录

### 2026-07-28 - 进一步缩小桌面侧栏

- 调整：展开侧栏下限由 `184px` 降到 `144px`，默认和上限继续为 `236px`；宽度不大于 `168px` 时自动收紧侧栏、品牌和导航留白，避免只缩外框造成内容截断。折叠态、移动端、业务页面和数据逻辑不变。
- 验证：前端 95 项、后端 100 项、启动器 28 项测试全部通过，ESLint、TypeScript、Ruff 和 Vite `8.1.4` 生产构建通过。真实生产页面在 `1440×900` 下使用 Home/End 检查 `144px` 与 `236px` 两端；最小宽度下品牌、导航和脚注均无裁切，页面无横向溢出。

### 2026-07-28 - 修复前端构建更新后的懒加载失败

- 根因：应用更新会替换 Vite 内容哈希 chunk，已打开页面仍可能持有旧主入口并请求已不存在的页面 chunk；FastAPI 又把缺失静态文件回退为 `200 text/html`，浏览器无法把响应作为 JavaScript 执行。启动器只等待 API 健康，不校验前端构建，React Router 也没有自定义错误边界，因此用户会看到默认英文应用错误，刷新后才取得新入口。
- 后端与启动器：新增前端构建检查，健康接口报告 `frontend_build_id`、module 入口和完整状态。`index.html`/SPA 回退使用 `no-store`，哈希资源使用 immutable 缓存，缺失 JS/CSS 返回真实 404。源码版和发布版启动器等待健康构建与本机 `index.html` 一致，并在课程 URL 附加构建标识。
- 前端恢复：所有懒加载路由共享 Lumina 中文错误边界；识别动态 import 和 chunk 错误后使用 `sessionStorage` 短时保护自动刷新一次，避免无限循环。第二次失败或普通路由错误显示明确中文说明和重新加载按钮，不再暴露 React Router 默认英文错误页。
- 修改范围：新增 `backend/app/static_assets.py`、`frontend/src/routeRecovery.ts`、路由错误边界及测试；更新 FastAPI 静态托管、两种启动器、路由、样式和对应测试，并同步规格与进度。未修改业务 API、数据库 schema、课程/笔记数据、学习流程、模型连接或版本元数据。
- 验证结果：后端 `100 passed`、启动器 `28 passed`、前端 `95 passed`；Ruff、ESLint、TypeScript、`git diff --check` 和 Vite `8.1.4` 生产构建通过。隔离 `8029` 使用两个真实 Vite 构建：页面先加载旧构建，服务端切换到新构建后旧课程详情 chunk 返回 404，点击课程自动刷新并最终进入新构建课程详情；最终标题、URL、页面结构正确，控制台 0 error。隔离服务、端口、数据库、构建、浏览器状态和缓存均已清理，正式 `8000` 未触碰。

### 2026-07-28 - 修复 Windows 笔记路径并优化课程目录操作

- 根因：笔记路径由实时课程、章节和小节标题直接拼接，Windows 非法字符、设备保留名、深层 Vault 和重命名都会造成写入或索引失败；索引警告只显示小节标题，跨课程同名时难以定位。课程详情又始终渲染章节和每章小节表单，长期阅读时形成重复操作噪声。侧栏宽度固定，不能按工作区调整。
- 后端修复：新增共享安全路径段规则和迁移 `20260728_0013`，为每个小节回填全局唯一的 `note_relative_path`。路径保持可读，必要时追加对象 ID，重命名后稳定；读写继续限制在 Vault 内，Windows 使用扩展长度路径，索引问题新增课程和章节上下文。旧版两层文件读取、冲突提示和下一次保存迁移保持不变。
- 前端优化：展开侧栏可在 `144–236px` 内拖拽，窗口级监听确保指针离开拖拽条后仍继续并在结束时持久化；同时支持方向键和 Home/End，小于等于 `168px` 时自动收紧内部留白，折叠态和移动端不变。章节与小节新增表单改为标题行/章节操作区按需展开，空状态提供同层入口，草稿恢复自动重开。
- 修改范围：后端路径工具、笔记服务、API、模型、schema、迁移及测试；前端 App、课程详情、笔记索引、API 类型、样式及测试；同步产品规格和本进度文档。未修改模型连接、学习流程、材料或版本元数据；正式环境误建的 QA 课程和临时 Vault 设置均已在隔离验收前恢复。
- 验证结果：后端 `98 passed`，Ruff `All checks passed!`，隔离数据库 `alembic check` 为 `No new upgrade operations detected.`；前端 `npm run check` 通过 ESLint、TypeScript、91 项 Vitest 和 Vite `8.1.4` 构建。隔离生产服务实际写入并索引 3 篇含冒号的小节笔记，索引问题为 0；浏览器验证新增表单默认隐藏/聚焦/取消、侧栏拖拽上下限与刷新持久化，以及 `1440×900`、约 `721×900`、`390×844` 无横向溢出，控制台 0 error/0 warning。

### 2026-07-28 - 修复课程详情头部与小节操作区挤压

- 根因：课程详情头部为横向 Flex，长课程信息和操作区同时参与收缩，导致中文按钮逐字换行；小节行在中等宽度仍使用桌面四列，`721-800px` 会撑宽页面，大字号在 `901-940px` 会把学习按钮压成多行。
- 修复：标题区设置 `min-width: 0` 并承担剩余空间，操作区保持内容宽度；`721-900px` 头部改为纵向布局和完整按钮换行。小节行与学习记录复用既有窄屏排列，在 `721-960px` 改为两列并约束展开面板宽度。未全局修改按钮换行规则。
- 修改文件：`frontend/src/App.css` 和本进度文档。
- 验证：`npm run check` 完成 ESLint、TypeScript、88 项 Vitest 和 Vite `8.1.4` 构建；真实 Chromium 对标准/大字号、课程材料关闭/展开、两按钮/含“完成课程”完整操作组执行 60 个头部布局用例。另以隔离响应数据构造已完成小节和学习记录，在 `1440/1100/961/960/940/901/900/800/721/720/390/320px` 执行 24 个小节布局用例，操作按钮均为 `38px` 高且页面、行、操作区和展开记录无横向溢出；课程、课程记忆、设置、笔记和错题等主要页面另执行 24 个回归用例。全部浏览器用例控制台零错误。
- 发布：功能与版本提交为 `528f625`，标签 `v0.1.2` 指向该提交；预发布 Release `360905562` 上传 Windows 安装器、源码 ZIP、发布清单、CycloneDX SBOM 和 SHA-256 文件。安装器 SHA-256 为 `2A200259326C0761BDAF0DD1852B40FAA8BB04D8A6F354220407F19AE829C89B`。
- 远端验证：`main` 和标签触发的 GitHub Actions `30338634091`、`30338632535` 均成功；5 个 Release 资产的大小与 GitHub SHA-256 digest 和本地逐项一致。

### 2026-07-28 - 修复本机模型连接状态读取

- 根因与修复：旧机迁移后的 Codex npm 包版本和目录形态与当前解析器不一致，状态探测又缺少整个调用链的硬超时，导致慢请求长期占据设置页。后端现为 Codex 和 Antigravity 的状态及模型探测设置 10 秒总超时并并发读取，取消时清理待处理请求和半启动进程；前端为快照请求设置 15 秒超时及明确重试提示。
- 本机工具：将官方 npm Codex CLI 从 `0.39.0` 更新为 `0.145.0`，Lumina 已解析到平台包原生 `codex.exe`；通过 Google 官方脚本安装 Antigravity CLI `1.1.8`。账号授权未被复制或修改，仍由用户在设置页分别完成。
- 主要文件：更新 `backend/app/ai_providers.py`、`backend/app/api.py`、`backend/tests/test_ai_integration.py`、`frontend/src/api.ts` 和本进度文档；新增 `frontend/src/api.test.ts`。未修改数据库、学习流程、模型提示词或其他页面行为。
- 安装与运行：本机修复安装前已创建 `%LOCALAPPDATA%\Lumina\backups\lumina-backup-20260728-101658.zip`；安装日志成功，`G:\Lumina\Lumina.exe` 与本次构建 SHA-256 一致，服务重新启动后健康接口返回 `status=ok`。
- 验证结果：后端 `94 passed`，Ruff 输出 `All checks passed!`；前端 `npm run check` 通过 ESLint、TypeScript、88 项 Vitest 和 Vite `8.1.4` 生产构建。真实快照约 1.05 秒返回，设置页显示“连接 Codex”“连接 Antigravity”、CLI `0.145.0`/`1.1.8` 和简短未登录状态，控制台 0 error/0 warning。
- 发布整理：版本元数据、README、测试期望和 CHANGELOG 提升为 `0.1.1`；npm 11 锁文件补齐 WASI 可选传递依赖并修复 `brace-expansion`。清理后的真实应用验证两个提供方均已连接，但未调用付费模型生成。
- 清理结果：删除本轮工作区 CLI 备份、安装脚本和日志、失败安装残留、依赖缓存、测试数据库、浏览器状态、应用日志、安装回滚包及 Codex 可重建状态/插件缓存；保留正式学习数据库、Codex `auth.json`、安装身份和 Antigravity 授权。
- 远端发布：提交 `3c683314d334fc4897dbf6b63c7f140f01c80dd8` 已推送到 `main`，`v0.1.1` 标签指向该提交；GitHub Actions `30324668171` 的 frontend 和 backend-and-launcher 均成功。预发布 Release 上传 5 个资产，安装器 SHA-256 为 `BE859AC0C1593862598D79D00ADCF39C00BDA2C28B55A446A29B92E30A18C27C`，源码 ZIP 为 `C7CD7E58C6071A6BADB574A7C3D8ECCBE8BD49485E3CE3D347DD5D0524807114`。

### 2026-07-16 - 修正学习流程生成反馈、材料范围和旧批改格式

- 材料范围：每日学习记录新增材料固定归属当前小节，移除无意义的课程/章节范围选择；课程详情和设置材料库仍保留共享范围管理，弹窗说明随入口变化。
- 生成交互：评阅、重构、练习、批改和预习的运行状态及错误放到当前操作旁；认证失效提供设置入口，重新生成直接刷新阅读内容，按钮移除重复的“AI”前缀。
- Markdown：前后端共同兼容列表中的分行 `\[...\]` 和同一行旧式公式，避免标题、列表和公式混排为普通红字；编辑/阅读组件在服务端内容更新时同步最新结果。
- 完成提示：未完成对话框中的取消操作统一为“继续完成”，保留输入和原有先保存后完成语义。
- 主要文件：更新 `backend/app/ai_providers.py`、`backend/app/markdown.py`、后端测试、`frontend/src/pages/DailyRecordPage.tsx`、`frontend/src/components/MaterialLibrary.tsx`、`EditableMarkdown.tsx`、Markdown 工具、样式、前端测试、产品规格和本进度文档。
- 验证结果：前端 ESLint、TypeScript、59 项 Vitest 和生产构建通过；后端 Ruff、37 项 pytest 与 Alembic 漂移检查通过。生产页面的既有批改渲染 91 个 KaTeX 公式且无原始定界符，桌面无横向溢出；未完成提示取消后未发出保存请求，未修改真实学习数据或 Obsidian 文件。
- 遗留：真实 Codex 任务仍需用户重新连接后验收；Antigravity 首次登录和真实润色仍待完成。

### 2026-07-16 - 优化材料添加、AI Markdown 阅读与笔记工作区

- 材料界面：PDF/URL 改为等宽分段选择，PDF 使用自定义居中文件选择区；移除添加时的主材料勾选，同一课程/章节/小节作用域的第一份材料由后端自动设为主材料。
- AI 阅读：新增共用 Markdown/KaTeX 渲染和可切换阅读/编辑组件；评阅、练习、批改与笔记模型输出统一规范公式定界符，生成中显示任务状态、加载动画和耗时，不做逐字流式输出。
- 笔记工作区：旧公式只提示并允许显式转换；桌面端编辑器和预览在剩余视口高度内独立滚动，移动端使用编辑/预览单面板切换。
- 主要文件：新增 `backend/app/markdown.py`、`backend/tests/test_markdown.py`、`frontend/src/markdown.ts`、`frontend/src/markdown.test.ts`、`frontend/src/components/MarkdownContent.tsx`、`frontend/src/components/MarkdownContent.test.tsx`、`frontend/src/components/EditableMarkdown.tsx` 和 `frontend/src/components/AiTaskStatus.tsx`；更新 API、提示词、学习记录、材料库、笔记页、样式、依赖锁文件、规格和进度文档。
- 验证结果：前端 ESLint、TypeScript、55 项 Vitest 和 Vite 生产构建通过；后端 Ruff、35 项 pytest 和 Alembic 漂移检查通过。生产浏览器在 1440×900 和 390×844 验证材料弹窗、AI 阅读区、63 个公式、双栏独立滚动、移动单面板及无横向溢出，控制台 0 error/0 warning；未修改真实学习记录或 Obsidian 文件。
- 遗留：真实 Codex 输出和 Antigravity 润色仍需用户完成对应账号登录后验收；KaTeX 使单入口构建约 821 kB，后续可在发布优化阶段评估路由拆分。

### 2026-07-16 - 完成第一版阶段 11：官方 CLI 兼容与持久参考材料

- AI 兼容：Codex 改为 GPT-5.5 通用模型优先、中等强度并按账号支持列表回退，避免 ChatGPT 账号误选不支持的 `gpt-5.3-codex`。移除已停止个人服务的 Gemini CLI ACP，安装并接入 Antigravity CLI `1.1.3`，笔记润色固定使用 `Gemini 3.5 Flash (High)`。
- 材料后端：新增 `learning_materials`、`material_chunks` 和 `daily_record_materials` 及迁移 `20260716_0006`；支持 PDF/URL、课程/章节/小节作用域、主材料、重复检测、URL 刷新、文本切片、范围加权检索和课程结构删除后的文件清理。
- 材料前端：课程页新增可折叠参考材料区，学习记录材料节点支持继承材料选择和本次范围，设置页提供统一材料库；添加、更新、刷新和删除均使用站内 UI，失败提示不在对话框内外重复显示。
- 上下文与网络：固定提示词和内置 AI 任务都会读取已启用材料的相关片段，并将材料标记为不可信引用；URL 抓取与 AI 子进程使用相同的失效本地代理回退规则。
- 主要文件：更新后端模型、schema、API、材料服务、AI provider/workflow、Alembic 迁移、依赖锁和测试；更新前端 API、课程页、每日记录、设置、`MaterialLibrary` 组件、样式和测试；同步 README、产品规格和本进度文档。
- 验证结果：前端 ESLint、TypeScript、49 项 Vitest 和生产构建通过；后端 Ruff、32 项 pytest、`alembic current` 和 `alembic check` 通过。真实浏览器完成 URL 添加、材料继承、选择、范围持久化、AI 上下文、桌面/移动布局和清洁控制台验收。
- 遗留：Antigravity 仍需用户在设置中完成首次 Google 登录；真实 Gemini 润色和本轮 Codex 真实生成尚未写成已验收。

### 2026-07-16 - 修复错题筛选并升级分层导出

- 错题筛选：`GET /api/mistakes` 改为同时返回完整课程/章节/小节层级和错题条目；前端选项不再从错题反向推导。全部课程状态下使用带课程上下文的章节、小节标签，选择父级后自动缩小并重置下级范围。
- 分层导出：移除设置中的直接单 Markdown 下载，新增课程与内容多选对话框；`POST /api/export/archive` 使用标准库在内存中生成 ZIP，按课程/章节/小节组织课程概览、每日记录、AI 评阅、练习与批改、错题和小节笔记，并生成导出说明。
- 修改文件：更新 `backend/app/api.py`、`schemas.py`、`exports.py`、`tests/test_indexes_and_export.py`、`frontend/src/api.ts`、`routeData.ts`、`pages/MistakesPage.tsx`、`pages/SettingsPage.tsx`、`App.css`、`App.test.tsx`、`README.md`、产品规格和本进度文档。
- 验证结果：前端完整检查通过 44 项测试和生产构建；后端 Ruff、22 项 pytest 和 Alembic 检查通过。隔离页面与实际下载验证完整筛选、零错题范围、选择摘要、ZIP 文件名、7 文件层级、笔记内容、未选课程排除、桌面/移动端布局和控制台 0 error。
- 范围边界：未修改数据库 schema、迁移、依赖或锁文件；未写入真实学习数据或真实 Vault；未实现日期范围、章节/小节级选择、目录直接写入、JSON/CSV 或全 Vault 索引。

### 2026-07-16 - 完成第一版阶段 8 和阶段 9

- 阶段 8：新增 `/api/mistakes` 只读上下文索引和独立“错题”页面；支持课程、章节、小节、错误类型、未解决/已理解筛选，以及总数、状态计数、类型分布和详情展开，不新增统计表或复杂图表。
- 阶段 9：新增 `/api/notes` 工具管理笔记索引和独立“笔记”页面；只读取数据库登记小节对应的 `课程/章节/小节.md`，支持课程筛选、课程/标题/正文搜索及进入现有笔记编辑器，不扫描整个 Vault。新增 `/api/export/markdown`，在设置底部下载课程大纲、每日记录、AI 评阅、练习、批改、错题和预习问题。
- 修改文件：新增 `backend/app/exports.py`、`backend/tests/test_indexes_and_export.py`、`frontend/src/pages/MistakesPage.tsx`、`frontend/src/pages/NotesPage.tsx`；更新后端 API/schema、前端 API/route loader/router/侧栏/设置/样式/测试，以及 `README.md`、产品规格和本进度文档。
- 验证结果：前端 `npm run check` 通过 ESLint、TypeScript、43 项 Vitest 测试和 Vite 生产构建；后端 Ruff、21 项 pytest 和 Alembic 漂移检查通过。隔离生产页面在 1440、2560、3840 和 390px 验证筛选、搜索、展开、下载、响应式宽度和无溢出，控制台 0 error。
- 范围边界：未增加数据库迁移、依赖或锁文件；未实现复杂图表、全 Vault 索引、JSON/CSV、LLM API、自动错题提取或提示词编辑器；隔离验收数据和临时 Vault 不进入仓库。

### 2026-07-14 - 建立 Agent 规则和进度基线

- 完成内容：新增项目级执行规则；建立实施状态、决策、验证和后续任务的初始快照。
- 修改文件：`AGENTS.md`、`IMPLEMENTATION_PROGRESS.md`。
- 验证结果：两份文档均已创建并完成一致性检查，初始状态与当前目录中的实际文件相符。
- 遗留问题：应用实现尚未开始；技术栈仍需在建立项目骨架时最终固定。

### 2026-07-14 - 确认第一版技术栈

- 完成内容：确定 Vite 8 + React 19 + TypeScript 前端、FastAPI + Python 3.12 后端、SQLAlchemy 2 + Alembic + SQLite 数据层，以及 npm/uv 锁文件策略。
- 修改文件：`learning_flow_coach_spec.md`、`IMPLEMENTATION_PROGRESS.md`。
- 验证结果：已核对本机 Node、npm、Python、Git 和 SQLite 环境；Node 与 Python 版本满足要求。
- 遗留问题：uv 尚未安装；依赖、项目骨架、构建和测试配置尚未创建。

### 2026-07-14 - 安装 uv

- 完成内容：通过 WinGet 安装 `astral-sh.uv` 版本 `0.11.28`，并确认用户级 PATH 已加入安装目录。
- 修改文件：`IMPLEMENTATION_PROGRESS.md`；系统环境新增 uv。
- 验证结果：从实际安装路径运行 `uv --version` 成功，输出 `uv 0.11.28`。
- 遗留问题：当前 Codex 进程仍使用安装前的 PATH；重启 Codex 或打开新终端后可直接使用 `uv`。

### 2026-07-14 - 完成第一版阶段 1 项目骨架

- 完成内容：创建 Vite 8 + React 19 + TypeScript SPA 和 FastAPI 应用；添加 `/api/health`、Vite `/api` 代理、FastAPI 静态文件服务与 SPA fallback；加入暖色最小应用壳、环境示例、忽略规则、启动文档和前后端检查配置。
- 前端文件：`frontend/package.json`、`frontend/package-lock.json`、`frontend/vite.config.ts`、`frontend/eslint.config.js`、`frontend/index.html`、`frontend/public/favicon.svg`、`frontend/src/App.tsx`、`frontend/src/App.css`、`frontend/src/index.css`、`frontend/src/main.tsx`、`frontend/src/App.test.tsx`、`frontend/src/test/setup.ts` 及 TypeScript 配置。
- 后端文件：`backend/pyproject.toml`、`backend/uv.lock`、`backend/.python-version`、`backend/.env.example`、`backend/app/__init__.py`、`backend/app/config.py`、`backend/app/main.py`、`backend/tests/test_app.py`。
- 仓库文件：新增 `.gitignore` 和 `README.md`，更新 `IMPLEMENTATION_PROGRESS.md`；删除 Vite 自带的演示页面、图片和说明文件。
- 安装命令：`npm ci`、`uv sync --locked` 均成功。当前 Codex 受限环境中的 `uv run` 无法访问用户级 uv cache，因此后端检查从锁定生成的 `backend/.venv` 直接执行；普通本地终端仍按 README 使用 `uv run`。
- 检查命令：`npm run check`、`.venv/Scripts/ruff.exe check .`、`.venv/Scripts/pytest.exe` 均通过；健康接口、开发代理、生产静态资源、API 404 和 SPA 刷新均通过 HTTP 与 Playwright 验证。
- 运行结果：开发地址为 `http://127.0.0.1:5173/status`，生产构建地址为 `http://127.0.0.1:8000/status`；当前两个本地服务均保持运行。
- 下一步：开始阶段 2，只建立 SQLAlchemy/Alembic/SQLite 核心数据模型和迁移基础，再进入课程与章节小节的最小 CRUD 闭环。

### 2026-07-14 - 完成第一版阶段 2 和阶段 3

- 完成内容：阶段 2 建立 SQLAlchemy 2 模型、SQLite Session、Alembic 首次迁移和启动升级策略；阶段 3 实现课程、章节、小节 CRUD，以及进入小节时创建或打开今日记录的最小闭环。
- 数据文件：新增 `backend/app/database.py`、`models.py`、`migrations.py`、`workflow.py`、`schemas.py`、`api.py`，新增 `backend/alembic.ini`、Alembic 环境和 `20260714_0001` 迁移；更新配置、应用入口、`pyproject.toml` 和 `uv.lock`。
- 前端文件：新增 `frontend/src/api.ts` 和课程列表、课程详情、今日记录、运行状态页面；更新路由、应用样式和组件测试。
- 测试文件：新增隔离数据库 fixture、迁移升级/降级测试和课程到今日记录的完整 API 测试；覆盖输入校验、404、CRUD、级联删除、状态流转、日期幂等和 8 节点初始化。
- 验证结果：前端 lint/typecheck/3 tests/build、后端 Ruff/5 tests、Alembic 漂移检查全部通过；Playwright 开发主流程和 FastAPI 生产深层路由刷新通过，控制台无错误或警告。
- 范围边界：未实现节点内容或完成操作，也未加入 AI、练习、错题、Obsidian、搜索和导出；这些模块状态保持未开始或进行中。
- 遗留事项：一条仅用于浏览器验收的临时课程仍在被忽略的本地数据库中，原因是删除确认审批通道中断；仓库不包含该数据库或任何学习数据。

### 2026-07-14 - 完成第一版阶段 4 和阶段 5

- 审计前提：重新执行阶段 2/3 的前端检查、后端 Ruff/pytest、Alembic 漂移、开发代理和生产深路由验证，确认上一阶段全部通过后才继续。
- 阶段 4：为闭卷回忆、看课/阅读和主动重构增加明确内容字段；实现内容保存、节点完成、练习跳过确认、今日完成、节点进度，以及课程内历史记录和最近 1-2 条记录回看。
- 阶段 5：新增固定提示词生成模块；实现闭卷回忆评价、主动重构评价、5-8 道分类型练习出题、批改提示词、提示词复制入口，以及 AI 反馈、整次题目和用户答案持久化。
- 后端文件：更新 `backend/app/models.py`、`schemas.py`、`api.py`；新增 `backend/app/prompts.py` 和 Alembic `20260714_0002` 迁移。
- 前端文件：更新 `frontend/src/api.ts`、`pages/DailyRecordPage.tsx`、`pages/CourseDetailPage.tsx`、`App.css` 和 `App.test.tsx`。
- 测试文件：新增 `backend/tests/test_learning_flow_ai.py`，并更新迁移与级联删除测试；前端补充节点保存完成、提示词生成和跳过确认测试。
- 运行命令：`uv sync --locked`、`npm run check`、`.venv/Scripts/ruff.exe check app tests`、`.venv/Scripts/pytest.exe -q`、`.venv/Scripts/alembic.exe check`。
- 验证结果：前端 5 个测试、后端 8 个测试、Ruff、类型检查、生产构建和 Alembic 漂移检查全部通过；开发代理、生产深路由、仅本机监听、桌面/移动端布局、刷新持久化和浏览器完整主流程通过。
- 范围边界：节点 6 明日预习问题和节点 7 小节笔记仍为只读占位；未实现错题、Obsidian、搜索、导出、LLM API 或提示词编辑器。
- 验收数据：阶段 2/3 留下的本地“阶段验收课程”用于本轮浏览器验证并写入了临时学习内容；数据库与截图均在忽略目录中，不进入仓库。

### 2026-07-14 - 优化每日记录返回入口与字体

- 完成内容：将每日记录顶部的弱文本链接改为只显示“返回”的粘性工具栏按钮；滚动长流程时保持可见，并保留返回当前课程的固定目标。
- 字体调整：安装并锁定 OFL-1.1 许可的 `Nunito Sans Variable` 和 `Noto Sans SC Variable`，由前端自托管；英文数字优先使用 Nunito Sans，中文使用 Noto Sans SC。
- 许可决策：未采用 MiSans 字体包，因为 MiSans 字体许可限制再次分发字体副本，不适合随本开源仓库发布。
- 修改文件：`frontend/package.json`、`package-lock.json`、`src/main.tsx`、`src/index.css`、`src/App.css`、`src/pages/DailyRecordPage.tsx`、`src/App.test.tsx`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：前端 ESLint、TypeScript、5 个测试和生产构建通过；桌面与 390px 移动端滚动验收、返回导航、实际平台字体、无横向溢出、FastAPI 生产深路由和浏览器控制台检查通过。
- 性能结果：生产页面按 Unicode 范围实际加载 13 个字体切片，合计约 684 KB；没有外部 CDN 请求。
- 范围边界：未修改后端、数据库、学习流程字段或阶段 6 功能。

### 2026-07-15 - 调整返回按钮重量与每日学习区宽度

- 完成内容：保持“返回”按钮 40px 点击高度和粘性行为，将字重从 600 降为 500，图标调整为 16px/1.7 线宽，并减弱文字、边框和圆角的视觉重量。
- 布局调整：每日学习主区域最大宽度从 920px 增加到 1040px；未修改课程页、状态页或移动端 18px 边距。
- 修改文件：`frontend/src/App.css`、`frontend/src/pages/DailyRecordPage.tsx` 和本进度文档。
- 验证结果：前端 ESLint、TypeScript、5 个测试和生产构建通过；Playwright 在 1280、1440、1920 和 390px 下确认宽度、留白、粘性位置和无横向溢出，FastAPI 生产深路由与控制台检查通过。
- 范围边界：未修改字体依赖、后端、数据库、学习业务流程或阶段 6 功能。

### 2026-07-15 - 优化返回位置与 2K/4K 布局

- 完成内容：每日流程桌面顶部内边距从 72px 调整为 60px；“返回”按钮相对正文左移 8px，粘性工具栏顶部内边距调整为 4px，并增加按钮与下方内容的视觉间距。移动端不做横向偏移。
- 高分辨率布局：1920px 及以下保持原宽度；从 2200px 起每日流程和宽页面分别使用 1280px/1320px 最大宽度，从 3200px 起分别使用 1440px/1520px 最大宽度。
- 修改文件：`frontend/src/App.css` 和本进度文档。
- 验证结果：`npm run check` 完成 ESLint、TypeScript、5 个 Vitest 测试和 Vite 生产构建；Playwright 在 390、1280、1440、1920、2560×1440 和 3840×2160 下确认初始位置、吸顶位置、内容宽度及无横向溢出；FastAPI 生产深路由在 2K 视口通过，控制台 0 error、0 warning。
- 范围边界：未修改字体、后端、数据库、学习流程逻辑或阶段 6 功能；QA 截图保存在被忽略的 `output/playwright/`，不进入仓库。

### 2026-07-15 - 统一全宽粘性返回栏

- 完成内容：新增共用 `PageBackBar` 组件，课程详情和每日记录统一使用“返回”文案、40px 按钮和同一粘性行为；移除课程详情的普通“返回课程”链接和每日记录的局部工具栏结构。
- 布局调整：返回栏从定宽正文中拆出，覆盖左侧导航以外的完整主工作区，使用略深暖灰背景和底部分隔线；栏内按钮继续与 wide/flow 正文容器对齐，正文在桌面和移动端分别保留 32px/28px 顶部空间。
- 修改文件：新增 `frontend/src/components/PageBackBar.tsx`；更新 `frontend/src/pages/CourseDetailPage.tsx`、`DailyRecordPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx` 和本进度文档。
- 验证结果：`npm run check` 完成 ESLint、TypeScript、6 个 Vitest 测试和 Vite 生产构建；Playwright 对两个页面执行 390、1440、2560×1440 和 3840×2160 验收，确认全宽覆盖、46-50px 标题间距、长页面滚动吸顶和无横向溢出；FastAPI 两个生产深路由均返回 200，控制台 0 error、0 warning。
- 范围边界：未修改后端、数据库、学习流程字段、字体依赖或阶段 6 功能；QA 截图保存在被忽略的 `output/playwright/`，不进入仓库。

### 2026-07-15 - 柔化返回栏并区分吸顶状态

- 完成内容：在共用 `PageBackBar` 前加入不占布局空间的 1px 定位标记，通过 `IntersectionObserver` 仅在工具栏真正吸顶时添加状态类；保留统一文案、返回目标、全宽覆盖和按钮尺寸。
- 视觉调整：顶部背景/边线改为 `#f7f3ed`/`#ebe3d8`，吸顶后改为 `#f4efe8`/`#ded4c7`；按钮背景改为页面暖白，桌面/移动端正文顶部空间调整为 28px/24px，颜色过渡为 180ms，并在减少动态效果设置下关闭过渡。
- 修改文件：`frontend/src/components/PageBackBar.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx` 和本进度文档。
- 验证结果：`npm run check` 完成 ESLint、TypeScript、6 个 Vitest 测试和 Vite 生产构建；单元测试覆盖吸顶状态类进入和退出。Playwright 对课程详情和每日记录执行 390、1440、2560×1440 和 3840×2160 验收，确认两态颜色、62px 稳定高度、42-46px 标题间距、全宽覆盖及无横向溢出；FastAPI 两个生产深路由均返回 200，控制台 0 error、0 warning。
- 范围边界：未修改页面返回目标、后端、数据库、字体依赖、学习流程逻辑或阶段 6 功能；QA 截图保存在被忽略的 `output/playwright/`，不进入仓库。

### 2026-07-15 - 缩薄返回栏并加入空闲淡出

- 完成内容：共用返回栏总高度从 62px 缩减为 44px，保留 40px 返回按钮点击区域；按钮从 wide/flow 居中容器中拆出，桌面固定在主工作区左侧 16px，移动端固定在视口左侧 12px。
- 交互调整：顶部和吸顶色阶进一步减弱，颜色过渡延长为 420ms 并采用平滑缓动；滚动停止 2.5 秒后仅隐藏整栏背景和底线，返回按钮仍可见，滚动、悬停和键盘聚焦会立即唤醒栏背景。
- 修改文件：`frontend/src/components/PageBackBar.tsx`、`frontend/src/pages/CourseDetailPage.tsx`、`frontend/src/pages/DailyRecordPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx` 和本进度文档。
- 验证结果：`npm run check` 完成 ESLint、TypeScript、6 个 Vitest 测试和 Vite 生产构建；新增单元测试覆盖 2.5 秒空闲状态及滚动唤醒。Playwright 在 390、1440、2560 和 3840px 宽视口确认栏高、按钮位置、无横向溢出、三种颜色状态和悬停唤醒；FastAPI 两个生产深路由直接刷新通过，控制台 0 error、0 warning。
- 范围边界：未修改后端、数据库、学习流程字段、字体依赖或阶段 6 功能；本轮未重复运行后端 Ruff、pytest 和 Alembic 检查。QA 截图保存在被忽略的 `output/playwright/`，不进入仓库。

### 2026-07-15 - 加快并柔化返回栏淡出

- 完成内容：返回栏空闲等待时间从 2.5 秒缩短为 1.5 秒；进入空闲态时的颜色过渡改为 600ms `cubic-bezier(0.16, 1, 0.3, 1)`，唤醒仍沿用较快的 420ms 过渡。
- 修改文件：`frontend/src/components/PageBackBar.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx` 和本进度文档。
- 验证结果：`npm run check` 完成 ESLint、TypeScript、6 个 Vitest 测试和 Vite 生产构建；单元测试确认 1499ms 时未进入空闲态、1500ms 时进入。Playwright 确认 1.4 秒仍显示、1.6 秒开始淡出，实际空闲过渡为 600ms 新缓动，最终背景和底线透明、按钮继续可交互；FastAPI 生产深路由正常，控制台 0 error、0 warning。
- 范围边界：未修改返回栏高度和位置、页面布局、后端、数据库或学习流程逻辑；本轮未重复运行后端检查。

### 2026-07-15 - 统一站内编辑与确认对话框

- 完成内容：新增基于 HTML `<dialog>` 和 React Portal 的 `AppDialog`、`TextEditDialog`、`ConfirmDialog`；课程编辑、章节/小节重命名、删除课程/章节/小节、继续已完成小节和跳过练习全部迁移到应用内居中对话框，前端业务源码不再使用原生 `prompt/confirm/alert`。
- 交互与错误处理：编辑框预填并聚焦当前值，未变化或空标题不能保存；危险确认默认聚焦取消。统一支持 `Esc`、遮罩取消、请求中禁用、弹窗内 API 错误、成功后关闭及焦点恢复。删除请求失败时不再产生未处理错误，弹窗会保留并显示原因。
- 修改文件：新增 `frontend/src/components/AppDialog.tsx`、`TextEditDialog.tsx`、`ConfirmDialog.tsx`；更新 `frontend/src/pages/CourseDetailPage.tsx`、`DailyRecordPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx` 和本进度文档。
- 验证结果：最终 `npm run check` 完成 ESLint、TypeScript、8 个 Vitest 测试和 Vite 生产构建；测试覆盖章节编辑、删除取消、删除确认和跳过确认。Playwright 在 1440px、390px 和长页面滚动位置验证编辑/确认框居中、遮罩、焦点、`Esc`、移动端尺寸和无横向溢出；FastAPI 生产深路由与生产对话框通过，控制台 0 error、0 warning。首次单独运行测试时 Windows 沙箱阻止 Vite 子进程并报 `spawn EPERM`，同一命令在受控沙箱外重跑及最终完整检查均通过。
- 范围边界：未修改后端、数据库 schema、学习流程字段、依赖或阶段 6 功能；创建章节/小节、状态选择和学习内容仍保持内联。QA 截图保存在被忽略的 `output/playwright/`，不进入仓库。

### 2026-07-15 - 完成第一版阶段 6 和阶段 7

- 审计前提：重新执行前端 lint/typecheck/8 tests/build、后端 Ruff/8 tests、Alembic 漂移、开发代理和生产深路由检查，确认上一阶段全部通过后才进入新实现。
- 阶段 6：新增练习下结构化错题 CRUD、站内删除确认、8 种错误类型和未解决/已理解状态；新增明日预习固定提示词、恰好 3 条问题保存、同小节下一条记录展示及节点 6 完成交互。
- 阶段 7：新增 Obsidian vault 设置、`课程名/小节名.md` 安全路径服务、Markdown 读取/原子写入、外部修改检测、站内覆盖确认、整理提示词和编辑/预览双栏笔记页；保存后可完成节点 7。
- 后端文件：更新 `backend/app/models.py`、`schemas.py`、`api.py`、`prompts.py`；新增 `notes.py`、`20260715_0003`、`20260715_0004` 迁移和 `test_obsidian_notes.py`，更新流程与迁移测试。
- 前端文件：更新 `frontend/src/api.ts`、`App.tsx`、`App.css`、`App.test.tsx`、`pages/DailyRecordPage.tsx`、`package.json` 和 `package-lock.json`；新增 `pages/SettingsPage.tsx`、`pages/SectionNotePage.tsx`。
- 运行命令：`npm install react-markdown@latest`、`npm run check`、`.venv/Scripts/ruff.exe check app tests`、`.venv/Scripts/pytest.exe -q`、`.venv/Scripts/alembic.exe upgrade head`、`.venv/Scripts/alembic.exe check`，以及 Playwright 的阶段 6/7 主流程和多视口检查。
- 验证结果：最终前端 11 个测试、后端 13 个测试、Ruff、类型检查、生产构建和 Alembic 漂移检查全部通过；开发代理、生产笔记深路由、真实 Markdown 文件、节点 6/7 状态、外部冲突确认和 1440/2560/3840px 布局通过。
- 范围边界：未实现错题跨层级统计、笔记搜索、Markdown 导出、整个 vault 索引、提示词编辑器、LLM API 或自动错题提取。
- 数据边界：浏览器 QA 使用的隔离数据库、临时 vault 和临时课程均已清理；正常前后端继续只监听 `127.0.0.1`，源码扫描未发现个人绝对路径。

### 2026-07-15 - 优化课程入口、侧栏、外观设置与 Vault 选择

- 完成内容：将课程列表的常驻创建表单迁移到中等宽度 `AppDialog`，创建成功后进入课程；桌面侧栏支持 236px/64px 平滑展开收起并记住状态；设置页新增三档界面字号、三档笔记字号、减少动效和恢复默认外观。
- Obsidian：新增从 Obsidian 配置文件自动发现 Vault、Windows 原生目录浏览、候选路径/配置目录/可写状态确认和手动路径备用入口；不扫描整个磁盘，选择结果未经确认不会写入设置。
- 数据清理：精确删除课程 ID `2`、名称为 `???????` 且无章节的乱码验收残留；保留“阶段验收课程”和其他记录。pytest 继续使用临时数据库，未修改迁移或业务 schema。
- 修改文件：新增 `backend/app/vaults.py`、`frontend/src/preferences.ts`；更新 `backend/app/api.py`、`schemas.py`、`tests/test_obsidian_notes.py`、`frontend/src/api.ts`、`App.tsx`、`App.css`、`App.test.tsx`、`pages/CoursesPage.tsx`、`pages/SettingsPage.tsx` 和本进度文档。
- 验证结果：最终 `npm run check` 通过 ESLint、TypeScript、13 个 Vitest 测试和生产构建；Ruff 与 15 个 pytest 测试通过。开发代理能发现本机有效 Vault，生产 `/settings` 和 `/api/health` 返回 200；浏览器在 390、1440、2560 和 3840px 完成交互及无溢出检查。
- 范围边界：未实现导出、提示词编辑、错题汇总、笔记搜索、整个 Vault 索引或桌面打包；浏览器验收未保存或更改真实 Obsidian 路径。

### 2026-07-15 - 修复路由闪屏并统一课程与笔记组织

- 完成内容：异步路由改为带当前路由 ID 的明确加载状态，StrictMode 中止旧请求不会结束新请求的加载状态；课程详情、每日记录和笔记页加载时保留共用返回栏及最终内容宽度，只有真实 404 才显示“不存在”。
- 课程与布局：课程、运行状态、设置统一为最大 1080px 顶层工作区；创建课程移到课程工具栏，新增名称/描述/学习目标关键词搜索，以及小节总数、已完成、进行中和进度条。课程列表 API 新增聚合摘要，不修改数据库 schema。
- 字号与笔记：三档字号差异扩大，大字号正文/标题/编辑器分别为 17px/36px/18px；Obsidian 路径改为 `课程名/章节名/小节名.md`，同章重名拒绝、跨章同名允许。旧两层文件可读取并在保存时迁移，新旧并存或旧路径归属不唯一时明确冲突。
- 修改文件：更新 `learning_flow_coach_spec.md`、`backend/app/api.py`、`schemas.py`、`notes.py`、`tests/test_learning_api.py`、`tests/test_obsidian_notes.py`、`frontend/src/api.ts`、`App.css`、`App.test.tsx`、`pages/CoursesPage.tsx`、`CourseDetailPage.tsx`、`DailyRecordPage.tsx`、`SectionNotePage.tsx`、`StatusPage.tsx`、`SettingsPage.tsx` 和本进度文档。
- 验证结果：最终前端 ESLint、TypeScript、15 个 Vitest 测试和 Vite 生产构建通过；后端 Ruff 和 18 个 pytest 测试通过。浏览器在 1440/2560/3840px 验证顶层页面对齐、无溢出、大字号计算值、课程与学习记录切换无错误横幅，以及真实三层笔记相对路径。开发服务继续仅监听 `127.0.0.1:5173` 和 `127.0.0.1:8000`。
- 范围边界：未实现错题汇总、笔记全文搜索、Markdown 导出、提示词编辑、LLM API、整个 Vault 索引或桌面打包；未修改数据库 schema、真实 Vault 路径或真实笔记内容。

### 2026-07-15 - 优化字号、学习记录语义与流程折叠

- 完成内容：将小/标准/大界面正文调整为对称的 `10px/14px/18px`，页面标题调整为 `27px/31px/35px`，笔记编辑字号调整为 `10px/14px/18px`；设置分段选项保持比正文小 `1px` 且最低 `9px`。
- 日期语义：课程详情为历史入口增加“学习记录”标签，并将每条记录明确显示为“日期 · 当日已完成/当日未完成”，避免相邻日期被误读为区间或未完成状态缺失。
- 流程交互：8 个每日流程节点均支持展开和收起；记录打开时默认展开首个未完成节点，完成或跳过后自动收起当前节点并展开下一个未完成节点，已完成节点仍可手动重开且内容不会因收起而卸载。
- 超宽屏布局：课程、运行状态和设置页面在常规桌面保持 `1080px` 工作区，在 2200px/3200px 断点分别扩展为 `1320px/1520px`；每日学习页继续使用集中阅读宽度。
- 修改文件：`frontend/src/pages/DailyRecordPage.tsx`、`frontend/src/pages/CourseDetailPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：实施前基线与实施后均通过后端 Ruff 和 18 个 pytest；最终 `npm run check` 通过 ESLint、TypeScript、16 个 Vitest 测试和 Vite 生产构建。浏览器在 1440/2560/3840px 验证工作区宽度，在真实学习记录验证默认折叠、已完成节点重开和日期状态，在设置页验证三档计算字号、小字号选项 `9px`、无横向溢出和控制台 0 error/0 warning。
- 运行状态：Vite 代理 `/api/health` 返回 `ok`，FastAPI 生产深路由 `/daily-records/1` 返回 200 SPA HTML；服务继续使用 `http://127.0.0.1:5173` 和 `http://127.0.0.1:8000`。
- 范围边界：未修改后端、数据库 schema、业务 API、真实学习数据、Obsidian 路径或笔记文件；未实现错题汇总、笔记搜索、导出、提示词编辑或 LLM API。

### 2026-07-15 - 精简流程字段并消除路由中间画面

- 完成内容：课程搜索框跟随界面正文档位；每日流程统一使用 8 个简短标题和一句用途说明，字段改为短标题配解释。闭卷回顾移除“遗忘与卡点”，主动重构移除“自我讲解”；旧记录按稳定节点 key 显示当前标题，避免修改既有学习数据。
- 提示词与反馈：后端固定模板不再读取两个删除字段；生成评阅、练习、批改、预习和笔记整理提示词时直接显示生成内容，不再显示“提示词已生成”横幅，保存、复制和错误提示保持不变。
- 路由体验：页面数据改由 React Router loader 在提交导航前读取，站内切换保留当前页面直至目标数据完成；超过 150ms 只显示顶部 2px 进度线，首次深层路由使用同款初始 fallback。删除了不再使用的页面骨架和旋转加载样式，设置页两个独立请求并行执行。
- 修改文件：新增 `frontend/src/routeData.ts`、`frontend/src/router.tsx`；更新 `frontend/src/main.tsx`、`App.tsx`、`App.css`、`App.test.tsx`、`api.ts`、六个页面组件、`backend/app/workflow.py`、`backend/app/prompts.py`、相关后端测试、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、16 个 Vitest 测试和 Vite 生产构建；Ruff 与 18 个 pytest 测试通过，保留 1 条既有 Starlette 上游弃用警告。浏览器确认搜索框为 14px，旧记录显示新标题，删除字段和骨架均不存在，控制台无新增错误或警告；390、1440 和 2560px 无横向溢出或标题截断。
- 范围边界：未删除数据库兼容字段、未增加迁移或依赖，未修改真实学习记录、Obsidian 路径或笔记文件；未实现错题汇总、笔记搜索、导出、提示词编辑或 LLM API。

### 2026-07-15 - 恢复课程位置并保护未保存内容

- 完成内容：侧栏“课程”在当前浏览器会话中记住课程列表、课程详情、每日记录或笔记的最后位置，从运行状态和设置返回时恢复原位置；统一按钮、链接、折叠标题、课程条目和选择控件的指针、悬停、按下及键盘焦点反馈，并兼容减少动效设置。
- 离开保护：新增共用 `UnsavedChangesGuard`。每日学习记录会追踪所有可编辑表单，小节笔记会比较编辑内容与已保存版本；站内离开时提供继续编辑、不保存离开和保存并离开，保存只持久化内容而不完成节点，失败或外部文件冲突时停留并显示原因。整个 Web 在用户交互后注册原生 `beforeunload` 关闭/刷新确认。
- 修改文件：新增 `frontend/src/components/UnsavedChangesGuard.tsx`；更新 `frontend/src/App.tsx`、`App.css`、`App.test.tsx`、`pages/DailyRecordPage.tsx`、`pages/SectionNotePage.tsx`、`learning_flow_coach_spec.md` 和本进度文档。未修改后端、数据库 schema、API、依赖、真实学习数据或 Obsidian 文件。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、21 项 Vitest 测试和 Vite 生产构建；新增测试覆盖课程位置恢复、全局 `beforeunload` 事件、每日记录保存后离开、保存失败停留及笔记保存后离开。浏览器验证课程详情/每日记录/笔记位置恢复、三选项离开弹窗、继续编辑保留输入、不保存离开不写入真实数据、可交互元素过渡样式，以及 390/2560px 无横向溢出。开发代理 `/api/health` 返回 `ok`，FastAPI 生产深路由返回 200，服务仅监听 `127.0.0.1:5173` 和 `127.0.0.1:8000`。
- 验证限制：自动化浏览器的程序化刷新没有展示原生 `beforeunload` 弹窗；事件取消行为已由单元测试确认，但最终弹窗文案和展示仍受实际浏览器的用户交互与安全策略控制。本轮为纯前端修改，未重复运行后端 Ruff、pytest 或 Alembic 检查。

### 2026-07-15 - 调整课程逐级导航与关闭保护范围

- 完成内容：侧栏“课程”改为基于当前位置的逐级导航。运行状态或设置中第一次点击恢复会话内最后课程位置；课程详情、每日记录或笔记中的下一次点击返回全部课程；课程列表中保持当前页面。没有引入双击计时，深层课程页面继续显示激活状态，并通过 `title` 区分“返回上次课程位置”和“返回全部课程”。
- 离开保护：移除 `App` 中“发生任意交互后始终注册”的全局关闭保护。`UnsavedChangesGuard` 仅在 `dirty` 为真时动态添加 `beforeunload` 监听器，保存或放弃修改后立即移除；站内导航继续使用统一三选项 `AppDialog`。浏览器标签页实际关闭和刷新仍只能使用浏览器控制的原生最后保护。
- 修改文件：`frontend/src/App.tsx`、`frontend/src/components/UnsavedChangesGuard.tsx`、`frontend/src/App.test.tsx`、`learning_flow_coach_spec.md` 和本进度文档。未修改样式、后端、数据库、API、依赖、真实学习记录或 Obsidian 文件。
- 验证结果：实施前完整 `npm run check` 通过 21 项既有测试；实施后 `npm run lint`、`npm run typecheck` 和 `npm run build` 均通过，开发代理健康检查与 FastAPI 生产深路由返回 200，`netstat` 确认服务仅监听 `127.0.0.1:5173` 和 `127.0.0.1:8000`。测试文件新增逐级导航、清洁关闭和脏状态关闭/保存解除覆盖。
- 验证限制：实施后的 Vitest 因 Windows 沙箱 `spawn EPERM` 无法启动；请求沙箱外重跑时又被当前工具用量限制拒绝。本地浏览器自动化被浏览器安全策略明确禁止访问该地址，因此未绕过限制，也未将新增测试或页面交互写成已通过。后端未变更，未重复运行 Ruff、pytest 或 Alembic。

### 2026-07-15 - 补齐课程导航与关闭保护自动化验证

- 修正内容：首次成功执行新增测试后发现测试使用的组合可访问名称与 jsdom 计算结果不一致；改为复用页面既有的“学习内容”标签定位方式。该调整仅修正测试定位器，没有修改产品逻辑。
- 验证结果：沙箱外 `npm run test` 最终通过 22 项测试；随后 `npm run check` 完整通过 ESLint、TypeScript、22 项 Vitest 测试和 Vite 生产构建。课程逐级导航、清洁页面不拦截关闭、脏状态拦截关闭及保存后解除均已有自动化覆盖。
- 剩余限制：本地浏览器自动化仍被浏览器安全策略明确禁止访问 `127.0.0.1:5173`，未尝试其他浏览器或绕过方式。开发代理、生产深路由和监听地址已通过命令行验证；后端未修改，未重复运行后端检查。

### 2026-07-16 - 统一未保存弹窗并补齐草稿保护

- 完成内容：`AppDialog` 新增可配置遮罩关闭和关闭图标；未保存弹窗禁止遮罩关闭、隐藏关闭图标并使用桌面三等宽/窄屏纵向等宽操作。课程创建、课程编辑和章节/小节标题编辑禁用遮罩误关闭。
- 草稿保护：课程详情的新增章节/小节和设置手动 Vault 路径接入共用 `UnsavedChangesGuard`；错题草稿取消、收起或切换前要求确认，缺失的脏表单保存时明确报错。练习答案和批改反馈改为保存成功后再完成节点，今日完成前会阻止遗留未保存内容。
- 修改文件：新增 `frontend/src/formState.ts`；更新 `frontend/src/components/AppDialog.tsx`、`UnsavedChangesGuard.tsx`、`TextEditDialog.tsx`、`frontend/src/pages/CoursesPage.tsx`、`CourseDetailPage.tsx`、`DailyRecordPage.tsx`、`SettingsPage.tsx`、`frontend/src/App.css`、`App.test.tsx`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：`npm run check` 完整通过 ESLint、TypeScript、26 项 Vitest 测试和 Vite 生产构建；FastAPI 生产 `/courses/3` 与 `/daily-records/5` 深路由返回 200。浏览器在 1440×900 和 390×844 验证遮罩不关闭、按钮等宽、无溢出，以及课程、每日记录和设置草稿的站内离开保护，控制台 0 error/0 warning。
- 范围边界：未修改后端、数据库 schema、API、依赖或锁文件；浏览器验收中的临时文本均选择“不保存离开”，未修改现有课程、学习记录、设置、Vault 路径或 Obsidian 文件。浏览器关闭/刷新继续使用平台原生 `beforeunload`，应用不尝试伪造可自定义的关闭提示。

### 2026-07-16 - 统一离开状态并增加刷新草稿恢复

- 完成内容：将各页面分散的 `useBlocker` 和 `beforeunload` 收敛到应用级未保存状态提供器；多个编辑来源可以统一注册保存与放弃动作。创建/编辑课程、修改章节/小节标题也纳入保护，现有站内三选项行为保持不变。
- 草稿恢复：新增版本化浏览器本地草稿，覆盖每日学习表单、课程新增章节/小节、创建/编辑课程、章节/小节标题、小节笔记和设置手动 Vault 路径。草稿只在已保存服务器基线一致时恢复，保存或明确放弃后清除；创建课程和每日记录恢复时显示明确状态。
- 修改文件：新增 `frontend/src/draftStorage.ts`、`frontend/src/draftStorage.test.ts`；更新 `frontend/src/App.tsx`、`App.css`、`App.test.tsx`、`components/UnsavedChangesGuard.tsx`、`components/TextEditDialog.tsx`、`pages/CoursesPage.tsx`、`CourseDetailPage.tsx`、`DailyRecordPage.tsx`、`SectionNotePage.tsx`、`SettingsPage.tsx`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：`npm run check` 完整通过 ESLint、TypeScript、32 项 Vitest 测试和 Vite `8.1.4` 生产构建。浏览器验证创建课程草稿在无原生提示的程序化刷新后自动重新打开并完整恢复；每日记录站内离开弹窗保留输入，三个操作齐全，“不保存离开”成功清理草稿并进入设置；控制台 0 error/0 warning。
- 平台边界：普通 Web 仍不能自定义或保证浏览器关闭/刷新时的原生提示。本实现保留脏状态期间的 `beforeunload` 最后保护，并以本地草稿恢复作为浏览器抑制提示时的数据安全兜底。未修改后端、数据库、API、锁文件、真实学习数据、Vault 路径或 Obsidian 文件；后端未变更，因此未重复运行 Ruff、pytest 或 Alembic。

### 2026-07-16 - 收敛草稿恢复与成功反馈

- 完成内容：新增共用 `DraftStatus`，将恢复来源和未保存数量合并为内容宽度的紧凑状态行；恢复文案短暂淡出，未保存状态持续保留。新增 `useTransientNotice`，保存和复制成功反馈改为内容宽度并自动消失，错误反馈保持整行。
- 接入范围：每日学习记录、课程创建、课程详情新增章节/小节、课程编辑、章节/小节标题编辑、设置手动 Vault 路径和小节笔记均使用统一状态语义；恢复的手动路径会自动展开相应设置区，之后仍可自由收起。
- 修改文件：新增 `frontend/src/components/DraftStatus.tsx`、`frontend/src/components/DraftStatus.test.tsx`、`frontend/src/useTransientNotice.ts`；更新 `frontend/src/App.css`、`App.test.tsx`、`components/TextEditDialog.tsx`、`pages/CoursesPage.tsx`、`CourseDetailPage.tsx`、`DailyRecordPage.tsx`、`SectionNotePage.tsx`、`SettingsPage.tsx`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：`npm run check` 完整通过 ESLint、TypeScript、35 项 Vitest 测试和 Vite `8.1.4` 生产构建。浏览器实际刷新每日记录后确认恢复与未保存状态为紧凑单行，2.9 秒后仅恢复文案消失；未保存状态继续显示，控制台 0 error/0 warning。
- 范围边界：未修改后端、数据库、API、依赖、锁文件、真实学习记录、Vault 路径或 Obsidian 文件；浏览器验收临时草稿已选择“不保存离开”清理。本轮为纯前端和文档变更，未重复运行 Ruff、pytest 或 Alembic。

### 2026-07-16 - 增加学习节点完成前检查

- 完成内容：新增共用 `IncompleteCompletionDialog`，点击保存并完成时检查当前节点并列出未完成字段。提示框提供“继续填写”和对应确认操作，禁用遮罩关闭和关闭图标，取消后恢复原完成按钮焦点；用户文案不描述输入字符形式。
- 节点规则：闭卷回顾、材料学习、主动重构和批改反馈可确认后仍然完成；练习两项都未完成时可改为跳过，部分未完成时可确认完成；预习问题保持三条完整规则并可稍后继续；笔记正文未完成时需确认，今日完成会列出仍待完成的前置节点。确认完成继续执行先保存、后更新节点状态。
- 修改文件：新增 `frontend/src/components/IncompleteCompletionDialog.tsx`；更新 `frontend/src/components/ConfirmDialog.tsx`、`frontend/src/pages/DailyRecordPage.tsx`、`frontend/src/pages/SectionNotePage.tsx`、`frontend/src/App.test.tsx`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：`npm run check` 完整通过 ESLint、TypeScript、40 项 Vitest 测试和 Vite `8.1.4` 生产构建。真实浏览器验证材料学习未完成提示、按钮、遮罩策略、焦点恢复和请求边界，控制台 0 error/0 warning。
- 范围边界：未修改后端、数据库、API、依赖、锁文件、真实学习记录、Vault 路径或 Obsidian 文件；浏览器验收只选择继续填写，没有调用保存接口。本轮未重复运行 Ruff、pytest 或 Alembic。

### 2026-07-16 - 解耦独立笔记导航并精简导出入口

- 完成内容：新增按小节标识的 `/notes/:sectionId` 独立笔记编辑路由；从笔记库进入时保持“笔记”高亮、返回笔记列表并恢复搜索和课程筛选，只提供 Obsidian 查看、编辑与保存。每日学习记录中的既有笔记入口继续返回对应记录，并保留整理提示词和完成节点操作。
- 数据边界：笔记索引移除 `daily_record_id`，不再为每份笔记加载最新每日记录；没有学习记录的小节笔记也可以直接从笔记库打开。未修改数据库 schema、迁移、Markdown 路径、Obsidian 文件、导出 ZIP 结构、依赖或锁文件。
- 界面调整：设置页入口改为“Markdown 导出”，说明改为“选择课程和内容，导出 Markdown 文件。”；入口标题使用标准档 `14px/600`，说明为 `12px`，弹窗和按钮不再重复强调分层或多文件 ZIP。
- 修改文件：`backend/app/api.py`、`backend/app/schemas.py`、`backend/tests/test_indexes_and_export.py`、`frontend/src/api.ts`、`routeData.ts`、`router.tsx`、`pages/NotesPage.tsx`、`pages/SectionNotePage.tsx`、`pages/SettingsPage.tsx`、`App.css`、`App.test.tsx`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、46 项 Vitest 测试和 Vite `8.1.4` 生产构建；Ruff、22 项 pytest 和 `alembic check` 通过，保留 1 条既有 Starlette 上游弃用警告和 Vite 大 chunk 非阻塞提示。真实生产页面在 1440×900 和 390×844 验证独立/流程双入口、返回筛选恢复、最近课程位置、导出字号、无横向溢出及控制台 0 error/0 warning；服务仅监听 `127.0.0.1:8000`。

### 2026-07-16 - 内置 Codex/Gemini 协作与学习记忆

- 完成内容：新增 Codex App Server provider、Gemini CLI provider、AI 运行记录和课程/小节学习记忆。闭卷回顾、主动重构、练习出题、答案批改和预习问题改为应用内直接生成；笔记流程支持 GPT 生成初稿和 Gemini 润色；原提示词折叠保留为手动回退。每次生成创建新的独立会话，不复用聊天历史。
- 上下文设计：新增课程脉络、核心概念、关键方法、未解决问题和常见错误等课程级记忆，以及对应的小节级记忆。AI 运行读取当前课程、小节和同章最近摘要；课程页新增学习记忆入口，可编辑课程级内容并重新整理小节内容，小节标记完成时自动尝试更新。
- 数据与安全边界：新增 `20260716_0005` 迁移及 `ai_runs`、`course_memories`、`section_memories`。Codex OAuth 使用被忽略的 `runtime-data/ai/codex-home`，不修改全局 Codex 配置；Gemini 使用官方 CLI 用户级登录。未使用模型 API、浏览器 Cookie 或 Chat 网页自动化，账号状态和本地数据不进入仓库。
- 修改文件：新增 `backend/app/ai_providers.py`、`backend/app/ai_workflows.py`、迁移和 `backend/tests/test_ai_integration.py`；更新后端模型、配置、schema、API、应用生命周期和迁移测试；新增 `frontend/src/pages/CourseMemoryPage.tsx`，更新 API、路由、设置、课程详情、每日学习、笔记、样式和前端测试；更新 README、产品规格和本进度文档。
- 验证结果：前端 `npm run check` 最终通过 ESLint、TypeScript、46 项 Vitest 和 Vite `8.1.4` 生产构建；后端 Ruff、24 项 pytest、`alembic upgrade head` 与 `alembic check` 通过。实机确认 Codex CLI `0.115.0` 的 App Server 可启动、`account/read` 返回未登录状态、`account/login/start` 返回官方 OAuth URL；官方 Gemini CLI `0.50.0` 已安装。Chrome Headless 在 1440×900 验证设置页提供两个 provider 状态和 ChatGPT 连接按钮，在 390×844 验证学习记忆深路由正常渲染；课程页 DOM 确认学习记忆入口。保留既有 Starlette 上游弃用警告和 Vite 大 chunk 非阻塞提示。
- 待真实账号验收：ChatGPT 与 Gemini 均尚未在本工具对应运行环境完成首次登录，因此未执行真实模型输出。设置页已提供 ChatGPT 登录入口；Gemini 需先在终端运行一次 `gemini` 完成官方登录。

### 2026-07-16 - 完善 Codex/Gemini Web 登录与代理恢复

- 完成内容：设置页统一使用“Codex”和“连接 Codex”；Codex App Server 接收 `account/login/completed` 并按登录标识向 Web 返回等待、成功或失败状态，令牌交换失败不再退化为通用超时提示。Gemini 新增 ACP 登录任务，Web 点击“连接 Gemini”后由 CLI 发起 Google 官方登录。
- 代理处理：Codex 和 Gemini 子进程启动前检查继承的本机代理；HTTP/HTTPS 代理不可达且 `ALL_PROXY` 可达时自动切换。用户级持久代理已同步到当前有效状态，具体个人地址未进入仓库；运行中的 Codex Desktop 仍需完全重启才能丢弃旧进程环境。
- 修改文件：`backend/app/ai_providers.py`、`backend/app/api.py`、`backend/app/schemas.py`、`backend/tests/test_ai_integration.py`、`frontend/src/api.ts`、`frontend/src/pages/SettingsPage.tsx`、`frontend/src/App.test.tsx`、`README.md`、`learning_flow_coach_spec.md` 和本进度文档。
- 验证结果：后端 Ruff、26 项 pytest 和 `alembic check` 通过；前端 `npm run check` 通过 ESLint、TypeScript、48 项 Vitest 和 Vite `8.1.4` 生产构建。实机确认 AI 子进程代理使用当前有效值、Codex App Server 返回 OAuth URL、Gemini ACP 返回 `oauth-personal` 登录方法。Playwright CLI 在 1440×900 与 390×844 验证设置页文案、两个登录入口、刷新状态、无横向溢出及控制台 0 error/0 warning。
- 验证限制：未代替用户完成 Codex 或 Google 账号授权，因此真实 Codex 生成和 Gemini 笔记润色仍待登录后验收。浏览器插件虽已列出但本轮没有暴露其控制工具，页面验收使用 Playwright CLI 回退完成。

### 2026-07-17 - 修正本机 CLI 启动权限与 Antigravity 登录生命周期

- 完成内容：新增根目录 `start-local.cmd`，明确本地服务必须从普通用户终端或资源管理器启动；Windows 外部程序返回 `WinError 5` 时统一提示关闭受限服务并使用本机启动器，不修改 ACL 或要求管理员权限。提供方状态新增未安装、启动受限、未连接、已连接、首选模型不可用和其他错误，并分别返回首选模型及其可用性。
- 登录交互：Antigravity 可见登录窗口运行期间，后端周期性执行官方 `models` 检查账号状态，成功后自动结束交互进程，不再等待用户手动退出；180 秒超时并支持 Web 取消。设置页新增不可通过遮罩误关的站内进度框、阶段说明和取消操作；High 不可用时保持账号已连接并显示警告，不静默降级。
- 修改文件：新增 `start-local.cmd`；更新 `backend/app/ai_providers.py`、`backend/app/api.py`、`backend/app/schemas.py`、`backend/tests/test_ai_integration.py`、`frontend/src/api.ts`、`frontend/src/pages/SettingsPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx`、`README.md`、`learning_flow_coach_spec.md` 和本进度文档。未修改数据库 schema、迁移、依赖、锁文件、学习数据或 Obsidian 文件。
- 验证结果：项目 `.venv` 中 Ruff 通过，40 项 pytest 通过并保留 1 条既有 Starlette 上游警告；`npm run check` 通过 ESLint、TypeScript、60 项 Vitest 和 Vite `8.1.4` 生产构建，保留既有大 chunk 非阻塞提示。旧受限 PID 已停止，更新后的 FastAPI 以普通用户环境运行在 PID `26144`，仅监听 `127.0.0.1:8000`；健康接口正常，提供方接口实机确认 Codex 已连接、Antigravity `1.1.3` 已连接且 High 可用。浏览器设置页状态刷新成功，1281×720 无横向溢出，控制台 0 error/0 warning。
- 验证限制：本轮只读取真实账号连接与模型列表，没有发起学习内容生成或消耗账号额度；Codex 实际模型回退和 Gemini 润色结果仍留到下一次真实任务验收。直接使用 `uv run` 的首次检查被当前 Codex 沙箱拒绝访问用户级 uv 缓存，随后改用项目 `.venv` 完成全部后端检查。

### 2026-07-17 - 固定 GPT-5.5 中等强度并完善账号与代理状态

- 完成内容：Codex CLI 更新到 `0.144.5`；后端改为仅在账号模型列表包含 `gpt-5.5` 时创建任务，并固定提交 `medium` 强度，不再静默回退到其他通用模型或 Codex 专用模型。提供方状态区分固定模型可用性与最近任务实际模型，避免把历史模型显示为当前模型。Antigravity 状态检查使用单次临时诊断日志识别 Google 账号，读取后立即删除，不读取 OAuth Token。
- 界面与启动：设置页显示两个提供方的账号、固定模型、强度、可用性、最近实际模型和真实 CLI 版本。`start-local.cmd` 会先清空继承的代理变量，再从当前用户注册表环境加载现值，避免旧 Codex 或终端会话把过期代理传入项目进程。用户环境、Codex 持久配置、注册表搜索和本机监听均已确认不存在旧端口；当前 Codex Desktop 进程仍需完全退出并重开才能丢弃其启动时继承值。
- 修改文件：`backend/app/ai_providers.py`、`backend/app/ai_workflows.py`、`backend/app/schemas.py`、`backend/tests/test_ai_integration.py`、`frontend/src/api.ts`、`frontend/src/pages/SettingsPage.tsx`、`frontend/src/App.test.tsx`、`start-local.cmd`、`README.md`、`learning_flow_coach_spec.md` 和本进度文档。未修改数据库 schema、迁移、依赖锁文件、学习数据或 Obsidian 文件。
- 验证结果：前端 `npm run check` 通过 ESLint、TypeScript、60 项 Vitest 和 Vite `8.1.4` 生产构建；后端 Ruff、43 项 pytest 与 `alembic check` 通过，保留 1 条既有 Starlette 上游弃用警告和 Vite 大 chunk 非阻塞提示。生产服务运行在 `127.0.0.1:8000`，健康接口正常；提供方接口实机返回 Codex 账号、Plus、CLI `0.144.5`、GPT-5.5 可用和中等强度，以及 Antigravity 账号、CLI `1.1.3` 与 Gemini 3.5 Flash High 可用。Chrome Headless 在 1440×900 验证设置页加载后布局与状态显示正常。
- 验证限制：为避免未经确认消耗账号额度，本轮未执行真实学习任务；因此“最近使用模型”保持为空是预期行为。当前 Codex Desktop 自身的进程环境无法由子进程热修改，彻底丢弃旧继承值需要用户在本轮结束后完全退出并重新打开 Codex Desktop。

### 2026-07-17 - 统一分层上下文、节点交接与材料复用

- 完成内容：新增统一 Codex 上下文包，显式包含课程、章节、小节、课程/章节/小节记忆、同章当前小节之前且已完成的最近 3 个小节、最近每日摘要、上次预习问题、上游节点交接和按任务限制的材料内容。所有生成任务继续使用新的临时会话；结构化输出同时保存展示 Markdown、材料摘要和下游交接。
- 记忆与材料：新增章节记忆、每日上下文摘要、材料摘要/状态签名、AI 交接字段和独立自动课程脉络。每日完成只生成本地摘要；小节完成或手动整理一次调用同时更新小节和章节记忆，并从全部记录、练习答案、批改、错题、评阅、预习问题和最终 Obsidian 笔记整理。课程详情新增课程/章节/小节固定材料入口，设置支持跨课程统一管理；有效材料摘要存在时不重复读取原文。
- 笔记行为：GPT 在已有编辑器内容上执行修订，自动生成与手动提示词使用同一完整构建逻辑，读取全部练习组；生成结果先在 920px 上限的站内 Markdown 预览框中展示，用户确认后才应用。Gemini 只接收当前笔记和小节定位，不再读取材料片段。
- 修改文件：新增 `backend/alembic/versions/20260717_0007_context_handoffs.py`；更新 `backend/app/models.py`、`schemas.py`、`ai_providers.py`、`ai_workflows.py`、`materials.py`、`prompts.py`、`api.py` 及后端测试；更新 `frontend/src/api.ts`、`components/AppDialog.tsx`、`pages/CourseDetailPage.tsx`、`CourseMemoryPage.tsx`、`SectionNotePage.tsx`、`SettingsPage.tsx`、`App.css`；同步产品规格和本进度文档。
- 验证结果：本地数据库升级到 `20260717_0007` 并确认章节记忆列存在；`alembic check` 无漂移。后端 Ruff 和 45 项 pytest 通过，保留 1 条既有 Starlette 上游弃用警告。前端 `npm run check` 通过 ESLint、TypeScript、60 项 Vitest 和 Vite `8.1.4` 生产构建，保留既有大 chunk 非阻塞提示。生产服务健康接口正常且只监听 `127.0.0.1:8000`；Chrome Headless 实际验证课程详情、学习记忆、笔记页及 390×844 窄屏，无空白页和新增操作溢出。
- 验证限制：为避免未经确认消耗会员任务额度，本轮未调用真实 Codex 或 Antigravity 生成；结构化输出契约使用隔离 Fake provider 覆盖，真实账号下的材料摘要质量和跨节点交接仍需下一步验收。未修改 Obsidian 文件。

### 2026-07-17 - AI 每日摘要与可追溯材料取证

- 完成内容：新增 `daily_summary` Codex 任务，完成今日学习时一次生成紧凑每日摘要并更新小节/章节记忆，不再持久化本地字段拼接摘要。各节点只读取任务需要的上游结构化交接；下游排除已实际引用的相同材料定位，缺少交接时再补充原文。
- 材料与引用：新增内容哈希版本快照，链接刷新后保留旧材料块；回顾评阅读取同小节前 1-2 次记录锁定的版本，其余节点读取当前记录版本。AI 运行保存模型声明实际使用的材料标题、页码、网页位置或字幕时间段，学习页提供紧凑折叠查看。URL 入口识别公开视频并通过 `yt-dlp` 只提取中文/英文字幕，不下载视频。
- 修改文件：新增 `backend/alembic/versions/20260717_0008_grounded_material_context.py`；更新后端模型、schema、材料解析、AI 工作流、提示词、API、provider 结果和测试；更新前端 API 类型、材料库、每日学习页及样式；更新 `pyproject.toml`、`uv.lock`、README、产品规格和本进度文档。
- 验证结果：`uv sync --locked` 成功；后端 Ruff、47 项 pytest、真实数据库 `alembic upgrade head` 和 `alembic check` 通过。前端 `npm run check` 通过 ESLint、TypeScript、60 项 Vitest 和 Vite `8.1.4` 生产构建。生产服务仅监听 `127.0.0.1:8010`，健康接口和 SPA fallback 正常；1600×1000 与 390×844 Chrome Headless 只读截图无新增溢出。
- 验证限制：未消耗真实 Codex/Antigravity 额度，未向课程添加真实公开视频，未修改 Obsidian 文件。真实模型是否稳定返回精确 `source_refs`、具体视频平台字幕可用性和无字幕视频仍需实际材料验收；无字幕语音转录、OCR、向量数据库和外部 RAG 服务不在本轮范围。

### 2026-07-19 - 完整材料基座、学习者背景与纯学习笔记

- 完成内容：新增按小节持久化的完整材料基座和 revision。Codex 第一次逐份读取完整授权范围，各评阅、重构、练习、批改、预习和笔记节点从基座锚点 fork；新增材料或扩围增量续接，刷新、替换、删除、停用或缩围重建。回顾改用最近历史记录锁定的材料版本，手动笔记提示词无需调用模型即可内联完整材料。
- 上下文与提示词：Codex 只读沙箱默认开启网络，允许可靠通用知识和网络拓展但要求与材料事实区分。设置新增学习者背景；所有小节完成后可显式生成课程成果摘要，并加入后续课程通用上下文。主动重构彻底移除旧“自我讲解”输入；GPT 笔记只读取完整材料、主动重构和已有编辑器内容，隔离练习、错题、批改、回顾评阅和预习问题。Gemini 保留内容语言润色，并增加 Obsidian Markdown、公式和可读性规则。
- 数据与界面：新增 `20260719_0009` 迁移、`material_context_sessions`、AI 运行材料 revision/清单哈希和课程完成字段。课程详情在满足条件后提供“完成课程”并展示成果摘要；设置提供可编辑学习者背景；笔记生成预览显示材料版本。旧数据库字段仅为兼容历史数据保留，不再进入 API 或界面。
- 主要文件：新增 `backend/app/material_sessions.py` 和 `backend/alembic/versions/20260719_0009_material_sessions_and_profile.py`；更新后端模型、schema、provider、AI 工作流、提示词、API 与测试；更新前端 API、课程详情、设置、笔记页、样式和测试；同步 README、产品规格和本进度文档。
- 验证结果：前端 `npm run check` 通过 ESLint、TypeScript、60 项 Vitest 和 Vite `8.1.4` 生产构建；后端 Ruff、49 项 pytest、真实数据库 `alembic upgrade head` 与 `alembic check` 通过。新增测试覆盖材料增量续接、刷新重建、节点 fork、历史版本、完整手动笔记材料、学习者背景和已完成课程上下文。生产构建健康接口与 SPA fallback 返回 200，`netstat` 确认只监听 `127.0.0.1:8000`；浏览器桌面/移动设置页和课程详情无横向溢出或控制台错误。用户提供的学习者背景已通过本机 API 写入忽略的运行数据库。
- 验证限制：为避免未经确认消耗会员任务额度，本轮未执行真实 Codex 或 Antigravity 生成，未修改 Obsidian 笔记。完整 PDF 图表理解、真实网页/视频材料覆盖质量、来源标注和 Gemini 润色质量仍需真实学习材料验收。

### 2026-07-19 - 六步每日流程、结构化练习与独立材料库

- 流程调整：每日记录从 8 个节点收敛为闭卷回顾、材料学习、主动重构、练习与推导、批改与纠错、今日收尾六步；数据库迁移将旧预习问题节点映射为今日收尾并移除每日笔记/完成占位。今日完成后可以继续下一天，也可以进入笔记页保存并完成小节。
- 结构化练习：新增 `exercise_items`、`exercise_responses` 和错题单题关联；Codex 固定生成 12 道题且恰好 4 道选择题，Web 一题一页，切题自动保存，全部完成后提交整组并一次调用 Codex 返回 12 条逐题反馈。旧整段练习继续只读兼容。
- 材料与启动：新增侧栏 `/materials` 独立材料库，支持关键词、课程、类型和状态筛选；设置只保留入口。Windows Codex 解析改为从 npm shim 定位随包原生二进制，避免误用 WindowsApps 打包路径导致 `WinError 5`；启动状态会返回实际程序路径与桌面启动模式。
- 主要文件：新增 `backend/alembic/versions/20260719_0010_structured_daily_practice.py`、`frontend/src/pages/MaterialsPage.tsx`；更新后端 workflow、models、schemas、prompts、AI schema、API、provider 和测试；更新前端 API、路由、侧栏、每日记录、笔记完成逻辑、设置、样式和测试；同步产品规格与本进度文档。
- 验证结果：后端 Ruff、50 项 pytest、真实数据库升级和 Alembic 漂移检查通过；前端 ESLint、TypeScript、62 项 Vitest 和 Vite `8.1.4` 生产构建通过。生产服务健康接口和材料库深路由正常；浏览器在 1440/2560px 验证材料页和六节点每日页无横向溢出，控制台无错误或警告。实机提供方状态确认 Codex Plus/GPT-5.5 Medium 与 Antigravity/Gemini 3.5 Flash High 均已连接。
- 验证限制：本轮只读取官方提供方状态，没有发起真实生成任务，也没有修改学习内容或 Obsidian 文件；12 道题的真实题型质量、材料依据和批改质量仍待用户使用真实小节验收。

### 2026-07-19 - Codex 权限兼容、材料库弹窗与提供方断开

- 权限兼容：Codex App Server 改用当前支持的 `:read-only` permission profile，不再提交已移除的 `readOnlyAccess` 或旧 sandbox 字段；旧服务返回该错误时转换为明确的重启提示。实机权限列表已确认该 profile 可用。
- 练习清理：旧整段练习新增站内确认后的级联删除，结构化练习拒绝使用该接口，避免误删当前 12 道逐题练习；页面重新生成失败时仍保留旧内容供查看或手动删除。
- 界面与连接：移除材料库侧栏入口，设置中的“打开材料库”改为可筛选和管理材料的站内工作区弹窗，旧 `/materials` 地址重定向到该弹窗。Gemini 与 Codex 一样支持从本工具断开和重新启用；模型说明只在各提供方下显示，强度统一使用 `Medium` / `High` 英文。
- 主要文件：更新后端 `ai_providers.py`、`api.py` 和 AI/学习流程测试；更新前端 API、设置页、每日记录页、路由、对话框、材料库组件、样式和测试；删除独立 `MaterialsPage.tsx`；同步 README、产品规格和本进度文档。
- 验证结果：后端 Ruff 与 51 项 pytest 通过；前端 ESLint、TypeScript、64 项 Vitest 和 Vite `8.1.4` 生产构建通过。生产服务健康接口正常且只监听 `127.0.0.1:8000`；浏览器在 1440×900 和 2560×1440 验证材料弹窗、旧版练习删除确认/取消和提供方状态，无横向溢出，控制台 0 error/0 warning。
- 验证限制：未发起真实 Codex 或 Antigravity 生成，未实际断开已登录账号，未删除真实练习或修改 Obsidian 文件；真实 12 道练习与逐题批改质量仍待用户验收。

### 2026-07-19 - Codex App Server 能力协商热修复

- 根因：所有 Codex 学习节点共用的 `turn/start` 请求携带 `runtimeWorkspaceRoots`，但客户端 `initialize` 未声明 `experimentalApi`，Codex CLI `0.144.5` 因而在生成前统一拒绝请求。上一轮只验证权限 profile 列表和连接状态，没有执行真实回合，未覆盖到该协议校验。
- 修复：App Server 初始化显式声明 `capabilities.experimentalApi=true`，继续使用 `:read-only` permission profile 和任务授权的运行工作区根目录，不退回废弃的 `readOnlyAccess` 或 sandbox policy，也不删除材料目录边界。新增对应中文兼容错误和初始化回归测试。
- 主要文件：更新 `backend/app/ai_providers.py`、`backend/tests/test_ai_integration.py`、README、产品规格和本进度文档；未修改数据库、前端业务代码、学习数据和 Obsidian 文件。
- 验证结果：Codex CLI 生成的当前实验 JSON Schema 确认能力依赖；后端 Ruff、52 项 pytest、前端 ESLint/TypeScript/64 项 Vitest/Vite 生产构建通过。使用空的项目 AI 工作区进行一次最小 GPT-5.5 真实调用并返回 `PROTOCOL_OK`；更新后的服务已在 `127.0.0.1:8000` 启动，健康接口和两个提供方状态正常。
- 验证限制：安全策略拒绝了对真实材料目录的自动模型读取，因此本轮未用课程 PDF、URL、学习记录或材料基座做真实生成；材料读取质量、12 道练习契约和完整业务节点仍需用户使用实际小节验收。

### 2026-07-19 - 结构化练习作答界面优化

- 界面调整：题号导航改为 34×32px 固定尺寸并左对齐排列，窄屏每行 6 题；已作答和当前题使用克制的边框与内侧状态线，不再使用大面积状态底色。
- 作答排版：选择题使用“18px 控件、选项编号、Markdown 正文”三列网格，控件固定在最左侧，各选项正文起点一致；题干和选项统一使用 16px 标准字号，Markdown 与 LaTeX 直接渲染，题目和作答区间距收紧为 12px。
- 主要文件：更新 `frontend/src/pages/DailyRecordPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx`、产品规格和本进度文档；未修改后端、数据库、提示词、练习答案、学习数据或 Obsidian 文件。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、65 项 Vitest 和 Vite `8.1.4` 生产构建；新增测试覆盖结构化选择题选项公式渲染。浏览器在 1440×900、2560×1440 和 390×844 验证题号导航、选项对齐、字号、间距和 KaTeX；均无横向溢出，控制台 0 error/0 warning。
- 验证限制：本轮只读检查既有结构化练习，没有点击选项、保存、重新生成或调用模型；真实题型和模型输出质量仍按下一步计划验收。

### 2026-07-19 - 小节学习记录渐进收纳

- 信息层级：课程详情不再把全部学习记录横向铺在小节下；默认改为 30px 高的一行摘要，显示记录次数、最近日期和最近状态。点击后纵向显示最近 3 条，可进一步查看全部记录，同时只允许一个小节展开。
- 当前动作：有今日记录的小节显示“继续学习”，否则显示“开始学习”。展开状态只在当前课程详情页面存活；进入记录后返回时恢复默认收起，避免历史区域反复占用页面。
- 界面细节：展开列表使用轻量左边线和日期/当次状态/箭头三列，不新增卡片、弹窗或独立历史页面；移动端隐藏摘要中的重复状态文字，保留完整无障碍名称，并遵守减少动效设置。
- 主要文件：更新 `frontend/src/pages/CourseDetailPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx`、产品规格和本进度文档；未修改后端、数据库、学习记录、提示词、模型或 Obsidian 文件。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、67 项 Vitest 和 Vite `8.1.4` 生产构建。浏览器在 1440×900、2560×1440 和 390×844 验证折叠摘要、最近 3 条、查看全部、开始/继续文案、会话恢复和单节展开，页面无横向溢出，控制台 0 error/0 warning。
- 验证限制：浏览器验收只点击摘要和“查看全部”，未进入记录、保存数据或调用模型；真实学习内容未被修改。

### 2026-07-19 - 模型首选项与持久化删除双重确认

- 模型设置：设置页从 Codex App Server `model/list` 和 Antigravity `models` 实时读取可用模型；选择模型后只显示其实际支持的思考强度。默认保持 GPT-5.5 · Medium 和 Gemini 3.5 Flash · High，可恢复默认或保存本机首选项；服务端保存前再次使用实时列表校验，不静默回退。
- 运行与材料边界：AI 任务启动时记录模型与思考强度快照，运行中变更设置只影响后续任务。Codex 材料基座按模型隔离，切换模型后不会复用其他模型的会话锚点；新增 `20260719_0011` 迁移为材料会话和 AI 运行记录补充对应字段。
- 删除保护：新增统一 `TwoStepDeleteDialog`。课程、章节、小节、参考材料、旧版练习和错题均需先确认影响范围，再执行一次永久删除确认；两层都禁止点击遮罩关闭，第二层可以返回第一层，只有最终确认才调用删除 API。确认区按钮使用一致尺寸。
- 主要文件：新增 `backend/app/ai_preferences.py`、`backend/alembic/versions/20260719_0011_ai_provider_preferences.py` 和 `frontend/src/components/TwoStepDeleteDialog.tsx`；更新后端模型、provider、AI workflow、材料会话、schema、API 与测试；更新前端 API、设置页、课程详情、材料库、每日记录、通用确认框、样式和测试；同步 README 与产品规格。
- 验证结果：后端 Ruff 与 54 项 pytest 通过；前端 `npm run check` 通过 ESLint、TypeScript、68 项 Vitest 和 Vite `8.1.4` 生产构建。本地数据库升级到 `20260719_0011`，`alembic check` 无漂移；隔离数据库完成 `0010 → 0011 → 0010 → 0011` 往返迁移。真实 CLI 返回 Codex GPT-5.6/5.5/5.4/5.2 及各自强度、Gemini 3.5 Flash/3.1 Pro 及各自强度；默认首选项保持不变。浏览器在 1440×900 验证模型联动、应用按钮状态、两层确认、返回、取消、等宽按钮和遮罩不可误关；未发起删除请求。生产服务健康接口正常且只监听 `127.0.0.1:8000`。
- 验证限制：本轮读取了真实账号的模型列表，但未保存不同模型首选项、未发起真实 Codex/Antigravity 生成、未删除学习数据，也未修改 Obsidian 文件。保留既有 Vite 大 chunk 提示、1 条 Starlette 上游弃用警告和本机无效 `SSL_CERT_DIR` 警告。

### 2026-07-19 - 模型设置控制条紧凑化

- 布局调整：设置页模型控制条不再使用会填满剩余空间的 `1fr` 轨道。桌面端模型选择框最大 270px、思考强度最大 145px，选择框、恢复默认和应用操作统一为 34px 高并保持左对齐；提供方区域移除多余最小高度并收紧纵向间距。
- 响应式：`721–900px` 保持模型和强度同排，将操作移到下一行；`≤720px` 继续使用全宽纵向布局，保证触控范围和长模型名称可读。2K/4K 下控件宽度不再随工作区增长。
- 主要文件：更新 `frontend/src/App.css`、产品规格和本进度文档；未修改 React 业务代码、后端、数据库、模型设置、学习数据或 Obsidian 文件。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、68 项 Vitest 和 Vite `8.1.4` 生产构建。实测 1440px 下模型框为 270×34px、强度框为 145×34px，整组操作紧邻排列；2560px 和 3840px 下尺寸保持不变。900px、720px 和 390px 响应式正常，大字号模式与全部视口均无横向溢出，浏览器控制台 0 error/0 warning。
- 验证限制：浏览器验收只读取布局并临时切换 DOM 字号，未点击应用、未修改本机模型首选项，也未调用真实模型。

### 2026-07-19 - 模型选择宽度二次收紧

- 调整内容：根据实际页面反馈，将桌面模型框上限从 270px 降至 235px、思考强度从 145px 降至 130px；`721–900px` 的对应上限降至 225px 和 125px。控件高度、操作位置和窄屏全宽规则不变。
- 主要文件：更新 `frontend/src/App.css` 和本进度文档；未修改组件逻辑、模型首选项、后端、数据库、学习数据或 Obsidian 文件。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、68 项 Vitest 和 Vite `8.1.4` 生产构建。1440px 下实测模型框 235×34px、强度框 130×34px；900px 大字号下 `Gemini 3.5 Flash` 在 225px 模型框中完整显示，无横向溢出，控制台 0 error/0 warning。
- 验证限制：未点击应用或修改本机模型设置，未调用真实模型。

### 2026-07-19 - 模型选择宽度第三次收紧

- 调整内容：继续将桌面模型框上限从 235px 降至 210px、思考强度从 130px 降至 110px；`721–900px` 的对应上限降至 200px 和 105px。控件高度、操作位置、间距和窄屏全宽规则不变。
- 主要文件：更新 `frontend/src/App.css` 和本进度文档；未修改组件逻辑、模型首选项、后端、数据库、学习数据或 Obsidian 文件。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、68 项 Vitest 和 Vite `8.1.4` 生产构建。1440×900 下实测模型框 210×34px、强度框 110×34px；900×1000 大字号下 `Gemini 3.5 Flash` 在 200px 模型框中完整显示，页面无横向溢出，控制台 0 error/0 warning。
- 验证限制：未点击应用或修改本机模型设置，未调用真实模型。

### 2026-07-19 - 模型框横向留白继续收紧

- 调整内容：只收紧模型选择框，将桌面上限从 210px 降至 190px、`721–900px` 上限从 200px 降至 185px；思考强度宽度、控件高度、列间距、操作位置和窄屏全宽规则均保持不变。
- 主要文件：更新 `frontend/src/App.css` 和本进度文档；未修改组件逻辑、模型首选项、后端、数据库、学习数据或 Obsidian 文件。
- 验证结果：`npm run check` 通过 ESLint、TypeScript、68 项 Vitest 和 Vite `8.1.4` 生产构建。1440×900 下实测模型框 190×34px、强度框 110×34px；900×1000 大字号下 `Gemini 3.5 Flash` 在 185px 模型框中完整显示并保留约 19px 文字余量，页面无横向溢出，控制台 0 error/0 warning。
- 验证限制：未点击应用或修改本机模型设置，未调用真实模型。

### 2026-07-20 - 真实材料全流程验收与稳定性修复

- 真实验收：使用 MIT 18.06 官方逐字稿和课程页完成线性代数六步学习流程，并使用用户提供的 David Silver 动态规划 PDF 与官方课程页验证跨作用域多材料和跨课程隔离。当时报告错误地把用户 YouTube 链接列为 MIT 材料；该链接实际是 Yale Financial Markets (2011) Lecture 1，且未进入 MIT 材料基座。线性代数样本真实生成 12 道结构化练习、逐题批改、两条错题、3 条衔接问题、每日/章节/小节记忆、课程成果摘要、GPT 初稿和 Gemini 润色，并写入临时 Obsidian Vault。
- 修复内容：材料会话认证异常改为统一 AI 错误；扩充 LaTeX 控制字符修复并覆盖练习选项、隐藏答案和三层记忆；结构化批改后重新读取每日记录以同步节点状态；错题草稿默认选择首个错误或部分正确单题；材料弹窗根据固定课程/章节/小节作用域显示真实文案。
- 主要文件：更新 `backend/app/api.py`、`backend/app/markdown.py`、`backend/app/ai_workflows.py`、`backend/tests/test_markdown.py`、`backend/tests/test_ai_integration.py`、`frontend/src/pages/DailyRecordPage.tsx`、`frontend/src/App.test.tsx` 和本进度文档。完整验收报告与原始响应、截图、测试前数据库备份位于被忽略的 `output/e2e-qa/2026-07-20-real-flow/`。
- 验证结果：后端 Ruff 与 57 项 pytest 通过；前端 ESLint、TypeScript、70 项 Vitest 和 Vite `8.1.4` 生产构建通过。真实输出综合质量 93/100；最终笔记 14,443 字符、0 个控制字符，三层路径写入和索引搜索通过；1440×900 页面包含 236 个 KaTeX 节点，无横向溢出，控制台 0 error。
- 验证限制：YouTube 字幕抓取真实返回 HTTP 429；未修改系统时间验证次日 UI；Gemini 输出使用少量 `<br>`，Web 暂不原生呈现 Obsidian Callout。保留 Starlette/httpx 上游弃用提示、本机无效 `SSL_CERT_DIR` 提示和 Vite 大 chunk 非阻塞建议。

### 2026-07-20 - 验收后可靠性、批改与加载优化

- 生成与材料：AI 运行状态可按每日记录或小节恢复，显示准备材料、等待提供方和已用时间；服务重启会将遗留任务标记为中断。失败 URL/PDF 保留记录并支持同源重新解析，不自动替代材料。
- 批改与来源：移除整组总分和单题分数，批改区改为逐题翻页并分别给出正确、部分正确或错误反馈；题干、选项、提交答案和错题来源只读。来源区区分可用材料与模型实际引用，哈希和 revision 收入技术详情。
- 笔记与前端：笔记提示词避免强行补历史背景和学习过程内容；模型结果应用前检查代码围栏、块公式与 LaTeX 环境。Web 支持 Obsidian Callout 和 `<br>` 换行；页面组件按路由懒加载，Markdown/KaTeX 拆为按需共享块。
- 主要文件：更新后端 AI schema、提示词、API、Markdown、schema、应用生命周期及测试；更新前端路由、每日记录、笔记、材料库、AI 状态、Markdown 渲染、API 类型、样式及测试；同步产品规格和本进度文档。未新增数据库迁移或依赖，未修改真实学习数据和 Obsidian 文件。
- 验证结果：后端 Ruff 与 62 项 pytest 通过；前端 ESLint、TypeScript、72 项 Vitest 和 Vite `8.1.4` 生产构建通过。注入日期的跨自然日测试确认上一日衔接问题进入次日回顾提示词；生产健康接口和两个深路由返回 200，1440×900 与 2560×1440 页面检查无横向溢出。主入口由约 853KB 降为约 313KB，Markdown/KaTeX 改为约 392KB 的按需共享块。
- 验证限制：本轮没有再次消耗真实 Codex 或 Antigravity 额度，也没有修改系统时间；AI 结构化输出使用隔离 provider 回归。完整 GFM 表格插件因 npm registry 安装超时未加入，保留 1 条 Starlette/httpx 上游弃用警告和本机无效 `SSL_CERT_DIR` 警告。

### 2026-07-20 - 完整 GFM 表格渲染与真实页面验收

- 实现内容：安装并锁定 `remark-gfm@4.0.1`，将其接入共用 Markdown 渲染器。新增语义化表格滚动容器、表头、单元格边界、交替行底色、对齐和焦点样式，并支持删除线、自动链接及任务列表。超宽表格只在自身区域横向滚动。
- 验收修复：首次 390px 截图发现 GFM 任务复选框继承全局 `input` 的 100% 宽度和 40px 高度，导致列表异常纵向拉伸；随后将任务复选框覆盖为 15×15px、每项约 25px 高，并重新执行视觉验收。
- 主要文件：更新 `frontend/package.json`、`frontend/package-lock.json`、`frontend/src/components/MarkdownContent.tsx`、对应测试、`frontend/src/App.css`、产品规格和本进度文档。QA 数据、SQLite、临时 Vault、脚本和截图位于被忽略的 `output/playwright/gfm-qa/`，未接触真实学习数据或用户 Vault。
- 验证结果：前端 `npm run check` 通过 ESLint、TypeScript、73 项 Vitest 和 Vite `8.1.4` 生产构建。真实临时笔记复用了端到端报告数据，并加入 8 列公式表；Playwright 快照确认 2 个语义化表格和 6 个 KaTeX 节点。1440×900 与 390×844 控制台 0 错误；390px 下页面 `scrollWidth` 为 390px，表格容器为 306px、内部宽度分别 725px 和 1288px，横向滚动边界正确。
- 依赖与体积：安装过程在把本次 npm 代理从失效的 `127.0.0.1:7890` 临时改为实际可达的 `127.0.0.1:7897` 后完成，未修改系统或用户环境；npm audit 为 0 个漏洞。主入口保持约 313KB，按需 Markdown 共享块由约 392KB 增至约 430KB。

### 2026-07-20 - 摘要标题去重与纠正材料真实复测

- 摘要界面：移除每日摘要内容外层的“今日摘要”标签，只保留模型 Markdown 中的“今日学习摘要”；摘要容器改为有语义标签的 `section`，并微调上边距和内边距。新增回归测试确认展开今日收尾后只有一个可见摘要标题。
- 材料纠正：确认用户 YouTube 链接属于 Yale Financial Markets (2011) Lecture 1，而非 MIT 18.06。隔离 QA 分别执行 Yale 完整流程、MIT 练习/批改/笔记定向回归和 David Silver RL 材料隔离回归；跨课程目标关键词污染均为 0。通过注入日期文件实际创建 7 月 20 日和 7 月 21 日记录，三条衔接问题全部进入次日回顾提示词。
- 质量结论：标题与材料均正确的 Yale 样本完成回顾、重构、12 题、逐题批改、错题、衔接、每日摘要、GPT 笔记、Gemini 润色和隔离 Vault 写入；有效输出综合质量 95/100。MIT GFM 表格与公式保留，RL 准确区分策略迭代和值迭代。完整 Yale 笔记首次约 311 秒超时，原入口重试 140 秒成功。
- 新发现：YouTube 首次解析成功得到 32 个字幕片段，但同源刷新遇到 429 后会把既有材料标为失败并排除旧 revision；Yale 官方 HTML 快照包含完整 transcript，通用网页分块却只生成 1 个 Overview 片段。参考材料状态已改为 `IN_PROGRESS`，两项均列为发布前 P1 修复。
- 主要文件：更新 `frontend/src/pages/DailyRecordPage.tsx`、`frontend/src/App.css`、`frontend/src/App.test.tsx`、`learning_flow_coach_spec.md` 和本进度文档。完整原始响应、隔离数据库、临时 Vault、脚本、截图与评价报告位于被忽略的 `output/e2e-qa/2026-07-20-corrected-material-flow/`。
- 验证结果：前端 `npm run check` 通过 ESLint、TypeScript、74 项 Vitest 和 Vite `8.1.4` 生产构建；后端 Ruff 与 62 项 pytest 通过。1440×900 和 390×844 摘要均为 0 个“今日摘要”、1 个“今日学习摘要”，无横向溢出、控制台 0 错误；最终笔记页面渲染 38 个 KaTeX 节点。逐题批改 API 与 DOM 翻页控件通过，第二次聚焦截图因无头浏览器残留进程超时，残留进程已清理。

### 2026-07-20 - 材料刷新可靠性、逐字稿索引与可恢复长笔记

- 材料可靠性：新增 `20260720_0012` 迁移，将活动材料版本与最近刷新尝试分离。已有成功版本刷新失败时继续保持可用，并独立记录刷新状态、错误和成功时间；解析成功后再原子切换活动哈希。网页解析识别内嵌 transcript、章节标题和时间定位，并对逐字稿低覆盖明确失败；解析器版本参与 revision 哈希。
- 上下文与引用：材料导入时本地建立全文块索引和只读工作区，不再单独调用模型生成覆盖确认。首个业务节点获得任务相关原文块和完整文件只读权限，成功后成为材料 revision 锚点；后续节点从锚点 fork。结构化输出按材料 ID 与块号校验，只持久化模型实际使用且属于当前候选版本的来源。
- 长任务体验：小节笔记初稿新增后台运行、状态查询和结果读取接口；页面保存运行 ID、轮询真实状态，并可在离开或刷新后恢复。结果先进入预览，用户确认前不覆盖编辑器；服务关闭或重启会把遗留运行标为可重试失败。材料刷新失败提示保持紧凑并明确仍在使用上次成功版本。
- 主要文件：新增迁移 `backend/alembic/versions/20260720_0012_material_refresh_reliability.py`；更新后端模型、schema、材料解析、材料会话、AI provider/workflow、API、生命周期和测试；更新前端 API、材料库、每日记录、笔记页、样式和测试；同步产品规格与本进度文档。隔离 QA 脚本和结果位于被忽略的 `output/e2e-qa/2026-07-20-material-reliability/`。
- 验证结果：后端 Ruff 与 65 项 pytest 通过；前端 ESLint、TypeScript、75 项 Vitest 和 Vite `8.1.4` 生产构建通过。真实 Yale 官方页面刷新成功，提取 45 个逐字稿块、约 5.2 万字符，首尾定位为 `Chapter 1 [00:00:00]` 与 `Chapter 6 [00:50:26]`。真实 GPT-5.5 Medium 回顾评阅 49.5 秒完成，输出 2,241 字符并持久化 10 条具体块引用，全部匹配新活动哈希。原“覆盖确认”额外调用在 12 分钟超时后被移除，改造后相同材料的用户可见评阅约 60 秒完成。
- 验证限制：未再次调用 Gemini 或写入 Obsidian；长笔记后台恢复由集成测试验证，真实 Yale 完整笔记此前已有成功重试样本。本机仍有 Starlette/httpx 上游弃用提示和无效 `SSL_CERT_DIR` 非阻塞警告。

### 2026-07-20 - 首次安装、桌面无窗口启动与安全停止

- 安装与首次使用：新增可重复执行的 `install-local.cmd` 和 PowerShell 安装器，检查 uv/Node/npm，优先复用现有 Python 3.12，按锁文件同步依赖、构建前端、使用 SQLite Backup API 保留最近 5 份一致性备份，并创建当前用户桌面与开始菜单快捷方式。全新数据库首次打开设置引导，修复或升级不重复显示。
- 日常运行：新增 `pythonw` 启动器和桌面 Uvicorn 入口。启动器重新读取当前用户代理，通过项目派生的 Windows 互斥避免并发启动，验证健康接口中的服务身份，只监听 `127.0.0.1:8000`；PID 和 5 MB×3 的轮转日志保存在被忽略的 `runtime-data`。再次双击只打开页面，不创建第二个进程。
- 关闭与回退：设置页新增“本地服务”和不可误关的确认框，只有桌面管理的同源 JSON 请求能够优雅停止；开始菜单提供独立停止入口。普通 Uvicorn 返回 409，`start-local.cmd` 保留为需要常驻窗口的诊断入口。关闭浏览器不会自动终止服务或后台任务。
- 主要文件：新增 `install-local.cmd`、`launcher/`、`backend/app/desktop_server.py`、`backend/app/maintenance.py` 及对应测试；更新后端应用和设置 schema/API、前端设置页/API/样式/测试、`start-local.cmd`、README、产品规格和本进度文档。快捷方式、PID、日志、备份和首次设置标记均为本机忽略内容。
- 验证结果：安装器在当前机器两次实跑成功，第二次明确复用 Python 3.12.3；桌面和开始菜单快捷方式均指向项目虚拟环境 `pythonw.exe`。实机启动后健康接口为 `ok`、设置返回桌面模式，PID 与监听 PID 一致；重复启动 PID 不变且只有一个监听，站内关闭后端口释放、PID 文件删除。设置与首次引导在 1440×900 和 390×844 无横向溢出，控制台 0 error/0 warning。后端 Ruff、74 项 pytest、Alembic 检查及前端 ESLint、TypeScript、77 项 Vitest、Vite 构建全部通过。
- 验证限制：缺失依赖时通过 WinGet 的真实安装分支未在已有完整环境的机器上触发；首次设置标记使用隔离测试验证，未为了验收删除或替换真实数据库。保留 1 条 Starlette/httpx 上游弃用警告和本机无效 `SSL_CERT_DIR` 警告。

### 2026-07-22 - Lumina 品牌、正式图标与安全卸载

- 品牌与图标：产品对外改名为 Lumina，公开项目标识为 `lumina-study-coach`。使用内置 ImageGen 生成“静谧启明”“晨光书页”“知识之门”三版候选，选择小尺寸最清楚的“晨光书页”；保留原始候选、透明主图和浅深背景尺寸检查图，并生成 1024px 主图、16-256px Windows ICO、32/192px favicon 和 180px Apple Touch Icon。侧栏、浏览器、设置页、启动/停止提示和包描述已同步，稳定内部数据标识保持兼容。
- 安装与卸载：安装器强制使用 Lumina ICO，升级时清理旧“学习流程教练”入口，并在桌面和开始菜单创建 Lumina 启动、停止和卸载快捷方式。新增 `uninstall-local.cmd`、PowerShell 卸载器和纯 Python 清理策略；默认只移除入口，第二档清理生成环境，完整清理额外删除 `runtime-data`，必须双重确认并默认向文档目录创建最后备份。删除目标固定白名单不包含源码或 Obsidian Vault。
- 主要文件：新增 `design-references/brand/`、`launcher/assets/lumina.ico`、`launcher/build-brand-assets.py`、`launcher/uninstall-study-web.ps1`、`launcher/uninstall_policy.py`、`launcher/tests/` 和 `uninstall-local.cmd`；更新前后端包元数据、前端品牌与图标、Windows 启动脚本、README、产品规格和本进度文档。未修改数据库 schema、课程/材料/记录数据、模型设置或真实 Vault。
- 验证结果：前端 ESLint、TypeScript、78 项 Vitest 和 Vite `8.1.4` 构建通过；后端 `ruff check app tests`、74 项 pytest 和 Alembic 漂移检查通过；启动器 Ruff、7 项 unittest、Python 编译和全部 PowerShell 解析通过。最终安装实跑创建的四个快捷方式均引用自定义 ICO，旧入口为零；默认卸载实跑后数据库 SHA-256 不变，随后重装成功。完整清理预演只列出 `.venv`、`node_modules`、`dist`、`runtime-data`，缺少第二确认时正确拒绝。无窗口启动后健康状态为 `ok`、桌面模式为真且只有一个 `127.0.0.1:8000` 监听。
- 视觉验收：1440×900 和 390×844 均显示 Lumina 与正式品牌图，PNG 实际加载为 32×32；页面无横向溢出、控制台 0 error/0 warning。Windows ICO 验证包含 16/24/32/48/64/128/256px，主图四角透明。
- 验证限制：为保护真实学习数据，没有实际执行完整数据清理，仅执行双重确认拒绝、白名单单元测试和无副作用预演。全仓库 `ruff check .` 仍会报告历史迁移 `20260720_0012` 的 3 处既有行宽问题，约定范围 `app tests` 和新增 `launcher` 均通过；保留 1 条 Starlette/httpx 上游弃用警告和本机无效 `SSL_CERT_DIR` 警告。

### 2026-07-22 - 发布前 13 项横向优化与完整验收

- 安全与数据：新增全局浏览器写请求来源保护；安装、升级和完整卸载改用带版本清单、SHA-256 和 SQLite 完整性校验的数据库+材料归档，新增可确认、可回滚的 `restore-local.cmd`，并保留登录状态与日志。
- AI 与性能：结构化练习、批改和预习问题在运行成功落库前验证完整性，失败状态可追踪；模型状态和选项合并为快照接口并对并发 CLI 探测做短时缓存，前端兼容尚未重启的旧服务；PDF 提取下沉线程池，课程关系限制字段加载，Obsidian 笔记读取按修改时间缓存。
- 界面与维护：提高陶土橙、成功绿和危险色的正文对比度；390px 导航压缩为稳定单行，品牌使用 192px 高清资源；运行状态显示 Lumina 与统一 `v0.1.0`，隐藏内部服务名。每日记录纯逻辑拆至支持模块，前端按路由拆包保持；前后端版本统一，补充 Windows CI，修复全仓库 Ruff，锁定无弃用警告的 TestClient 传输依赖并初始化 Git 元数据。
- 主要文件：新增 `.github/workflows/ci.yml`、`backend/app/request_security.py`、`backend/app/ai_output_validation.py`、`frontend/src/pages/dailyRecordSupport.ts`、`launcher/data_archive.py`、`launcher/restore-study-web.ps1`、`launcher/tests/test_data_archive.py` 和 `restore-local.cmd`；更新前后端应用/API/样式/测试、安装卸载脚本、锁文件、README、产品规格和本进度文档。
- 验证结果：前端 79 项测试、后端 80 项测试、启动器 13 项测试全部通过；全仓库 Ruff、TypeScript、ESLint、Vite 构建、Alembic 漂移和 PowerShell 解析通过。生产深层路由、跨站写拒绝、状态页、390/1440/2560/3840px 响应式及控制台均通过；隔离验收资源已清理。
- 验证限制：未执行真实数据覆盖恢复、真实完整卸载、模型生成或 Vault 写入；恢复正确性使用临时数据库和材料完成同等路径演练。本机继承的无效 `SSL_CERT_DIR` 仍会由 uv 输出非阻塞提示，不属于项目配置。

### 2026-07-22 - 真实材料复验、Gemini 登录修复与内置完整示例

- 真实复验：使用 MIT OCW 18.06 Lecture 1 官方课程页和逐字稿 PDF，在隔离数据库、材料目录和 Vault 中完成回顾、重构、12 题练习、逐题批改、错题、3 条衔接问题、每日摘要、跨自然日交接、GPT 笔记初稿、Gemini 润色和 Obsidian 写入。刻意错第 2、10 题，批改恰好得到 10 正确、2 错误；所有验收检查通过。
- 登录与 AI 修复：确认 Gemini OAuth 报错来自 CLI 令牌交换直连 Google 或继承已停用代理，子进程现会从可达 `ALL_PROXY` 补齐 HTTP/HTTPS 大小写变量；兼容 Antigravity 1.1.5 的 `gemini-3.5-flash-high` 标识。材料锚点改为持久 Codex 线程，并在旧锚点失效时自愈重建一次；Gemini 润色提示词禁止新增事实判断、疑点和外部知识。
- 示例与界面：新增脱敏示例生成器、静态 JSON、授权说明和懒加载 `/example`，课程页始终可进入，首次设置页提供入口。示例展示六步流程、逐题批改和最终长笔记，不调用 API/模型或写入任何用户数据。真实移动端验收修复了反馈正文被横向拆列及长笔记整页溢出，宽表格只在局部滚动。
- 主要文件：新增 `scripts/qa_example_app.py`、`scripts/qa_real_example.py`、`scripts/build_example_pack.py`、`frontend/src/data/example-course.json`、`frontend/src/data/EXAMPLE_LICENSE.md`、`frontend/src/pages/ExamplePage.tsx` 和 `docs/real-example-acceptance.md`；更新 AI provider/workflow/API、前端路由、课程/设置入口、样式与测试，并同步 README、产品规格和本进度文档。
- 验证结果：后端全仓库 Ruff、82 项 pytest 和 Alembic 漂移检查通过；前端 ESLint、TypeScript、80 项 Vitest 和 Vite `8.1.4` 生产构建通过。示例页 390/1440/2560/3840px 无整页横向溢出，390px 笔记渲染 120 个 KaTeX 节点、1 张 GFM 表格和 5 个 Callout，控制台 0 error/0 warning，网络记录只有本地静态资源。生产深层 `/example` 可直接打开和刷新。
- 数据边界：账号、套餐、可执行文件路径、数据库 ID、提示词、缓存目录、原始 PDF/网页快照和隔离 Vault 均未进入静态示例或文档。完整原始响应、隔离数据库、临时 Vault、导出 ZIP 和截图保存在被忽略的 `output/`。

### 2026-07-22 - 发布风险收敛、扫描 PDF OCR 与混合检索

- 课程与示例：移除独立“查看示例”入口，将 MIT 只读示例放入课程首页，可经统一两层确认从本机隐藏；隐藏状态不写业务数据库。课程 API 新增状态、最近学习和创建时间，按进行中最近学习、未开始最近创建、已完成最近完成三组排序；隔离生产端点实测顺序为 `active > not_started > completed`。
- AI 可靠性：生成状态显示真实耗时，45 秒后明确可继续等待或取消；同一范围重复任务被拒绝。取消端点会中断活动 Codex turn 或终止 Antigravity 子进程，后台笔记保留用户取消原因。选择题正确性由本地结构化答案键确定，模型只负责解释，避免错误总评覆盖确定性结论。
- 材料处理：远端 429 和 5xx 最多同源重试 3 次并尊重受限 `Retry-After`。PDF 原生文本优先，扫描页由 Apache-2.0 的 `pypdfium2` 渲染后调用可选 Tesseract `chi_sim+eng`；安装器只在确认后准备 OCR，任何可选依赖失败都不阻断普通安装。真实中英文扫描 PDF 成功识别并复用页级缓存，样例仍出现一个汉字误识别，已作为需人工复核的能力边界记录。
- 混合检索：材料块默认使用本机 SQLite FTS；设置可明确下载 `paraphrase-multilingual-MiniLM-L12-v2` 并启用向量混合排序。真实下载缓存为 252,141,277 字节，中文改写查询把贝叶斯材料排在几何和数据库材料之前。模型缺失、关闭或推理失败时退回 FTS；sidecar、模型和 OCR 缓存不进入业务数据库、源码或备份。
- 开源与数据：建立 Apache-2.0 根许可证和第三方声明，明确 MIT 示例的 CC BY-NC-SA 4.0 边界。因 PyMuPDF 的 AGPL/商业许可不适合作为默认 Apache 项目依赖，实施中改用 Apache-2.0 `pypdfium2`；锁文件已无 PyMuPDF。隐私扫描未发现个人路径、邮箱、数据库、材料、日志、登录状态或 Vault 内容进入源码。
- 验证结果：正式工作区 `uv sync --locked` 通过；前端 `npm run check` 通过 ESLint、TypeScript、81 项 Vitest 和 Vite `8.1.4` 构建；后端 91 项 pytest、Ruff 与 Alembic 漂移检查通过；启动器 13 项 unittest 和四个 PowerShell 脚本默认解析通过。恢复脚本补齐 UTF-8 BOM，避免 Windows PowerShell 把中文误读为语法错误。隔离发布源码不带 `.venv`、`node_modules` 或 `runtime-data`，后端从锁文件同步 59 个包、空数据库自动迁移，FastAPI 生产 `/courses`、`/example`、`/settings` 和任意深路由均返回 200。隔离归档恢复保留数据库与原材料，并排除 OCR 缓存、搜索索引和模型。
- 当时限制：系统外部审批额度在首轮验收期间耗尽，隔离前端联网 `npm ci` 和阶段 26 四视口截图当时未完成；这两项已在下一条“阶段 26 干净发布复验”中补齐。正式 `127.0.0.1:8000` 仍需用户从桌面 Lumina 正常重启以加载新后端；隔离 `8014` 服务验收后已停止。

### 2026-07-22 - 阶段 26 干净发布复验与真实材料补测

- 发布阻塞修复：从 Git 发布清单构建全新副本时发现 `frontend/src/data/example-course.json` 与 `EXAMPLE_LICENSE.md` 被根目录通用 `data/` 忽略规则误伤，导致干净安装缺少内置示例并使前端构建失败。`.gitignore` 现只对这两个静态发布资源增加明确例外；重新生成的干净副本包含 158 个源码文件，不含 `.venv`、`node_modules`、`runtime-data` 或 `dist`。
- 干净安装与自动检查：隔离副本完成 `npm ci`（393 个包、0 个已知漏洞）和 `uv sync --locked`（59 个包）；前端 ESLint、TypeScript、81 项 Vitest 与 Vite `8.1.4` 构建通过，后端 Ruff、91 项 pytest 与 Alembic 漂移检查通过，启动器 13 项测试通过。空数据库由应用启动自动迁移，生产 `/courses`、`/example`、`/settings`、课程详情和任意 SPA 深路由均返回 200。
- 真实材料补测：在隔离 `8016` 服务上传两份图片型扫描 PDF，实际调用 Tesseract 与 `chi_sim+eng` 生成页级 OCR 文本；同一小节另行抓取 Wikipedia `Negative feedback` URL，得到 26 个正文块，确认 PDF 与 URL 多材料并存。默认 FTS 精确命中 `HELIOSTAT`；实际加载既有 252,141,277 字节多语言模型后，控制系统材料排在无关植物材料之前。临时隐藏模型文件时 `model_ready=false` 且 FTS 继续可用，恢复文件后状态自动恢复；临时移走 PDF 原文件时保留上次成功 revision，恢复后同源重新解析成功。
- 页面验收：Codex In-app Browser 在 390×844、1440×900、2560×1440 和 3840×2160 实测课程页无整页横向溢出；2K/4K 主区分别限制在约 1320/1520px 并在侧栏后的工作区居中。实际顺序为进行中、未开始、示例、已完成；示例两层确认后隐藏，直接访问 `/example` 返回课程页。设置页实际关闭并重新启用语义检索，材料库保持站内弹窗并显示 3 项真实材料；浏览器控制台 0 error/0 warning。
- 数据与运行边界：所有课程、PDF、URL、OCR 缓存、搜索 sidecar、模型副本和浏览器隐藏偏好均位于被忽略的隔离发布目录；未读取或修改正式课程数据库、正式材料、账号设置或 Obsidian Vault。本轮没有重复消耗 Codex/Gemini 学习生成额度，阶段 25 的真实六步材料验收继续有效；由自动化环境启动的隔离服务仍按设计显示外部 CLI `WinError 5` 指引，正式 `8000` 服务需由桌面 Lumina 重启。

### 2026-07-23 - AI 计时、节点结果阅读状态与运行状态整合

- 时间契约：Pydantic 响应把 SQLite 无时区时间统一解释为 UTC 并输出带时区 ISO；前端兼容旧无时区 AI 运行时间，非法或未来值从零秒安全显示，修复上海时区固定出现 `28800` 秒的问题。课程活动、材料刷新和课程完成时间同步使用 UTC 输出。
- 流程状态：结构化练习完成后复用普通节点推进规则，练习收起并展开批改。AI 批改成功只生成结果并保持节点待完成、展开到第一道需复核题；用户点击“完成批改与纠错”后才收起并进入今日收尾。今日完成生成摘要后保持收尾节点展开，确保摘要和小节完成入口可见。
- 设置导航：移除侧栏独立“运行状态”，在设置底部显示服务在线状态、版本和启动/关闭方式；旧 `/status` 重定向到 `/settings#local-service`，懒加载完成后主动滚动到目标区域。
- 主要文件：更新 `backend/app/api.py`、`backend/app/schemas.py`、相关后端测试，更新前端 AI 状态、每日记录、设置、路由、样式和测试；删除 `StatusPage.tsx`，新增独立时间解析工具及测试，并同步产品规格和本进度文档。
- 验证结果：前端 ESLint 零警告、TypeScript、85 项 Vitest 和 Vite `8.1.4` 构建通过；后端 Ruff、91 项 pytest 和 Alembic 漂移检查通过，`git diff --check` 无空白错误。隔离生产服务实测普通完成自动推进、旧状态地址锚点、1440×900 和 390×844 响应式，均无横向溢出，控制台 0 error/0 warning；隔离服务和数据已清理。
- 验证限制：没有再次消耗真实 Codex/Gemini 生成额度；批改结果留存、结构化练习推进、今日摘要留存由前端交互回归与后端集成测试覆盖，真实 AI 输出质量沿用阶段 25 的材料验收结论。正式 `8000` 服务仍需从桌面 Lumina 重启后加载本轮后端改动。

### 2026-07-23 - 最新代码安装生命周期隔离回归

- 干净副本：从当前工作树复制 159 个已跟踪及未忽略的新文件，确认不包含 `.git`、`runtime-data`、`.venv`、`node_modules` 或 `dist`。安装器以跳过快捷方式和自动启动的隔离参数执行，完成 uv/npm 锁定同步和 Vite 生产构建。
- 生命周期：隔离启动器使用测试端口 `8018`，正式 `8000` 服务全程保持运行。空数据库自动迁移后，经生产 API 创建课程、章节和小节，并在材料目录写入哨兵；桌面管理关闭接口正常移除监听、进程和 PID 文件。KeepData 卸载不删除数据库、材料、生成环境或源码。
- 数据验证：卸载前后数据库 SHA-256 均为 `A2FD9BE3E0F17991F50C0E20F752DC70D87141B79DC8EA760EDC3029D91EF27C`，材料 SHA-256 均为 `E0F15C246070D49F69DD6F19A5A59863EC73C15F00C76DB9B44940E307F44300`。重装前自动生成带数据库和材料清单的归档，重装后 SQLite 完整性为 `ok`、外键问题为 0，原课程层级和材料仍可读取。
- 生产验证：重装后再次启动成功，`/courses`、`/settings`、`/example`、`/courses/1` 和任意 SPA 深层回退均返回 200。最终隔离服务和大体积依赖副本已清理，只保留被忽略的 `output/e2e-qa/2026-07-23-install-lifecycle-latest-01/RESULT.md`。
- 限制：为避免删除真实桌面/开始菜单入口，隔离副本跳过快捷方式创建和移除；真实快捷方式安装/默认卸载此前已经实跑通过。本机 Tesseract 未向本轮安装环境同时暴露 `chi_sim+eng`，因此只记录可选 OCR 警告，不影响普通安装和本次生命周期结论。

### 2026-07-23 - 首次欢迎流程与设置页解耦

- 产品流程：启动器不再根据首次标记跳转设置页，所有启动统一进入 `/courses`。全新数据目录在课程首页显示一次性“欢迎使用 Lumina”站内弹窗，只提供“开始使用”入口，不要求预先配置模型、Obsidian Vault 或个人描述。
- 交互与状态：欢迎弹窗不能通过遮罩或 Esc 误关；完成请求成功后才关闭并删除 `runtime-data/first-run.pending`。请求失败时保留弹窗并就地显示错误。刷新和后续启动不再出现；旧 `/settings?setup=1` 与普通设置页行为一致。
- 主要文件：更新 `backend/app/api.py`、`backend/app/schemas.py`、`launcher/runtime.py`、前端 API、课程 loader、课程页、设置页、样式及前后端测试，并同步 README、产品规格和本进度文档。未修改数据库 schema 或用户学习数据。
- 验证结果：前端 `npm run check` 通过 ESLint、TypeScript、87 项 Vitest 和 Vite `8.1.4` 生产构建；后端 Ruff、91 项 pytest 和 Alembic 漂移检查通过，`git diff --check` 无空白错误。隔离 `8023` 生产页面实际验证欢迎框、Esc 防误关、完成后删除标记、刷新不复现及旧设置入口失效，浏览器控制台 0 error/0 warning；临时服务已停止。
- 验证限制：没有重复执行完整安装/卸载生命周期，因为本轮未修改安装器创建标记的条件或卸载策略；启动器目标路由和标记状态由单元测试及隔离生产页面共同覆盖。正式 `8000` 服务和正式运行数据未触碰。

### 2026-07-23 - 自包含 Windows 安装包与可选安装位置

- 发布形态：新增 PyInstaller `onedir` 发布入口和 Inno Setup 6 中文安装向导。普通用户安装包内置前端、后端和 Python 运行时，可在向导中选择程序目录并可选创建桌面快捷方式；开始菜单提供启动、停止和卸载入口，不显示命令窗口、不注册 Windows Service、不设置开机启动。源码安装、恢复和诊断入口继续保留。
- 数据与卸载：安装包通过同目录 `install-config.json` 把课程、材料、设置和日志固定到 `%LOCALAPPDATA%\Lumina`，材料目录不再回落到源码根目录。默认卸载只删除程序并保留数据；交互选择删除数据时需要二次确认，并在文档目录备份失败时自动改为保留。新增动态配置文件卸载清单，修复程序目录只剩 `install-config.json` 的问题。
- 可靠性修复：安装配置兼容 Windows PowerShell 写出的 UTF-8 BOM，显式空环境不会错误继承父进程设置；构建规则不再收集 Alembic 测试套件和 ONNX 工具模块。新增限时独立包 QA 脚本，在成功、失败或超时时都会关闭并清理服务，避免把常驻 `--serve` 误判为卡死。
- 主要文件：新增 `installer/lumina.spec`、`installer/Lumina.iss`、仓库内简体中文语言资源、`launcher/release_entry.py`、`launcher/build-release.ps1`、`launcher/qa_release_package.py`、`build-installer.cmd` 及对应测试；更新后端运行目录配置、依赖锁、README、第三方声明、产品规格和本进度文档。
- 实际回归：QA 版本使用独立 AppId、端口 `8127` 和 `%LOCALAPPDATA%\Lumina-QA`，先后安装到两个不同工作区目录。首次与保留数据重装分别在 2.53 秒和 1.30 秒达到 `/api/health=ok`；默认卸载保留 303104 字节 SQLite 数据库和材料哨兵，重装后继续可读；备份 ZIP 含数据库、材料和清单；修复后的最终卸载完整删除程序目录。QA 安装、数据和端口均已清理，正式安装、端口 `8000` 和正式数据未触碰。
- 自动验证：前端 `npm run check` 通过 ESLint、TypeScript、87 项 Vitest 和 Vite `8.1.4` 构建；后端与启动器 Ruff、113 项 pytest、`uv lock --check`、全部 PowerShell 解析和 `git diff --check` 通过。PyInstaller 窗口化包、QA 安装器和生产 `Lumina-Setup-0.1.0.exe` 均成功构建，生产安装器约 60 MB。
- 发布限制：当前本地安装器和 `Lumina.exe` 未代码签名，公开分发前仍需可信 Windows 证书；未自动点击交互卸载的二次确认框，删除分支的备份组件已通过真实 QA 数据归档验证，默认保留分支已完成完整安装生命周期实测。

### 2026-07-23 - 安装器命名与运行中安全卸载

- 命名与布局：安装包改为 `install_Lumina-版本号.exe`，Windows 显示名称保持 Lumina，Inno Setup 的 `unins*.exe/.dat` 收纳在 `uninstall_Lumina` 子目录，不再出现在安装根目录。开始菜单继续使用可读的“卸载 Lumina”。
- 停止契约：发布启动器新增服务 PID 读取、进程存活检查和最长 15 秒的退出等待。卸载与升级只有在 Lumina 对应进程真正退出后才继续；失败时交互卸载提供重试或取消，静默卸载返回失败。其他程序单独占用相同端口不会被误认为 Lumina 正在运行。
- 精准清理：卸载器显式清理安装器拥有的 `install-config.json` 和 PyInstaller `_internal`，随后删除内部卸载组件和空安装目录；不递归清空整个用户选择目录。目录含用户文件的真实测试中，Lumina 程序与组件全部删除，用户文件和目录按设计保留。
- 主要文件：更新 `launcher/release_entry.py`、`launcher/tests/test_release_entry.py`、`launcher/build-release.ps1`、`installer/Lumina.iss`、README、产品规格和本进度文档。未修改数据库 schema、课程数据、正式材料、正式 Vault、模型设置或端口 `8000` 服务。
- 验证结果：前端 ESLint、TypeScript、87 项 Vitest 和 Vite `8.1.4` 生产构建通过；后端 92 项 pytest 通过；启动器 26 项 pytest、本轮修改文件 Ruff、uv 锁检查、PowerShell 解析和 `git diff --check` 通过。最终 QA 包在独立端口 `8127`、数据目录 `%LOCALAPPDATA%\Lumina-QA` 和工作区安装目录中启动后直接卸载，记录真实 `service.pid`，约 3.11 秒后端口释放，`Lumina.exe`、`_internal`、`uninstall_Lumina` 和纯程序目录全部消失；默认数据先确认保留后清理测试目录。另一轮安装目录用户文件边界测试通过。最终 PyInstaller 包、QA 安装器和约 60 MB 的 `install_Lumina-0.1.0.exe` 编译成功，正式安装器 SHA-256 为 `E4411757053760D500B8D56980BAEF372ED40F6038E7FEC949ECBBAA9792C131`。
- 发布限制：正式安装器和 `Lumina.exe` 仍未代码签名。当前更改 `UninstallFilesDir` 适用于尚未公开分发的预发布安装；已安装旧测试版应先用其原卸载入口卸载，再安装新命名版本，避免 Inno Setup 旧卸载日志路径与新路径并存。额外的全 `launcher/` Ruff 扫描仍报告 9 条位于本轮未修改旧文件中的格式问题，本次按范围未改动这些文件。

### 2026-07-23 - v0.1.0 公开发布候选整理与验收

- 公开资料：版本统一为 `0.1.0`，README 默认英文并提供中文切换，加入正式图标、真实应用截图、变更记录、贡献指南、安全策略、NOTICE、开发、隐私和 AI 连接说明；README 明确当前界面语言、未签名 SmartScreen 风险、数据目录与外部 CLI 边界。
- 发布工程：新增统一版本检查、GitHub Actions、Dependabot、CycloneDX SBOM、源码 ZIP 验证、SHA-256 汇总和发布清单；PyInstaller EXE 写入产品版本信息，最终程序根目录直接包含 `LICENSE`、`NOTICE` 和 `THIRD_PARTY_NOTICES.md`。源码归档通过 `.gitattributes` 排除内部执行文档和真实材料 QA 脚本。
- 依赖与安全：升级 FastEmbed `0.8.0` 和 Pillow `12.3.0`，保留 Starlette 官方建议的 `httpx2` 测试依赖。Python 与 npm 漏洞审计均为 0 个已知漏洞；源码密钥、私钥和个人绝对路径扫描无匹配。全前端检查通过 87 项 Vitest，后端与启动器通过 118 项 pytest，Ruff、Alembic、uv 锁、PowerShell 解析、版本元数据和 `git diff --check` 通过。
- 实际安装回归：QA 安装包使用独立 AppId、端口 `8127` 和 `%LOCALAPPDATA%\Lumina-QA`。安装后程序与三份许可文件齐全，服务健康检查成功；运行中静默卸载会先停止服务、删除程序目录并保留材料哨兵，最后 QA 数据、程序目录和端口均已清理。一次验收命令因 `/DIR` 引号拆分误装到 `D:\Everything\Desk\study`，已立即使用该实例自身卸载器完整清理，修正引号后同一测试通过。
- 发布限制：安装器与 `Lumina.exe` 仍未代码签名，本版按 Public Preview 发布。当前记录完成的是本地发布候选验收；GitHub 仓库、标签、Release 和远端资产验证将在发布完成后更新。

### 2026-07-24 - Lumina v0.1.0 Public Preview 发布

- GitHub 发布：创建公开仓库 `https://github.com/Throb7777/lumina-study-coach`，默认分支为 `main`；创建 `v0.1.0` 预发布 Release，并上传 Windows 安装器、验证后的源码 ZIP、发布清单、CycloneDX SBOM 和 `SHA256SUMS.txt`。
- CI 修复：首次 GitHub Windows Runner 发现 Node 22 自带 npm 与项目 npm 11 锁文件不一致，以及一项 Codex 初始化测试隐含依赖本机安装 Codex。CI 现固定 npm `11.6.1`，测试显式使用虚拟可执行路径；本机前端 87 项、后端与启动器 118 项测试复验通过。
- 远端验收：GitHub Actions 运行 `30026337450` 的 `frontend` 和 `backend-and-launcher` 均为 `success`。远端 `main`、`v0.1.0` 标签和 Release 目标均指向 `a31a2e29062cf6bd7cbd776b23ff26dc86a41e8a`。
- 资产验收：最终安装器经 Windows Defender 扫描未发现威胁；远端 5 个资产的文件大小和 GitHub SHA-256 digest 与本地逐一一致。最终安装器 SHA-256 为 `D33B85A8A0BADF8C15498E46CA92B62E7C83D63153FF64628A5A42A81DD1D78E`，源码 ZIP SHA-256 为 `35E3CA2AA988DD454F54A76F043155B832695CB3D6360D7F76484CC82E3CC0D0`。
- 网络说明：本机失效的 `HTTP_PROXY/HTTPS_PROXY=127.0.0.1:7890` 和不可达的 `github.com:443` 使普通 Git HTTPS 推送失败；已使用现有 GitHub CLI 授权通过官方 Git Data API 上传并逐对象校验 SHA，没有新增 SSH key、deploy key 或长期凭据。

### 2026-08-04 - PDF 全路径解析与材料交互更新

- PDF 解析：Windows 发布包内置并校验 Tesseract 5.5.0、`eng`、`chi_sim` 和 `chi_sim_vert`；原生文本页不执行 OCR，扫描页按来源哈希和页码缓存，单页失败不再丢弃其他成功页面。
- 材料规则：材料新增总页数、OCR 页数、失败页数和警告元数据；同一课程作用域支持多个重点材料，首个成功材料仍自动成为重点，失败材料不能被标记。
- 课程交互：章节和小节的材料入口改为独立模态框，关闭后保持原课程阅读位置；全局材料库继续使用既有页面。
- 主要文件：更新材料 API、解析器、模型、schema、迁移、材料库组件、课程详情页、发布构建和 OCR 准备脚本，以及对应测试与文档。
- 验证结果：后端 102 项 pytest、启动器 28 项 pytest、前端 ESLint、TypeScript、97 项 Vitest、Vite `8.1.4` 生产构建和 `git diff --check` 全部通过。正式数据目录未用于自动化测试。

### 2026-08-04 - v0.1.2 公开文档与 Release 一致性修订

- README：英文和中文下载入口直接指向 prerelease v0.1.2，并让版本徽章包含 prerelease；补充内置中英文 OCR、扫描 PDF 部分成功、多重点材料和材料弹窗相关说明。
- 隐私与变更记录：删除 OCR 仍需联网安装的过时描述，明确 OCR 完全本地运行；CHANGELOG 标记 2026-08-04 同版本资产刷新及覆盖安装保留数据。
- Release：正文整体统一为英文，合并原始修复、两次同版本更新、当前验证结果、升级与未签名限制。
- 主要文件：更新 `README.md`、`README.zh-CN.md`、`docs/DATA_AND_PRIVACY.md`、`CHANGELOG.md` 和本进度文档；未修改代码、安装器或发布资产。
- 验证结果：检查 Markdown 本地链接、版本号、`releases/latest`、过时 OCR 安装措辞、Release 中文字符和 `git diff --check`。

### 2026-08-07 - 按钮、错题整理与来源展示一致性修复

- 按钮与布局：为主要、次要、危险、开始和图标按钮统一最小高度、内边距、行高与禁止文字断行规则；同一动作组使用等宽布局。笔记保存区和错题表单在桌面端并排、390px 移动端纵向满宽排列。移动端首次验收发现工具栏说明区占据剩余高度，已修正 flex 基础尺寸并复验。
- 错题流程：批改页只在当前错误或部分正确题目上显示“整理本题”，不再默认选择第一道错题。表单固定显示当前原题与批改后的正确答案，只要求用户填写“注意点”并选择错误类型；后端根据单题、作答和批改结果派生原题、原始作答和正确思路，并拒绝未批改、正确题、跨练习题或重复整理。错题汇总卡片只显示正确答案与注意点。
- 来源显示：练习、批改和内置示例题卡不再渲染“依据”小字；新生成笔记的提示词明确正文不得输出材料引用、来源标记或来源列表，同时结构化交接中的 `source_refs` 仍保留用于内部追溯。既有历史笔记不会被自动改写。
- 主要文件：更新 `backend/app/api.py`、`backend/app/models.py`、`backend/app/prompts.py`、`backend/app/schemas.py`、对应后端测试，`frontend/src/api.ts`、`frontend/src/App.css`、`frontend/src/App.test.tsx`、`frontend/src/dailyRecordSupport.ts`、`DailyRecordPage.tsx`、`ExamplePage.tsx`、`MistakesPage.tsx`、`SectionNotePage.tsx`，以及产品规格和本进度文档。未新增依赖或数据库迁移。
- 自动化验证：主要实现完成后，前端 `npm run check` 通过 ESLint、TypeScript、97 项 Vitest 和 Vite `8.1.4` 生产构建；后端最终复验 `ruff check .` 和 102 项 pytest 通过；隔离数据库执行 `alembic upgrade head` 与 `alembic check`，结果为无新增迁移；`git diff --check` 无空白错误。最后一处单按钮 CSS 网格方向调整后的前端复跑因受限沙箱无法 `lstat C:\Users\Peter` 而未启动 ESLint，该调整已进行 diff 静态检查，但未再次执行完整 Node 检查。
- 实际验收：隔离服务使用 `127.0.0.1:18000`、独立 SQLite 和临时 Vault。桌面/390px 下笔记双按钮与错题保存/取消按钮均等高，组内等宽且文字不拆行；多道错误题可精确选择当前题，表单题目和正确答案对应，保存后显示“已整理”；页面无“依据”或“原始作答”，控制台 0 error/0 warning。
- 数据边界：验收开始时发现 `8000` 已有正式服务，误创建的临时课程及临时 Vault 设置已立即按精确对象回滚；随后复核正式服务健康状态为 `ok`、课程 ID 仍为 `[1]`、Vault 已恢复为 `G:\BaiduSyncdisk\Obsidian Vault`。其余验收全部转移到隔离服务，未调用真实 AI、未修改正式学习内容或正式 Obsidian 文件，也未提交、推送或发布。

### 2026-08-08 - AI 引导回顾、重构与练习质量约束

- 回顾与重构：将原有多个无提示自由填写框重构为“一个自由回忆/重构起点 + 恰好 3 个 AI 针对性问题 + 分题作答 + 综合反馈”。用户可先保存草稿，再生成问题；AI 不可用时仍可只保存起点并完成节点。已有旧版回顾、重构内容继续只读展示，不强制迁移或丢失。
- 数据与接口：新增 `guided_reflections` 持久化模型、schema、迁移和回顾/重构问题生成、答案保存、综合评阅接口；问题、答案、提示词和反馈均与每日记录及类型绑定。迁移同时扩展 `ai_runs.task` 约束，降级路径会先移除新任务运行，避免恢复旧约束失败。
- 练习质量：生成提示词显式禁止复述材料原题、材料示例和既有练习，要求新题至少改变多个情境维度，并限制“结合你的背景/任选一个场景”式空泛任务。生成时纳入同小节历史题干，服务端再拒绝批内重复、历史精确/近似重复和把情境选择转嫁给用户的泛题；材料检索查询补入题目与示例关键词，帮助模型主动避让材料中的现成题例。
- 翻题边界：练习、批改和内置示例统一为第一题不渲染“上一题”、最后一题不渲染“下一题”；桌面端使用不可交互占位保持布局稳定，移动端隐藏占位。引导问题区在 390px 下纵向排列，成组按钮同宽且不产生整页横向溢出。
- 主要文件：新增 `backend/alembic/versions/20260808_0015_add_guided_reflections.py` 和 `backend/tests/test_ai_output_validation.py`；更新后端模型、schema、API、提示词、AI 工作流及测试，更新前端 API、每日记录页、示例页、样式及测试，并同步 `learning_flow_coach_spec.md` 和本进度文档。未新增生产依赖，未修改正式学习数据、正式模型设置或 Obsidian Vault。
- 自动化验证：后端 `backend/.venv/Scripts/python.exe -m ruff check backend` 通过，`backend/.venv/Scripts/python.exe -m pytest backend/tests -q` 为 `107 passed in 66.38s`；前端 `npm run check` 依次通过 ESLint、TypeScript、`9` 个测试文件的 `99` 项 Vitest 和 Vite `8.1.4` 生产构建。`git diff --check` 无空白错误。
- 实际验收：隔离服务运行于 `127.0.0.1:8127`，使用独立 SQLite 和确定性引导数据。1440×1000 与 390×844 均实际显示一个起点、3 个问题、对应答案和综合反馈；两个视口的页面与引导区横向溢出均为 0，移动端 6 个回顾/重构动作按钮宽度均与父容器一致。示例第一题按钮计数为“上一题 0、下一题 1”，最后一题为“上一题 1、下一题 0”；页面公式由 KaTeX 正常渲染。
- 验证边界：为避免消耗用户的 Codex/Gemini 额度，本轮未再次采样真实模型生成；AI 调用契约、三题结构、失败状态和质量拦截由单元/集成测试覆盖，浏览器验收使用隔离的确定性数据验证完整交互和布局。隔离服务、数据库和浏览器会话在验收后关闭，截图保留在工作区 `outputs/`，未提交、推送或发布。

### 2026-08-08 - 跨小节回顾交接、逐题反馈与模块级 AI 状态

- 回顾交接：下一次闭卷回顾改为读取同一课程中时间顺序上最近一条已完成学习记录，不再限定同一小节，也不会跳过“最近记录”去错误复用更早的问题。上一条记录的三个衔接问题在当前页只读显示，并自动建立闭卷回顾的三道定向问题；没有衔接问题时明确提示可在保存自由回忆后生成兜底问题。今日收尾中的问题同样只读，统一命名为“下次回顾问题”。
- 批改与并发：回顾/重构评阅输出新增与三道问题 ID 严格对应的逐题 `correct/partial/incorrect` 结果和反馈，同时保留整体建议。前端将单一全页 AI 忙碌状态拆为按任务管理；生成或评阅某一模块时只锁定该任务自身，练习翻题、完成练习和其他 AI 入口继续可用。成功生成提示显示约 3 秒后自动消失，错误提示持续保留。
- Markdown：后端生成链和前端展示链共同修复 JSON/字符串转义造成的 Bell、Backspace、Form Feed、Vertical Tab、Tab 与 Carriage Return 控制字符，包括截图中的 `omega` 以及 `binom`、`frac`、`cdots`、`times` 等常见 LaTeX 命令。无法识别的控制字符会在生成链被拒绝，在笔记校验预览中被移除并报告；迁移对已确认存在的历史摘要和 AI 运行文本执行确定性修复。
- 主要文件：更新 `backend/app/ai_workflows.py`、`api.py`、`markdown.py`、模型、schema、提示词、输出校验和 `20260808_0015` 迁移及其测试；更新 `frontend/src/pages/DailyRecordPage.tsx`、`dailyRecordSupport.ts`、`api.ts`、`markdown.ts`、样式和测试。未新增依赖，未修改正式数据库、正式材料、模型设置或 Obsidian Vault。
- 自动化验证：后端 `python -m ruff check app tests` 通过，完整 pytest 为 `110 passed in 71.16s`；前端 ESLint、TypeScript、完整 `9` 个测试文件的 `101` 项 Vitest 和 Vite `8.1.4` 生产构建全部通过。测试覆盖跨小节且跨日期/同日期 ID 顺序交接、逐题反馈结构、AI 任务期间练习控件可用、成功提示自动清除、Markdown 前后端修复及真实 Alembic 历史数据修复。
- 实际验收：独立 SQLite 和 `127.0.0.1:8131` 服务中建立 `Class 1 Sample Space → Class 2 Probability Function` 两个小节，第一条记录存入带 U+0007 的 `$P(omega)=1$` 损坏文本并标记完成。下一记录 API 实际返回来源记录、小节标题和三道自动携带问题；浏览器中“上次学习回顾”和“AI 定向追问”均将公式渲染为 KaTeX `P(ω)=1`，今日收尾显示三个只读问题且没有对应编辑框。隔离服务与浏览器标签已关闭；未调用真实 Codex/Gemini，也未提交、推送或发布。

### 2026-08-08 - 作答附件、历史收纳、错题重排与完整备份恢复

- 作答附件：新增 `exercise_response_attachments` 迁移和本地附件存储。仅非选择题显示 30px 高的轻量“添加附件”入口；支持真实签名为 PNG/JPEG/WebP/PDF 的文件，每题最多 5 个、单个最多 10 MB。图片调用本机 OCR、PDF 复用材料解析，提取文本与文字答案共同进入批改；附件自身可构成有效作答并随完整备份迁移。
- 历史与错题：课程详情历史展开状态从 `sessionStorage` 改为页面本地状态，进入每日记录再返回时自动收起。错题整理区重排为纵向原题、全宽正确作答以及紧凑的注意点/错误类型；错题索引展开后保持题答同宽，并对无法关联结构化单题的历史数据标记“旧版记录”。浏览器验收时补发现并修复 `MistakeIndexItem` schema 丢弃 `exercise_item_id` 的根因。
- 备份与恢复：新增共享版本 2 归档实现和设置页“备份与恢复”。归档包含 SQLite、原材料、作答附件和数据库登记的受管笔记，排除登录状态、日志、PID、OCR/搜索缓存及设备偏好；逐文件校验大小和 SHA-256，并检查数据库完整性和外键。导入先预检，包含笔记时要求选择新机器 Vault，恢复后改绑数据库路径；同路径不同内容停止写入。桌面服务退出后启动器先生成安全备份，再原子替换数据、写回结果并启动替代服务；版本 1 归档继续兼容。
- 自动化验证：后端 `python -m ruff check app tests` 通过，完整 pytest 为 `111 passed in 79.62s`；启动器 `python -m ruff check launcher` 通过，补充编排测试后为 `31 passed in 2.34s`；前端 `npm run check` 通过 ESLint、TypeScript、`9` 个测试文件的 `103` 项 Vitest 和 Vite `8.1.4` 生产构建。Alembic 唯一头为 `20260808_0016`，`git diff --check` 无空白错误。
- 实际验收：独立 SQLite 和 `127.0.0.1:8765` 服务中，课程历史状态实测为“初始收起 → 手动展开 → 进入记录返回后收起”；选择题附件入口数量为 0，非选择题为 1，按钮高度 30px。错题页实测原题、正确答案与注意点层级及旧版标记；设置页在桌面和 390×844 下实测完整备份/导入入口、操作组排列和无横向溢出。跨机器归档实验真实写入旧 Vault 受管笔记，恢复到新 Vault 后文件内容一致且 SQLite 路径已改绑；启动器编排实验真实替换临时数据库和材料，并生成恢复前安全备份。
- 验证边界与清理：用户此前提供的临时截图路径在本轮已失效，因此未以该文件完成浏览器中的 OCR 上传；附件签名、持久化、仅附件完成、提取文本进入批改和删除由确定性 API 集成测试覆盖，既有 OCR/PDF 解析回归继续全量通过。验收未调用真实 Codex/Gemini、未读取或修改正式数据库和正式 Vault；隔离服务已优雅停止，浏览器标签及隔离运行目录已清理，未提交、推送或发布。

### 2026-08-09 - 备份安全、附件资源边界与错题渲染自检修复

- 归档安全：ZIP 预检新增 Windows 大小写/Unicode 路径碰撞、保留文件名、成员数量、单成员/总展开大小、加密成员和异常压缩比校验；清单与归档内容均检查重复规范路径。清单读取设独立上限，逐文件大小和 SHA-256 校验及恢复复制改为 1 MB 分块读取，并在恢复写入时再次核对哈希，避免整成员内存读取和校验后替换。
- 附件生命周期：完整备份只按 `exercise_response_attachments.storage_path` 收录仍被引用的文件，不再递归带入孤儿文件或附件 OCR 缓存；课程、章节、小节和旧版练习删除后同步清理物理附件。图片增加像素/边长限制，PDF 限制为 12 页并关闭附件 OCR 缓存，OCR 命令与总时限收紧；单附件和单题提取文字分别限制为 50000/100000 字符，超限标记为“文字已截取”。
- 交互与展示：附件上传/删除使用当前题级忙碌状态，处理期间练习翻题、完成练习和其他模块保持可用；服务端在 OCR 后重新读取题目，降低并发响应覆盖新答案的风险。错题摘要与展开原题统一渲染 Markdown/KaTeX，详情补回完整原题卡片；多个错题表单使用 `useId` 派生唯一标题 ID，消除重复 `aria-labelledby` 目标。
- 暂存与隐私：取消恢复预览会调用删除暂存 API；服务启动和新预检清理超过 24 小时的暂存包及超过 7 天的结果。备份下载补充 `Cache-Control: no-store, private`、`Pragma: no-cache` 和 `X-Content-Type-Options: nosniff`。
- 自动化验证：`backend/.venv/Scripts/python.exe -m ruff check backend launcher` 通过；后端完整 pytest 为 `116 passed`，启动器完整 pytest 为 `33 passed`。前端 `npm run check` 依次通过 ESLint、TypeScript、`9` 个测试文件的 `104` 项 Vitest 和 Vite `8.1.4` 生产构建。新增回归覆盖大小写碰撞、高压缩比包、孤儿/缓存排除、图片尺寸、受限 PDF 提取、文本截取、父级删除物理清理、暂存取消/过期清理、下载隐私头、附件处理期间控件可用、错题公式和唯一 DOM ID。
- 实际验收：独立 SQLite、附件目录和 `127.0.0.1:8765` 服务完成最新迁移并健康启动。真实错题摘要及展开后的原题/正确作答均呈现可访问数学节点，截图检查布局层级正常；真实备份响应返回三项隐私头，上传预检后暂存文件确实存在，取消返回 204 后文件确实消失。隔离服务、浏览器标签、临时归档和运行目录已清理。
- 验证边界：未调用真实 Codex/Gemini、未使用正式数据库、正式附件目录或正式 Obsidian Vault，未执行发布、提交或推送。OCR 的 Tesseract 超时分支由参数契约和既有 OCR 全量回归验证，没有为验收重复消耗长时间真实 OCR。

### 2026-08-09 - v0.1.3 发布、本机升级与正式数据核对

- 版本与发布：统一 `VERSION`、前后端包元数据、安装器、健康接口、测试期望和中英文公开文档为 `0.1.3`；功能提交 `0b337f9` 已推送到 `main`，`v0.1.3` prerelease 标签与 Release 指向该提交。Release 包含安装器、源码 ZIP、manifest、CycloneDX SBOM 和 `SHA256SUMS.txt`。
- 自动化与打包验证：本地 Ruff 通过，后端 `116 passed`、启动器 `33 passed`，前端 ESLint、TypeScript、`104 passed`和 Vite `8.1.4` 生产构建通过；空库升级到 `20260808_0016` 后完整性正常且 `alembic check` 无漂移。GitHub Actions `31290908882` 的 frontend 与 backend-and-launcher 两个 job 全部成功。
- 自包含与资产：复用已安装包中符合锁文件的 Tesseract 与 `eng/chi_sim/chi_sim_vert` 构建输入，PyInstaller 和 Inno Setup 6.7.3 完整成功。隔离数据目录的打包 EXE 实跑返回版本 `0.1.3`、前端就绪、数据库完整且迁移头为 `0016`，内置 OCR 报告三个语言。安装器 SHA-256 为 `30A78F875723517D445AE315828B36730A1BDE02BBDFAB97E2CC1C9D075774AA`，GitHub 远端 5 个资产的大小和 digest 与本地全部一致。
- 本机升级：升级前使用旧版 EXE 在 `C:\Users\Peter\Documents\Codex\Lumina-Data-Safety` 创建独立备份，安装器又在 `%LOCALAPPDATA%\Lumina\backups` 创建升级备份。`G:\Lumina\Lumina.exe` 已替换为 `0.1.3`，其 SHA-256 与打包 EXE 一致；正式服务在 `127.0.0.1:8000` 返回 `ok`、`0.1.3` 和匹配的前端构建标识。
- 数据完整性：升级前后 SQLite `integrity_check=ok`，升级后外键错误数为 0；旧库 21 张业务表行数均保留，19 张内容指纹不变。2 张表中共 5 个 AI 生成文本字段只按 `0015` 迁移执行损坏控制字符到 `omega/binom/frac` 的确定性修复，66 个相关字段逐项复算的意外差异数为 0。11 个已归档材料哈希无差异，原 OCR 缓存和 9 个语义模型文件保留；新增的引导反思与作答附件表均为空，没有伪造历史数据。

### 2026-08-14 - Antigravity CLI 1.1.12 模型列表兼容修复

- 根因与修复：`agy models` 从旧版单列输出变为 `模型 ID + Tab + 显示名` 双列输出后，原解析器的整行 `fullmatch` 和状态精确比较把全部模型过滤为空。后端现按列提取模型 ID 与显示名，并从解析出的模型/强度生成等价 CLI ID；模型选项、连接状态和登录完成检查共用同一兼容逻辑，旧版单列显示名和单列 ID 继续支持。
- 自动化验证：新增 CLI `1.1.12` 双列解析、双列首选模型可用和旧版显示名可用回归；聚焦测试通过，完整 Ruff 通过，后端为 `118 passed`。启动器 `33 passed`，前端 `npm run check` 通过 ESLint、TypeScript、`9` 个测试文件的 `104 passed` 和 Vite `8.1.4` 生产构建；uv 锁检查、空库升级到 `20260808_0016`、`alembic check` 和发布元数据检查通过。
- 实际与打包验收：修复后的源码在 Antigravity CLI `1.1.12`、最终自包含 EXE 在自动更新后的 `1.1.13` 均通过实际调用；账号 `yanyu020816@gmail.com` 返回 4 组 Gemini 模型，首选 `Gemini 3.5 Flash / high` 状态为 `connected` 且 `model_available=true`。PyInstaller 与 Inno Setup 6.7.3 完整构建成功，公开安装器 SHA-256 为 `AFD47EA99A4E0BB6C44C33350BE9A75CE870CDF3930F74B77EF8CA9E1B5F8736`。
- 本机替换与数据边界：未运行安装器，正常停止服务后只覆盖 `G:\Lumina` 程序文件，保留现有安装配置和卸载组件。最终 EXE 版本为 `0.1.3`，SHA-256 为 `8697954A4178ACF9B66A242328B958DBC9F221C537FAD7059F2DDA762AF80770`；正式服务重新启动后健康、前端就绪且 Gemini 状态正确。
- 数据验证与发布：替换前后正式数据库、材料和作答附件目标共 16 个文件的路径、大小和 SHA-256 逐项一致，`install-config.json` 哈希一致；未读取或修改 Obsidian Vault，未执行数据库迁移、备份恢复或真实 Gemini 内容生成。修复提交 `9766c4b` 已推送到 `main`，同一 `v0.1.3` 标签已移动到该提交；GitHub Actions `31767280194` 和 `31767329766` 均成功，Release 5 个资产的大小和 digest 与本地逐项一致。

### 2026-08-25 - 小节笔记生成结果恢复与无限轮询修复

- 根因：AI 运行 41 已得到完整结构化输出并过早标记为 `completed`，但后续 Markdown 规范化遇到位于 `\\sigma` 前的 U+007F 时抛错；后台异常处理只会把 `running` 改成 `failed`，结果接口又重复触发同一异常并返回 500。前端把所有非 404 读取错误都当成暂时未完成，每 5 秒持续重试，因此页面一直显示“生成中”，实际结果也无法恢复。
- 后端修复：在 Codex/Gemini 运行进入完成态之前统一规范化 `display_markdown`，可确定修复的 U+007F 加 LaTeX 命令组合会移除控制字符；所有后处理异常都会收敛为 `failed`。结果接口对历史不可规范化内容返回明确 422，对可修复的历史完成结果返回规范化正文，不再产生无说明的 500。
- 前端修复：把“已有待恢复运行 ID”和“正在读取结果”拆成独立状态；结果完成或明确失败时停止轮询，非 404 读取错误保留运行 ID 并显示可操作说明，用户可点击“重新读取结果”。按钮状态区分生成中、已有待恢复结果和重读，不再用无限忙碌状态掩盖服务端错误。
- 自动化验证：`backend/.venv/Scripts/python.exe -m pytest backend/tests -q` 为 `120 passed in 73.21s`，后端 Ruff 通过；`npm run test` 为 `9` 个文件、`105 passed in 54.67s`，ESLint、TypeScript 和 Vite `8.1.4` 构建通过；launcher 为 `33 passed in 2.42s` 且 Ruff 通过。最新迁移头仍为 `20260808_0016`，隔离数据库 `alembic check` 返回无新增操作，`git diff --check` 通过。
- 隔离与打包验收：隔离服务在 `127.0.0.1:8141` 复现未知 U+0001 结果后，页面停止轮询并显示“重新读取结果”；把同一结果改为可修复的 U+007F 加 `\\sigma` 后，预览弹窗正常渲染 KaTeX `σ`，浏览器控制台 0 error/0 warning。PyInstaller 自包含包在隔离目录和 8142 端口约 4.31 秒健康就绪，版本仍为 `0.1.3`。
- 本机替换与数据边界：正式服务停机后只替换 `G:\Lumina` 的 `Lumina.exe`、`_internal` 和许可文件，保留 `install-config.json`、卸载组件和 `%LOCALAPPDATA%\Lumina`。正式运行 41 随后返回 `completed` 与 12174 字符结果，U+007F/U+0001 均不存在；替换前后数据库、材料、附件和安装配置共 17 项大小与 SHA-256 完全一致。新 EXE SHA-256 为 `424AA2B61CDC5B9369A8298EBBE677F9E19C186D3CE9FF3C97421EE0B2E05181`。修复已提交到 `main`，`v0.1.3` 标签和 5 个 Release 资产已按同版本刷新；本轮生成的旧程序备份、隔离 QA、依赖、构建输出和缓存已在远端核对后清理。

### 2026-08-26 - 练习批改控制字符、原子失败与完成结果恢复

- 复现与根因：正式数据库只读检查确认批改运行 47 至 50 都已由 `gpt-5.5 / medium` 返回完整 12 题结构化结果并标记完成，但第 6 次练习仍停在 `submitted`。结果中的 U+000C 加 `rac` 可由既有规则恢复，另一个位于 `\{X>t+s\}` 前的 U+007F 不符合原来仅接受字母命令的规则，后续逐题写入时抛出 `ValueError` 并由接口暴露为通用 500；重试因没有复用已完成运行而重复调用同一提示词。
- 修复设计：U+007F 只在紧邻反斜杠时移除，覆盖 LaTeX 命令和 `\{` 等符号转义，其他未知控制字符继续拒绝。结构化批改验证器在 AI 运行进入完成态前检查题号、判断值、非空反馈并逐条规范化；失败运行收敛为明确 502，练习和全部作答保持提交态。再次批改仅在练习仍为提交态且提示词、模型、推理强度完全一致时，倒序验证并复用已有完成结果，避免重复消耗模型请求。
- 回归验证：精确 Markdown 和 API 集成测试为 `47 passed in 18.27s`；后端 Ruff 通过，完整后端测试为 `124 passed in 67.42s`；启动器为 `33 passed in 2.22s`；前端完整检查通过 ESLint、TypeScript、`9` 个文件的 `105 passed` 和 Vite `8.1.4` 生产构建，发布元数据检查保持 `0.1.3`。回归覆盖可恢复 `\{`、未知 U+0001 的 502 与零部分写入、以及完成结果恢复不新增 AI 调用或运行记录。正式运行 50 的输出仅以 SQLite 只读模式加载，修复后的验证器成功解析 12 条结果且规范化后无控制字符。
- 构建与本机验证：前端 `npm ci` 与 Vite `8.1.4` 生产构建、PyInstaller `6.21.0` 独立包均成功；产物使用独立数据目录和端口 `18123`，约 3.81 秒达到健康状态并正常关闭。正式服务优雅停机后只交换 `G:\Lumina` 的 `Lumina.exe`、`_internal` 和许可文件，保留安装配置与卸载组件；最终 EXE SHA-256 为 `0995049DC4C0235F35F4E9BC2C7A6CF25CC3F829B860F839250C91CF2F809F54`，服务返回 `ok`、`0.1.3` 和前端就绪。
- 数据与发布边界：程序替换前后 `learning-flow-coach.db` SHA-256 均为 `7F8A5A08212CD38437C5E5028149C765A533AFCE9D57837EF10931C24A8C9406`；材料、作答附件和材料上下文共 53 个文件的路径、大小与 SHA-256 逐项一致。未执行数据库迁移、清理、恢复或正式批改写入；未修改 Obsidian Vault。功能提交 `0e92dcb` 已推送到 `main`，轻量标签 `v0.1.3` 已移动到该提交；同版本 prerelease 的安装器、源码 ZIP、manifest、CycloneDX SBOM 和 SHA256 清单均已覆盖并与远端 digest 一致，GitHub Actions `32932113889` 与 `32932081844` 均成功。

### 2026-08-28 - AI 控制字符净化与 Codex App Server 自愈

- 根因与内容修复：结构化 JSON 中误写的 LaTeX 反斜杠会先被解析为 U+0008 等 C0 控制字符；既有白名单只能恢复 `begin/binom/beta` 等已知命令，位于 `E[X]` 前的 U+0008 会让整组批改失败。生成链现先恢复可确定的 LaTeX 转义，再递归移除所有没有可见语义的剩余 C0 控制字符，并在结构化验证前处理每个字符串；清理后的 JSON 才会进入运行记录和业务写入。历史用户内容的严格校验与问题报告行为保持不变，提示词同时明确 JSON 反斜杠转义要求。
- Codex 连接修复：App Server 的 asyncio stdout 单行上限从默认约 64 KiB 提升为 16 MiB，避免大 JSONL 消息终止读取协程。读取流异常会立即释放请求与生成等待；下一次 `account/read` 或 `model/list` 会关闭失效进程、重建连接并只重试一次。模型刷新临时失败时保留并明确标注上次成功列表，不再把短暂刷新故障直接伪装成模型不可用。
- 自动化与实际验证：后端 Ruff、启动器 Ruff、前端 ESLint/TypeScript 均通过；完整后端、启动器和前端分别为 `129 passed`、`33 passed`、`105 passed`，Vite `8.1.4` 生产构建成功。回归覆盖 U+0008 `E[X]`、未知 U+0001、128 KiB JSONL、读取流异常释放活动 turn、只读请求单次自愈和模型列表缓存回退。真实 Codex App Server 在隔离认证副本中读取到 5 个模型，主动终止 stdout reader 后再次成功读取账号；副本随即删除。
- 打包、本机与数据边界：PyInstaller `6.21.0` 使用锁定依赖和既有校验通过的 OCR 资产构建 `0.1.3` 自包含包；隔离数据目录与端口 `18130` 实跑返回 `ok`、前端就绪、5 个 Codex 模型选项和中英文 OCR，随后正常停止。`G:\Lumina` 只替换程序文件，最终 EXE SHA-256 为 `FDA3BE3C342EC35B24D46D6A3D11F144FAF064350573696B8BD3353D684C92CD`；正式数据库与材料共 24 个文件在替换前后的聚合 SHA-256 均为 `D8271C627810E36C09591DA56A1B7AE1DE9C38342733D30C5EC5271596D1F78F`，`install-config.json` 不变。为严格避免启动过程打开正式数据库，替换后服务保持停止，未执行正式迁移、生成、恢复或数据写入；同一 `v0.1.3` 标签和 Release 资产随后按本轮提交刷新。

### 2026-09-02 - 图片多模态批改、页面定位与未保存状态修复

- 多模态作答：图片附件不再只把 OCR 文本拼入提示词。后端安全解析属于当前作答的本地图片路径，验证模型显式输入能力后，将原图以 Codex App Server `localImage`、`detail: original` 传入，并把图片目录加入运行时工作区根；OCR 文本保留为辅助信息，空 OCR 不再把有效图片误判为未作答。PDF 仍按文本提取批改，选择题仍由本地答案键判断。
- 输入透明度：作答附件 API 增加实际批改输入方式、提取文字长度和限长预览。页面以紧凑标签显示“原图直读 · OCR 辅助 N 字”或“文本提取 N 字”，可按需展开 OCR 预览，避免用户误以为图片已上传但模型没有看到原图。模型明确声明仅支持文本时会在调用前返回清晰错误，不静默退化。
- 页面与状态：React Router 的新导航在无锚点时回到页面顶部，浏览器前进/后退和锚点导航保留原语义。每日记录只统计当前已挂载表单的未保存状态；生成练习和今日完成前先保存当前可见草稿，卸载组件遗留的幽灵键不再阻断完成。浏览器草稿恢复能力保持不变。
- 公式与收尾：收尾问题提示词要求单行紧凑数学表达；输出验证移除自带编号，将简单单行展示公式转换为行内公式并合并多余换行，残留展示公式会被拒绝重试。前后端仅在行内数学范围修复重复 LaTeX 命令反斜杠，不破坏矩阵等展示公式的换行。
- 自动化验证：后端 Ruff 通过，完整 pytest 为 `133 passed in 79.96s`；前端 `npm run check` 通过 ESLint、TypeScript、`9` 个测试文件的 `109 passed` 和 Vite `8.1.4` 生产构建；启动器发行检查为 `17 tests OK`，发布元数据检查保持 `0.1.3`，`git diff --check` 无空白错误。回归覆盖准确的 `localImage` 协议和工作区根、文本模型拒绝、图片批改集成、输入方式 UI、收尾公式规范化、Markdown 反斜杠、生成练习/今日完成前自动保存及新导航顶部定位。
- 实际验收：独立数据目录和 `127.0.0.1:18141` 服务中建立 12 章课程，从课程页 `scrollY=1739` 点击“继续学习”后每日记录为 `scrollY=0`；填写自由回忆后直接点击“今日完成”，表单先保存且未完成项弹窗正常出现，浏览器控制台无错误。PyInstaller `6.21.0` 自包含包和安装位置 `G:\Lumina\Lumina.exe` 分别使用隔离目录、端口 `18142/18143` 启动，均返回 `ok`、`0.1.3`、前端就绪，内置 Tesseract `5.5.0.20241111` 可运行。
- 本机与数据边界：只替换 `G:\Lumina` 的 `Lumina.exe`、`_internal` 和许可文件，保留安装配置与卸载组件；EXE SHA-256 为 `3F6E719F5F7FA972E530DAB8F6A3A9DC129AC8D58F1AFE137A8455AA2280DF7C`。替换前后正式 `learning-flow-coach.db`、`search-index.db`、`materials`、`answer-attachments` 和 `backups` 的五组聚合 SHA-256 逐项一致，`install-config.json` 哈希不变。未启动正式数据目录、未执行迁移、生成或恢复；正式服务保持停止。该验收阶段尚未提交、推送、打标签或刷新 Release。
- 发布刷新：功能提交 `c9b62e8574d12b1c64c29ab3de19e78799d8a87c` 已推送到 `main`，轻量标签 `v0.1.3` 已移动到该提交；GitHub Actions `33628879531`（main）和 `33629303868`（tag）均成功。重新构建的安装器 SHA-256 为 `7DC9E93FB5AF799D87AC99274C3AF65671DF1534654E326FE1F35A06FA3968F6`，源码 ZIP 为 `32F637AD54EED7F897F8A7477912298DA0E3E4B7CA345CDE88E6896E0EB7BE00`；同版本 prerelease 的安装器、源码 ZIP、manifest、CycloneDX SBOM 和 SHA256 清单均已覆盖，GitHub 端 5 个资产的大小与 digest 和本地逐项一致。发布整理未触碰正式安装位置或 `%LOCALAPPDATA%\Lumina`。
