# Lumina：本地学习流程教练产品规格

## 1. 产品定位

本工具是一个面向个人研究型学习的本地 Web 应用。它的核心不是做普通笔记软件，也不是做自动总结工具，而是提供一个可持续执行的学习流程框架。

产品品牌名为 **Lumina**，公开项目标识使用 `lumina-study-coach`，完整描述名为“Lumina · 学习流程教练”。桌面、开始菜单、侧边栏和浏览器标题优先显示 Lumina；“学习流程教练”继续作为产品类别和 AI 角色描述，不作为紧凑界面的重复品牌名。

品牌图标为暖色打开书本，光从书脊向上散出，表达理解和启发。正式图标必须在 16px 下仍可辨认书本与光线，并与暖白、陶土橙界面一致。ImageGen 原始候选、透明主图和尺寸检查图保存在 `design-references/brand/`；Windows 和 Web 使用由同一主图生成的 ICO/PNG 资源。

主要目标是解决“学习过程散”的问题：用户打开工具后，可以自然进入当前课程、当前小节和当天学习流程，不需要每天重新设计学习步骤。系统通过固定流程、节点进度、打勾反馈和轻量动画，给学习过程提供节奏感和完成感。

第一版主要服务用户本人，但项目会开源。因此设计应以个人高效使用为第一优先，同时保持代码和配置边界清晰，避免个人数据进入仓库。

## 2. 核心原则

1. 流程优先，功能次之。
2. 主动学习优先，AI 辅助次之。
3. 结构化笔记优先，卡片化概念不是第一目标。
4. 本地可控优先，不做云同步。
5. Obsidian Markdown 是整理后笔记的主来源。
6. Web 负责学习流程、记录、练习、错题、索引和编辑。
7. 第一版不做应用登录、不做移动端专项适配、不引入 Electron/Tauri 桌面壳；Windows 同时提供自包含安装包和源码级安装/修复脚本，两者都使用无窗口本地启动器并在浏览器中呈现界面。
8. 导出能力重要，但属于低频功能，应放在设置或低频入口中。

## 3. 用户真实学习流程

用户当前理想学习流程如下：

1. 上次学习内容闭卷回忆。
2. 自主看课、阅读 PDF 教材或论文。
3. 合上材料，自己主动重构。
4. 让 AI 根据当前内容出题，用户自己作答。
5. 使用 AI 批改答案，整理错误。
6. 生成下一次闭卷回顾问题。
7. 小节接近完成时，整理结构化小节笔记。
8. 将小节笔记保存到本地 Obsidian 仓库。

工具需要围绕这条流程降低启动成本和组织成本，而不是替代用户学习。

## 4. 第一版完整主流程

第一版应优先打通下面这条主路径：

```text
创建课程
→ 创建章节和小节
→ 进入小节
→ 自动生成或打开今日学习记录
→ 按固定节点完成学习流程
→ 在 Web 内生成练习；连接不可用时查看并复制手动提示词
→ 用户在 Web 中作答
→ 在 Web 内批改答案；连接不可用时使用手动提示词
→ 保存错题记录
→ 在 Web 内生成下一次闭卷回顾问题
→ 将只读问题自动交接到同一课程的下一条学习记录
→ 小节接近完成时整理小节笔记
→ 保存小节笔记到 Obsidian
→ 手动标记小节完成
```

## 5. 信息架构

### 5.1 课程

界面中统一使用“课程”这个名称，不再使用“项目”作为主对象名称。

课程示例：

- 概率论与数理统计
- 因果推断入门
- 动态系统与复杂系统
- 某门视频课程
- 某篇论文或一组论文的系统学习

课程包含：

- 课程名称
- 课程描述
- 长期研究方向或学习目标
- 章节列表
- 小节列表
- 学习记录
- 练习和错题
- Obsidian 笔记目录配置

### 5.2 章节

章节用于组织课程结构。Web 内需要保留章节层级，便于按课程结构推进。

章节包含：

- 章节标题
- 排序
- 所属课程
- 小节列表

### 5.3 小节

小节是学习组织的基本内容单位。

小节包含：

- 小节标题
- 所属章节
- 所属课程
- 状态：未完成、进行中、已完成
- 多条每日学习记录
- 关联练习
- 关联错题
- 关联问题
- 一份整理后的结构化笔记
- 对应 Obsidian Markdown 文件路径

小节状态规则：

- 新建小节默认为未完成。
- 用户进入并产生学习记录后，可视为进行中。
- 小节是否完成由用户手动确认。
- 已完成小节默认不再新增学习记录。
- 用户仍可手动选择继续已完成小节，但系统应提示确认。

### 5.4 每日学习记录

学习对象按小节组织，但学习行为按天记录。

一个小节可以有多条每日学习记录。例如一个小节学了 3 天，则该小节下有 3 条每日学习记录。

每日学习记录包含：

- 日期
- 所属课程、章节、小节
- 当天流程节点完成情况
- 上次内容闭卷回忆
- 主动重构
- AI 出题练习
- 用户答案
- AI 批改反馈
- 错题整理
- 明日预习问题
- 是否今日完成

进入小节时的规则：

- 如果当天还没有学习记录，自动创建今日学习记录。
- 如果当天已经有学习记录，直接打开今日记录。
- 上次内容闭卷回忆参考该小节最近 1-2 条学习记录。
- 课程详情中的历史入口必须逐条显示为“日期 + 当日已完成/当日未完成”，不得只省略未完成状态，也不得让相邻日期看起来像日期区间。

## 6. 每日学习流程节点

每日学习记录采用固定顺序。固定顺序能减少决策负担，也更适合第一版实现。

标准节点：

1. 闭卷回顾
2. 材料学习
3. 主动重构
4. 练习与推导
5. 批改与纠错
6. 今日收尾

这六步属于“今天在当前小节上的一次学习记录”。小节可以一天学完，也可以跨多天；系统不要求用户选择学习模式。每天都会保留独立记录并完成一组练习。小节笔记不属于每日固定节点，只在用户判断小节已经学完时进入小节完成流程。

节点完成规则：

- 普通“保存”用于保留阶段性内容，不强制完成节点。
- 点击“保存并完成”时，系统先检查当前节点约定字段；存在未完成项时使用站内居中提示框列出具体字段，并提供明确选择，不使用浏览器原生校验提示。
- 闭卷回顾、材料学习、主动重构和批改反馈允许用户确认后仍然完成；保存必须先成功，之后才能更新节点状态。
- 练习题目和作答均未完成时，替代选择为“改为跳过”；今日收尾原则上保留三条完整衔接问题。
- 点击“今日完成”时，如果前面仍有未完成节点，应列出节点名称并让用户选择返回流程或仍然结束今天。
- 提示文案只说明哪些字段或节点未完成，不描述输入内容的字符形式。

### 6.1 闭卷回顾

目的：让用户先主动回忆，而不是直接进入材料。

节点只保留一个“自由回忆”输入区，引导用户在不看材料的情况下写下仍能回忆的相关知识、核心概念及其关系，不再要求用户自行判断并分别填写“相关知识”“核心概念”和“清晰部分”。同一课程时间顺序上最近一条已完成学习记录留下的 3 个问题会只读显示并自动成为本次定向问题；不能只限定同一小节，也不能跳过最近记录去复用更早的问题。最近记录没有留下问题时，保存自由回忆后由 AI 根据上次学习内容生成恰好 3 个兜底问题；问题不得泄露答案、提示或材料位置，也不得混入本次尚未学习的内容。用户回答全部问题后再由 AI 进行一次评阅，对每题给出“正确 / 部分正确 / 错误”和具体反馈，并补充整体建议，不继续追加问题。

“遗忘与卡点”由 AI 根据自由回忆和定向问题回答识别，不作为用户填写字段。最近 1-2 条学习记录摘要只能在用户保存自由回忆后作为“回忆后核对”的折叠参考出现，不能在首次作答前直接提示答案。AI 不可用时允许用户仅保存自由回忆并明确确认完成，不能因为账号连接问题阻塞学习记录。

回顾与重构的逐题反馈和综合反馈应优先使用紧凑段落：短单行公式使用行内公式并与“可写为”“可得”“满足”等引导语保持同段；矩阵、分情况表达、多行推导和确实过长的公式继续使用独立公式块。该规则同时用于新生成内容和既有历史反馈的显示兼容，不改写历史学习数据。

### 6.2 材料学习

实际看课和阅读仍主要发生在 Web 外部，但 Web 需要保存后续 AI 处理所依赖的参考材料。

支持的来源：

- PDF 文件
- 公开 HTTP/HTTPS 网页 URL
- 带可访问中文或英文字幕的公开视频 URL；只保存字幕，不下载视频文件

材料可以归属整个课程、某个章节或某个小节。课程材料会自动向下继承，章节材料在本章小节中可用，小节材料只在当前小节可用；同一范围可以指定一个主材料。某一范围添加的第一份材料自动成为该范围的主材料，添加时不要求用户额外勾选；后续通过材料列表中的主材料操作进行切换。

从每日学习记录添加材料时，作用域固定为当前小节，不显示课程、章节和小节范围选择器，避免用户误以为正在为本次记录之外的范围配置材料。课程详情页分别在课程、章节和小节位置提供固定作用域的材料入口；设置页中的“打开材料库”在站内大尺寸弹窗中提供跨课程的统一查看、筛选、刷新、删除和归属调整。用户从哪个层级入口添加，材料就固定归属哪个层级，不再要求重复选择范围。

每日学习记录展示当前小节可继承的材料，并允许用户：

- 选择本节后续 AI 是否使用某项材料
- 填写本次页码、章节或范围
- 继续使用“学习范围”字段记录讲次、页码或无法解析的其他来源
- 手动点击保存材料选择或完成节点

PDF、网页正文和视频字幕保存在本机运行数据目录中。系统在导入或刷新时解析一次，保留带页码、网页位置或字幕时间段的完整提取文本和历史版本。每条每日学习记录锁定当时使用的材料内容版本；链接刷新后不改写过去记录的版本，使历史回顾仍能读取当时实际学习的内容。

材料解析失败时仍保留材料名称、原始 PDF 路径或 URL、作用域和错误原因，并在材料库提供“重新解析”。重新解析只重试同一来源，不自动切换到其他网页、字幕或替代材料。首次解析失败时材料不可用于 AI；已有成功版本的材料刷新失败时继续保持旧活动版本可用，并单独显示本次刷新错误，只有下一次解析成功后才切换活动版本。网页解析需要识别页面内嵌逐字稿和时间章节，并在检测到逐字稿却未达到正文覆盖要求时明确失败，不得用页面 Overview 冒充完整正文。

系统在材料导入或刷新时完成本地全文解析、稳定分块和定位索引，并把当前记录选择的版本写入只读材料工作区；这个步骤不额外调用模型生成覆盖摘要。当前小节第一次调用需要材料的 Codex 业务节点时，提示词直接包含与任务相关的原文块，同时授权只读访问完整材料文件；模型需要扩大核对范围时直接读取原文件，不依赖有损本地摘要。回顾评阅使用同小节最近一次历史学习记录锁定的材料版本；重构检查、练习生成、批改、预习问题和笔记整理使用当前记录的材料版本。

首个业务节点成功后，其会话成为该材料 revision 的锚点；后续业务节点从锚点 fork 新会话，因此不共享不断增长的节点聊天历史，也不必重复注入已经建立的材料访问上下文。新增材料或扩大页码范围时继承现有锚点，并在下一节点提供新增材料定位和完整文件；替换、刷新、删除、停用材料或缩小范围时建立新 revision，避免旧内容残留。每次结果只保存模型实际声明并能匹配当前候选块的来源，包含材料 ID、块号、标题、页码、时间段或章节位置；可用材料清单不能冒充实际引用。手动笔记提示词不调用模型，而是直接内联当前选择范围的完整提取文本。

PDF 解析先读取原生文本，只对文字极少且含图像的页面调用可选的本机 Tesseract OCR。OCR 语言包和页级缓存保存在被忽略的运行目录；缺少组件、语言包或识别失败时保留材料与错误原因，允许同源重新解析，不自动替换来源。课程目录自动生成仍不属于当前第一版。

材料候选块始终使用本机 SQLite FTS 参与排序。用户可在设置中明确下载多语言嵌入模型并启用语义混合排序；向量和全文索引使用独立可重建的 SQLite sidecar，不成为业务事实来源，也不进入备份。模型缺失、关闭或运行失败时自动退回 FTS 和既有匹配，不影响材料读取与学习流程。第一版不连接外部向量数据库或 RAG 服务。

### 6.3 主动重构

目的：合上材料后，用自己的语言重建知识。

节点只保留一个“我的重构”输入区，引导用户用自己的语言讲清这部分解决的问题、核心机制或概念关系，以及适用的关键定义、条件或推导，不再拆成三个并列空白字段。保存初始重构后，AI 基于当前材料和用户已经写出的内容生成恰好 3 个定向问题，分别优先检查逻辑链条、条件或边界，以及一个具体应用、推导步骤或方法比较；不适合数学推导的学科不能机械要求公式。

用户回答全部问题后，AI 再生成综合评阅，指出表达清楚的部分、概念或条件缺口、可能错误和进一步讲清楚的方法，不继续追问。AI 不可用时允许用户仅保存主动重构并明确确认完成。旧版记录中的“问题与目标”“核心概念”“清晰部分”和“定义与推导”等字段继续保留用于历史兼容，不自动删除或覆盖；新版页面以原“相关知识”和“主要内容”字段分别承载两个节点的单一自由回答。

### 6.4 练习与推导

题目由 AI 生成，用户在 Web 中作答。

规则：

- 默认一次生成 12 道题，允许后续配置在 10-15 道范围内调整；第一版固定为 12 道。
- 恰好 4 道选择题，约占三分之一；其余题目覆盖概念简答、计算/应用、推导/证明/分析、综合解答和思维延伸。不适合证明题的学科用分析或方法比较替代。
- 题目需要有基础、中等、挑战三档难度，并由浅入深。
- 一次生成结果仍保存为一次练习，但内部拆为有稳定题号的结构化单题；这不是跨课程题库。
- 每道题独立保存题干、类型、难度、选项、用户答案、隐藏参考答案、评分标准、材料定位和逐题反馈。
- Web 一次只展示一道题及其作答区，支持上一题/下一题和题号导航；第一题不显示“上一题”，最后一题不显示“下一题”，中间题显示两个操作，切换题目前自动保存当前修改。批改复核和内置示例遵循同一边界规则。
- 选择题直接点击选项；其他题型使用 Markdown 作答区，并可附加图片或 PDF 作为答案的一部分。每题最多 5 个附件、单文件最多 10 MB，附件入口使用不抢占主操作层级的紧凑按钮。后端校验真实文件签名；图片限制为 4000 万像素且单边不超过 20000 像素，PDF 最多 12 页，附件 OCR 使用较短的总时限且不写入材料 OCR 缓存。单附件最多保存 50000 个提取字符、单题最多 100000 个，截取时在界面明确提示。图片原文件应作为 Codex App Server 的 `localImage` 多模态输入并按题号关联，OCR 文字只作为辅助，不得因 OCR 为空或乱码把图片作答判为未作答；PDF 继续使用文本提取。所选 Codex 模型明确声明不支持图片时应在调用前给出可操作错误。界面应显示“原图直读”或“文本提取”的实际批改方式，并允许按需查看紧凑 OCR 摘要。原文件保存在本机，只有仍被数据库引用的附件进入完整备份。附件处理只锁定当前题的附件控件，不阻塞练习翻题、完成练习或其他模块。全部题目完成后提交整组，再调用 Codex 一次完成逐题批改。
- 题号导航使用固定尺寸的紧凑按钮，不随内容区宽度拉伸；选择题按选择控件、选项编号和选项正文三列左对齐，题干与选项使用一致的可读字号，选项中的 Markdown 与 LaTeX 公式直接渲染。
- AI 必须先检查当前材料中的例题、练习题和思考题，以及当前记录和历史记录已经生成的题干。具有相同已知条件、求解目标和主要解题路径的题，即使只改写措辞、变量或数值，也视为重复；围绕同一知识点重新出题时至少改变场景、数据或约束、已知量与未知量方向、表示方式、推理路径和输出目标中的 3 项。
- 题干必须给出明确对象、必要条件和作答目标。学习者背景只能用于选择一个已经具体化的场景，不能使用“任选一个”“自己选择一个”“结合你的背景举例”等方式把场景构造交给用户。纯开放设计题最多 1 道，并必须提供约束、评价标准和明确产出；计算和应用题必须提供足以作答的数据或假设。
- 后端在保存前检查同一组题目内部以及当前/历史练习中的明显近重复题，并拒绝空泛地让用户自行选择场景的题目。质量检查未通过时不得保存不合格练习，应明确提示重新生成或执行一次受限修复。

该节点可跳过，但点击跳过时需要确认。

### 6.5 批改与纠错

用户完成答案后，可以在 Web 内调用 Codex 批改并保存反馈。固定批改提示词仍可查看和复制，作为账号未连接或官方 CLI 不可用时的手动回退。

AI 批改反馈应要求固定格式，便于用户整理错题。但批改阶段不需要 AI 继续追问。

批改不计算整组总分或单题分数。Web 与作答区一样一次只审阅一道题，逐题标记“正确”“部分正确”或“错误”：正确题简要确认依据，部分正确和错误题明确指出具体问题并给出正确思路。需要修改答案时返回对应练习题；部分正确和错误题在当前复核题内提供“整理本题”入口，入口必须绑定当前题目，不能隐式回退到第一道错题或作答区当前题。

错题字段：

- 原题
- 正确作答
- 注意点
- 错误类型
- 状态：未解决、已理解

原题和正确作答由后端根据已批改的结构化练习题派生并保持只读，浏览器创建错题时只提交题目 ID、注意点和错误类型；原始作答不在错题编辑器或错题索引中展示。整理区按“原题 → 正确作答 → 注意点与紧凑错误类型”纵向组织，原题和正确作答使用同一可读宽度，错误类型不得拉伸成与长文本框同等高度。错题索引的摘要和展开详情都按 Markdown/LaTeX 渲染，展开后同样以完整原题、全宽正确作答和注意点为主要层级；无法关联结构化单题的历史记录明确标记为旧版。同页出现多个错题表单时，每个标题和 `aria-labelledby` 必须使用实例级唯一 DOM ID。旧版练习仍保留兼容读取能力，但不影响结构化练习的简化整理流程。同一练习题只能整理为一条错题，已有记录时显示已有状态，不再打开重复新建表单。

预设错误类型：

- 概念理解错误
- 公式适用条件错误
- 推导步骤错误
- 计算错误
- 题意理解错误
- 表达不清
- 不会做
- 其他

错题主要从 AI 练习和批改结果中整理，不强调独立新建。最好支持从批改反馈一键生成错题；如果第一版实现复杂，可以先做手动整理。

### 6.6 今日收尾

系统生成提示词，让 AI 基于当前小节、今日学习记录和课程长期研究方向，生成 3 条供下一次闭卷回顾使用的衔接问题。

下次回顾问题的交接规则：

- 保存在当前每日学习记录中。
- 生成后在当前记录中只读显示，不提供手工编辑框；需要修改时重新生成整组。
- 同一课程的下一条学习记录自动显示来源日期和小节，并把 3 个问题建立为闭卷回顾定向问题，不要求用户再次复制。
- “上一条”按 `(study_date, id)` 的严格时间顺序在已完成记录中确定，可以跨章节或小节；若最近记录没有生成问题，只显示缺失提示，不回退到更早记录。
- 每条问题使用一个紧凑段落，短公式使用行内公式，不在题内使用块级公式或额外题号；服务端保存前统一规范化并拒绝仍含块级公式的结果。
- 用户点击“今日完成”时先自动保存仍挂载的修改，再结束当天记录；已关闭表单的浏览器草稿继续保留，但不得留下无法定位的幽灵阻塞状态。当天完成不等于小节完成。

### 6.7 小节完成与笔记

小节笔记是学习后整理成果，不是学习前材料本身。

该流程只在用户判断当前小节已经学完时使用，不要求每天执行。完成当天学习后，页面提供“整理笔记并完成小节”入口；如果仍需继续，下一次进入同一小节会创建新的每日记录。用户可以让 GPT 整理、Gemini 润色，也可以直接在 Web 笔记区编辑，最终保存到 Obsidian。

规则：

- 一个小节对应一个 Markdown 文件。
- 笔记可在 Web 中编辑。
- GPT 在编辑器为空时生成初稿；编辑器已有内容时必须把原文作为不可遗漏的基础进行补充和修订。生成依据只包括当前小节完整材料、用户主动重构内容和编辑器已有笔记，不把练习、错题、批改、回顾评阅或预习问题写入知识笔记。笔记需要详细覆盖原知识内容，并在必要处补充逻辑连接、前置解释、宏观框架、背后思想、方法由来、适用边界和明确标识的可靠拓展。结果先在站内大尺寸预览框中展示，用户确认后才应用到编辑器，不得自动覆盖。
- Gemini 只接收当前完整 Markdown 笔记和必要的小节定位，既进行基于原内容的中文语言润色和逻辑整理，也进行 Obsidian Markdown 结构、公式、代码块和可读性优化；不得删除独有知识或擅自加入新知识。用户明确点击润色后，结果可以替换编辑器当前内容，但仍需用户保存到 Obsidian。
- 笔记不强制固定文档结构，但 AI 生成和润色内容统一使用标准 Markdown，并以 `$...$` 和 `$$...$$` 书写行内、块级公式。
- Web 预览兼容既有的 `\(...\)` 和 `\[...\]` 公式。发现旧格式时只提示并提供显式转换，不在打开或保存用户原稿时静默改写。
- 模型生成或润色的笔记在应用到编辑器前执行确定性的 Markdown 检查，至少覆盖未闭合代码围栏、块公式和 LaTeX 环境。存在问题时使用站内对话框列出位置，用户可以继续完成修改，或明确采用规范化后的结果；不因检查失败静默丢弃模型输出。
- GPT 笔记初稿或修订使用服务端运行记录在后台执行。页面保存运行 ID 并轮询真实状态；离开、刷新或重新进入笔记页后继续观察同一任务，结果完成后仍先进入预览，不得自动覆盖编辑器。服务重启造成中断时显示可重试错误。
- Web 预览支持 Obsidian Callout 和模型输出中的 `<br>` 换行，使站内确认结果与 Obsidian 阅读模式尽量一致；不改写用户未保存的原始 Markdown。
- Web 预览使用完整 GFM 解析，支持表格、列对齐、删除线、自动链接和任务列表。表格采用紧凑表头、单元格边界和交替行底色；超出预览宽度时只在表格容器内横向滚动，不得撑宽笔记页面。表格单元格中的 Markdown 与 LaTeX 继续正常渲染。
- 笔记保存到用户配置的 Obsidian vault 下。

### 6.8 完成状态

每日学习记录需要有“今日完成”按钮。

完成感来自：

- 节点打勾
- 节点进度条
- 轻量完成动画

每日记录完成不等于小节完成。保存小节笔记并点击“保存并完成小节”后，系统才将小节状态更新为已完成；不再更新某个每日记录中的“笔记节点”。

## 7. Obsidian 集成

### 7.1 基本定位

Obsidian 是结构化小节笔记的主来源。Web 是编辑器、索引器和流程层。

也就是说：

- 结构化笔记最终保存为 Obsidian vault 中的 `.md` 文件。
- Web 可以读取这些 `.md`。
- Web 可以编辑并保存回这些 `.md`。
- Web 只管理本工具生成或配置的课程文件夹，不索引整个 vault。

### 7.2 路径规则

用户需要整理小节笔记时，在设置中填写 Obsidian vault 路径；之后可以随时修改。首次进入应用不强制配置 Vault。

小节笔记路径规则：

```text
Obsidian Vault/
  安全课程目录/
    安全章节目录/
      安全小节文件名.md
```

Web 中的课程、章节和小节标题保持用户原始输入，不要求用户为 Windows 文件名规则修改标题。创建小节时，后端一次性分配并持久化对应的三层相对路径：普通标题保持可读名称；Windows 不允许的字符转换为对应全角字符，控制字符、尾随空格或点、保留设备名和超长名称统一安全化；发生转换、截断或同名冲突时追加稳定对象 ID 后缀。路径在后续重命名课程、章节或小节时保持不变，避免原笔记失联或被静默移动。

所有已分配路径必须位于配置的 Vault 内且全局唯一。Windows 端写入需要兼容深层 Vault 和长路径；路径无法安全解析时必须显示包含课程、章节和小节上下文的索引问题，不得要求用户修改本地课程标题，也不得静默跳过。

从旧版 `课程名/小节名.md` 升级时，Web 可以读取旧文件，并在用户下一次保存时迁移到章节目录。若新旧文件同时存在，或旧路径对应多个跨章节同名小节，必须提示冲突，不得静默覆盖或猜测归属。

### 7.3 同步规则

第一版采用简单同步策略：

- Obsidian Markdown 文件是主数据源。
- Web 打开小节笔记时读取对应 Markdown 文件。
- Web 保存笔记时写回对应 Markdown 文件。
- 如果文件不存在，保存时创建。
- 如果文件在 Web 打开后被外部修改，保存前应检测修改时间，并提示用户确认覆盖。

第一版不做复杂双向冲突合并。

## 8. AI 协作设计

### 8.1 第一版 AI 边界

第一版不接模型 API，也不自动控制 ChatGPT 或 Gemini 网页。应用通过本机官方工具完成内置协作：

- Codex 主流程使用 Codex CLI App Server，并通过官方 ChatGPT OAuth 登录。
- Codex 默认使用当前 ChatGPT 账号提供的 GPT-5.5 通用模型和 `Medium`。设置页从 Codex App Server 的实时模型列表中选择模型，并只展示该模型实际支持的思考强度；首选项不可用时明确提示用户更新 CLI 或检查账号权限，不静默回退。
- Gemini 只用于小节笔记润色，通过官方 Antigravity CLI 登录 Google 账号，默认使用 `Gemini 3.5 Flash (High)`。设置页从 Antigravity 的实时模型列表中选择 Gemini 模型及其可用思考强度；账号已登录和首选项是否可用必须分开显示，不得误报为未登录或静默降级。
- Codex 和 Antigravity 都从 Web 设置页发起连接；Antigravity 首次登录会打开其官方交互窗口，后端在窗口仍运行时检查官方 `models` 命令，确认登录后自动结束登录窗口。Web 使用站内进度框显示当前阶段并允许取消，不要求用户手动退出 CLI。
- 设置页顶部只给出简短的协作说明；账号、实时连接状态、模型和思考强度分别放在对应提供方下展示。模型和思考强度保存为本机首选项，默认值为 Codex `GPT-5.5` / `Medium`、Gemini `Gemini 3.5 Flash` / `High`；切换 Codex 模型后必须使用该模型对应的材料基础会话，不存在时建立新基座，不得复用其他模型的会话锚点。已经开始的生成任务继续使用其启动时快照。Codex 的“最近使用”只表示上一次成功任务的实际模型，不得用缓存结果冒充当前模型可用性；Antigravity 可以从官方 CLI 同一次状态检查的诊断输出中尽力识别账号，但不得读取或复制 OAuth Token。两个提供方都允许从本工具断开；Antigravity 的断开只禁用本工具调用并结束当前子进程，不删除官方 Google 登录凭据，重新连接时可复用仍有效的官方登录。
- Codex App Server 使用当前 CLI 支持的 `:read-only` permission profile，并显式传入运行工作区；客户端初始化时声明 `experimentalApi` 能力，以使用 `runtimeWorkspaceRoots` 管理本次任务授权的材料目录。不得继续提交已废弃的 `readOnlyAccess` 或旧 sandbox policy 字段，能力协商错误需要转换为可操作的中文提示。
- 本地 Web 服务必须由普通用户终端或资源管理器中的项目启动器运行，不得从 Codex 沙箱等受限自动化环境长期启动；外部 CLI 启动权限不足时，应明确引导用户改用本机启动器，不要求扩大目录 ACL 或以管理员身份运行。
- 项目不保存 API Key，不复制浏览器 Cookie，不读取 ChatGPT/Gemini 网页会话。
- Codex 默认允许网络访问，并保持本地材料和项目目录只读。它可以使用可靠通用知识或网络资料解释和拓展，但必须区分材料事实与补充内容，不得虚构来源。
- 每个小节建立独立材料 revision 和只读工作区；首个材料业务节点建立会话锚点，后续节点从该锚点 fork，不复用其他小节或其他节点不断增长的回答历史。
- 提示词查看、复制和手动粘贴保留为故障回退。

AI 的角色：

- 在用户完成自由回忆后生成定向问题，并综合评阅回答
- 在用户完成初始重构后生成定向问题，并综合检查回答
- 根据内容出题
- 批改题目答案
- 生成下次回顾问题
- 生成小节笔记初稿
- 将小节学习成果压缩为可复用的学习记忆

AI 不负责替用户先总结，也不应该把主动学习外包出去。

### 8.2 提示词生成上下文

所有 Codex 任务使用同一个结构化上下文包，并明确包含：

- 课程名、章节标题、小节标题和课程长期研究方向
- 用户在设置中维护的个人学习背景
- 已完成课程的 AI 学习成果摘要；只有用户在所有小节完成后显式点击“完成课程”才加入，后续修改课程结构或继续已完成小节会使旧完成状态失效
- 用户维护的课程记忆、自动课程脉络、当前章节记忆和当前小节记忆
- 同章中位于当前小节之前、状态为已完成的最近 3 个小节摘要；必须显示真实小节标题，不显示数据库 ID，不读取后续或未完成小节
- 最近 2 次每日学习摘要
- 同一课程上一条已完成学习记录的 3 条回顾问题及其真实日期、小节；下一次学习的 Codex 任务必须自动读取并建立定向问题，不能只显示在页面
- 当前每日流程中已经产生的结构化节点交接
- 当前小节材料基座的 revision、完整材料清单和来源位置；回顾任务使用最近一次历史记录锁定的版本，其余材料节点使用当前记录的版本
- 当前任务必须使用的用户输入，例如本次回顾、重构、题目、答案或编辑器中的已有笔记

材料 revision 已有锚点时，每个业务节点从锚点 fork 新会话；尚无锚点时，首个业务节点在相关原文块和完整文件访问权的基础上直接建立锚点。模型输出除用户可见的 `display_markdown` 外，还应同时产生结构化 `handoff`，至少包含已确认内容、纠正、核心概念、关键公式、未解决问题、错误模式和来源引用。下游节点读取精简交接，而不是重复注入上游完整回复；用户当前题目和答案等直接任务输入保持完整。

GPT 负责评阅、出题、批改、预习问题、笔记初稿或修订和学习记忆整理。笔记任务与练习、错题、批改、回顾评阅和预习问题隔离，只读取当前小节完整材料、用户主动重构和编辑器已有笔记。手动笔记提示词使用同一任务正文和通用上下文，并内联完整材料文本作为无需账号连接的回退。Gemini 只接收当前 Markdown 笔记和必要的小节信息，进行内容语言润色与 Obsidian 格式优化，不读取材料原文，不参与其他学习节点。

学习记忆分为课程、章节和小节三层。课程记忆中的个人课程概览、核心概念、关键方法、未解决问题和常见错误由用户维护；自动整理只能写入独立的“自动课程脉络”，不得覆盖用户编辑内容。章节与小节记忆包含摘要及同类字段。完成每日学习时使用一次 Codex 调用，同时生成供下一次学习读取的紧凑每日摘要，并更新小节记忆与章节记忆；随后由本地逻辑重建自动课程脉络，不再使用字段拼接生成本地摘要，也不为记忆更新增加第二次模型调用。每日摘要区域只显示模型结果中的“今日学习摘要”标题，不再叠加“今日摘要”等外层可见标签。手动重新整理小节记忆仍作为修复和回退入口。

小节记忆整理来源包括全部每日摘要、用户填写字段、全部练习题与答案、批改、错题、评阅反馈、节点交接、预习问题和最终 Obsidian 笔记。不得再限制为最近 5 天或只读取最近一组练习。

材料正文是外部参考，不是系统指令。加入 AI 上下文时必须明确标记为不可信引用，禁止执行材料中出现的命令或提示词。

课程学习完成后，Codex 根据课程、章节和小节记忆生成一份紧凑的课程学习成果摘要。该摘要与用户学习背景一起进入以后课程的通用上下文；它描述已经形成的知识和能力边界，不把课程目录或材料内容冒充为用户已掌握内容。

### 8.3 提示词和反馈保存

系统需要保存：

- 生成过的提示词
- 用户粘贴回来的 AI 反馈
- 每次内置 AI 运行的提供方、任务、提示词、上下文快照、输出、状态和错误
- 每次内置 AI 运行的结构化节点交接
- 课程级、章节级与小节级学习记忆
- AI 生成的每日学习摘要
- 每次任务实际使用的材料版本和来源定位

这些内容应关联到对应每日学习记录。

生成成功以结果实际写入对应编辑区为主要反馈，不显示“提示词已生成”横幅；保存、复制、生成完成和错误可使用紧凑的短暂状态提示。提示词默认折叠，需要时可以查看和复制。

生成状态、成功结果和错误提示应紧邻发起操作显示，并且只改变当前操作按钮的状态，避免页面顶部的全局提示与实际任务脱节。不同 AI 任务可以在界面侧同时等待后端执行；某一模块生成或评阅时不得禁用其他模块、练习翻题或完成按钮，同一任务自身必须防止重复提交。成功提示约 3 秒后自动消失，错误提示持续保留。账号登录失效时，当前任务位置必须显示明确错误和“前往设置”入口；提供方状态随本次失败同步为未连接，用户重新连接后再恢复。

长时间任务由服务端 AI 运行记录作为状态来源。页面显示“准备材料”“等待 Codex/Gemini”等真实阶段、加载动画和已用时间；离开后返回页面可以恢复当前任务，而不是重新发起模型调用。服务重启时仍处于运行中的任务标记为已中断并提示从原操作重新生成，不展示无法验证的百分比。

模型连接状态至少区分未安装、外部程序启动受限、等待登录、已连接、已连接但首选模型不可用和其他错误。账号认证成功不等于首选模型可用，两者不能共用一个布尔状态覆盖。

AI 生成的评阅、练习、批改和笔记内容默认使用 Markdown 阅读模式呈现，标题、列表和公式应直接渲染；用户需要修改时再显式切换到编辑模式。生成期间显示当前任务、加载动画和已用时间，不使用虚假百分比，也不要求逐字流式输出，以保持界面稳定和渲染流畅。

来源依据区分“当前任务可用材料”和“模型实际引用”。用户首先看到可用材料数和实际引用数，展开后查看模型声明的材料标题与页码、时间段或网页位置；内容哈希、revision 和清单哈希只放在次级技术详情中，不能冒充实际引用。小节笔记的可见正文不输出材料引用或来源清单，练习题与批改题卡也不显示逐题“依据”小字；两类数据仍在结构化交接、数据库和统一来源依据面板中保留，供后续流程与溯源使用。

后端在保存模型输出前移除包裹全文的 Markdown 代码围栏，并将旧式公式定界符规范化为 `$...$` 或 `$$...$$`。兼容规则必须覆盖独立公式块、同一行公式、列表项中由 `\[` 和 `\]` 包裹的公式，以及 JSON/字符串转义把 `\omega`、`\binom`、`\frac`、`\cdots`、`\times` 等命令损坏为控制字符的情况，不能把定界符、控制字符或 Markdown 标题作为普通红字显示。后端生成链与前端历史内容展示链都执行兼容；已确认的历史数据库损坏通过迁移做确定性修复，不能安全识别的控制字符必须报告而非静默展示。

### 8.4 模板编辑

第一版先内置固定模板。后续可以支持用户编辑模板。

界面上可以先预留“提示词模板”设置入口，但 MVP 不必实现复杂模板管理。

## 9. 练习、错题与统计

### 9.1 练习

练习由 AI 出题结果产生。

第一版练习保存粒度：

- 一次 AI 出题结果保存为一次练习。
- 一次练习内部拆成 12 个结构化单题及其一一对应的作答；不把这些单题提升为跨记录复用的独立题库。

练习包含：

- 所属课程、章节、小节、每日学习记录
- 出题提示词
- AI 出题结果
- 单题类型、难度、选项、材料定位和隐藏评分依据
- 每题用户答案与提交状态
- 批改提示词
- 整组批改摘要与逐题反馈

### 9.2 错题

错题从练习批改结果中整理。

错题应关联到：

- 课程
- 章节
- 小节
- 每日学习记录
- 对应练习
- 对应结构化练习题

结构化错题必须在练习完成批改后创建，题目与正确作答由服务端保存，用户只维护注意点、错误类型和解决状态。错题索引以“题目、正确作答、注意点”为核心，不显示原始作答或独立的原因分析输入框。

错题状态：

- 未解决
- 已理解

### 9.3 错误统计

错误统计优先级：

1. 小节级统计
2. 章节级统计
3. 课程级统计

统计维度：

- 错误类型分布
- 未解决错题数量
- 已理解错题数量

第一版不做复杂图表，简单列表或基础统计即可。

## 10. 结构化笔记

### 10.1 笔记定位

结构化笔记是第一版最重要的沉淀对象，比问题池和错题更重要。

笔记不是传统“卡片”。第一版不强调卡片系统，而是强调：

- 小节结构化笔记
- 可搜索
- 可编辑
- 可保存到 Obsidian
- 可与学习记录、练习和错题关联

### 10.2 笔记来源

用户常见流程：

1. 将课程内容、课件或材料发给 ChatGPT，先梳理出一份笔记。
2. 再用 Gemini 润色。
3. 将最终笔记粘贴回 Web。
4. Web 保存为小节 Markdown 文件。

当前第一版已支持在 Web 内导入并解析 PDF、网页和公开视频字幕，由 Codex 基于完整材料生成笔记初稿，再由 Gemini 可选润色；手动提示词和粘贴仍作为连接不可用时的回退。

### 10.3 笔记编辑方式

第一版不要求笔记遵循固定文档结构，也不根据 Markdown 标题生成单独的文档结构栏。

规则：

- 笔记主体是一个可自由编辑的 Markdown 文档。
- 系统可以提供“生成整理提示词”，帮助用户在外部 AI 中整理笔记，但不强制输出固定标题。
- 用户粘贴回来的内容可以直接覆盖当前笔记。
- 第一版不做标题检查、目录检查或格式校验。
- 笔记页不展示独立的文档目录、标题导航或大纲侧栏。

## 11. 页面设计

### 11.1 首页

首页应该是“今天继续哪里”的工作台，不是复杂资料库入口。

首页展示：

- 当前课程
- 今天该继续的学习记录
- 学习节点进度条
- 开始/继续入口
- 课程列表入口
- 设置入口

首页不展示最近问题，避免干扰。

### 11.2 课程列表页

功能：

- 查看所有课程
- 创建课程
- 进入课程
- 显示课程下小节完成概况
- 按课程名称、描述和学习目标进行列表内关键词搜索
- 将内置只读示例作为特殊课程行展示，并允许经两次确认从本机隐藏

课程创建入口与搜索放在课程列表工具栏中，避免在宽屏下与课程列表主体相距过远。完成概况使用真实的小节总数、已完成数、进行中数和轻量进度条，不增加无业务含义的统计卡片。真实课程按三组稳定排序：进行中课程按最近学习时间倒序，未开始课程按创建时间倒序，已完成课程按完成时间倒序并位于末尾；示例位于未完成真实课程之后、已完成真实课程之前，不参与真实课程数量和进度。

全新数据库首次安装后，桌面启动器仍直接打开课程列表页，并在页面上显示一次不可因误点遮罩关闭的简洁欢迎弹窗。弹窗只包含 Lumina 标识、简短说明和“开始使用”按钮；点击后记录已进入状态并关闭弹窗，不要求预先配置模型、学习者背景或 Obsidian。未点击便关闭应用时，下次启动继续显示；修复、升级和保留数据重装不重复显示。设置页不承担首次欢迎或“完成设置”流程。

### 11.3 课程详情页

功能：

- 查看课程目标和长期研究方向
- 查看章节列表
- 查看小节列表
- 创建章节
- 创建小节
- 进入未完成或进行中的小节

新增表单默认不占用课程阅读区。章节入口位于“章节与小节”标题行；小节入口位于对应章节操作区，均在用户明确点击后就地展开并自动聚焦。空课程或空章节可以显示与当前层级一致的新增行动入口。取消后立即收起；存在可恢复的本地草稿时自动展开对应表单，并继续纳入统一未保存保护。

课程详情中的每日学习记录属于辅助历史信息，不逐条横向铺在小节下。每个有记录的小节默认只显示一行紧凑摘要，包含记录次数、最近日期和最近一次状态；点击后在当前小节内纵向显示最近 3 条，并可显式查看全部。同一时间只展开一个小节，展开状态只属于当前页面实例：离开课程详情再返回时恢复默认收起，避免每次回到课程页都展开冗长历史。未完成小节已有今日记录时主操作显示“继续学习”，否则显示“开始学习”；已完成小节统一显示“再次学习”，确认说明必须明确既有学习记录和笔记都会保留，不能暗示重置或清空。历史区域只负责回看，不承担当前学习入口。

### 11.4 小节详情页

小节详情页是核心页面之一。

应包含：

- 小节标题
- 小节状态：未完成、进行中、已完成
- 今日学习记录入口
- 历史每日学习记录列表
- 练习列表
- 错题列表
- 小节笔记入口
- 手动标记小节完成按钮

如果小节已完成，点击继续学习时应提示确认。

### 11.5 每日学习记录页

这是最核心的执行页面。

包含固定节点：

- 闭卷回顾
- 材料学习
- 主动重构
- 练习与推导
- 批改与纠错
- 今日收尾

页面体验要求：

- 不要过于密集。
- 有节点进度条。
- 节点完成后有轻量动画。
- 所有流程节点都可以手动展开或收起；已完成和已跳过节点默认收起，打开记录时默认展开顺序最前的未完成节点。
- 用户明确完成或跳过普通输入节点后，当前节点自动收起并展开下一个未完成节点；用户仍可重新展开已完成节点查看和编辑既有内容。
- AI 生成评阅、重构检查或逐题批改后，结果所在节点保持展开，先供用户阅读；结构化批改由用户点击“完成批改与纠错”后再收起并进入今日收尾。点击“今日完成”生成摘要后，今日收尾在当前页面保持展开，确保摘要与小节完成入口可见；下次重新打开记录时仍按已完成节点默认收起。
- 每个节点字段尽量少。
- 节点和字段使用“简短名称 + 一句用途说明”的两级文案，避免直接把完整问题句当作标题。
- 学习流程中的操作按任务命名为“评阅回顾”“生成练习”“批改答案”“生成下次回顾问题”等，不在每个按钮前重复“AI”；提供方名称只在连接设置、任务运行反馈和确实需要区分模型时显示。
- 允许跳过练习与推导节点，但需要确认。
- 站内路由切换时保留当前页面，直到目标页面数据读取完成；快速切换不显示中间骨架页或“不存在”错误，超过短暂延迟时只显示顶部细进度线。真实 404 仍在目标页明确显示。
- 用户通过站内链接进入新的课程、小节或每日记录路由时，目标页面应从顶部开始；浏览器前进/后退继续交给浏览器恢复原滚动位置，带锚点的导航不强制覆盖锚点定位。
- 用户修改学习内容但尚未保存时，离开当前页面必须使用站内对话框确认，并提供“继续编辑”“不保存离开”“保存并离开”三个明确选择；保存后离开只保存内容，不自动完成流程节点。保存失败时停留在当前页面并显示错误。

### 11.6 笔记页或笔记节点

笔记可以从每日记录末尾的“小节完成”入口打开，也可以从独立笔记库打开。两种入口编辑同一个小节 Markdown 文件，但导航和流程职责必须分离。

- 从每日学习记录进入时属于小节完成流程：返回每日记录，可以生成整理提示词，并可保存后完成当前小节。
- 从独立笔记库进入时属于笔记工作区：返回笔记列表，只负责查看、编辑和保存，不生成或完成任何每日学习节点。
- 独立笔记路由按小节标识，不依赖该小节是否已经存在每日学习记录。

功能：

- 显示当前小节 Markdown 笔记
- 编辑笔记
- 保存到 Obsidian
- 读取 Obsidian 文件内容

布局要求：

- 不设置独立的“文档结构”栏。
- 桌面端以 Markdown 编辑器和实时预览双栏为主，工作区占用当前视口的剩余高度，编辑器和预览分别独立滚动。
- 窄屏下使用“编辑/预览”分段切换，同一时刻只显示一个面板，避免双栏挤压和页面内多重长滚动。
- 编辑器是主要工作区，第一版不需要复杂布局切换。
- 顶部显示文件名、Obsidian 相对路径、保存状态和“保存到 Obsidian”操作。
- 学习流程入口提供“GPT 生成初稿”和“Gemini 润色”；手动整理提示词作为折叠回退入口，不使用聊天界面。
- 笔记内容发生修改但尚未保存时，离开页面采用与每日学习记录一致的三选项保护；选择保存后离开时仍需执行 Obsidian 外部修改冲突检测，存在冲突则不得直接离开。
- 笔记列表的搜索词和课程筛选保存在 URL 查询参数中，从编辑器返回后恢复原筛选范围。

### 11.7 错题统计页

第一版可以简单实现：

- 按小节查看错题
- 按章节汇总错题
- 按课程汇总错题
- 按错误类型筛选
- 按未解决/已理解筛选

### 11.8 材料库弹窗与设置页

材料库不占用侧栏导航。设置页的“打开材料库”按钮打开站内大尺寸弹窗，弹窗包含关键词、课程、来源类型和处理状态筛选，以及跨课程的添加、刷新、删除和归属调整；关闭后仍停留在设置页。每日记录和课程详情中的材料入口继续使用固定作用域，避免重复选择层级。

设置页正文只保留材料库入口和本机配置，不直接展开完整材料列表；材料管理弹窗不可因误点遮罩关闭。

模型和思考强度在桌面端组成紧凑、左对齐的控制条，宽度由实际内容和可读性决定，不随 2K/4K 工作区持续拉伸；恢复默认和应用操作紧邻选择控件。中等宽度允许操作换到下一行，窄屏再改为全宽纵向排列，不能为了桌面端紧凑而造成文字截断或横向溢出。

设置页包含低频功能：

- Codex 和 Antigravity 官方账号连接状态与登录入口
- 材料库弹窗入口
- Obsidian vault 路径
- 课程笔记保存目录配置
- 完整备份下载与备份导入恢复
- Markdown 导出
- 提示词模板查看或后续编辑入口

### 11.9 视觉与排版规范

整体视觉定位是简洁、安静、轻量的学习工作台。可以参考 NotebookLM 的信息组织感和纸张感，但不能直接复制其品牌、组件或页面布局。

界面基调：

- 使用暖白背景和接近纸张的浅色表面，避免纯白造成的生硬感。
- 主文字使用深墨色，主操作使用低饱和陶土橙，完成状态使用鼠尾草绿。
- 页面不要做成营销型 SaaS，不使用大幅 Hero、夸张标语或装饰性图表。
- 不堆叠卡片，不使用卡片套卡片；边框主要用于明确真实的工作区域。
- 圆角以 `6px` 到 `8px` 为主，不使用大量胶囊形控件。
- 阴影应极轻或不用，主要依靠边框、留白和背景层级区分区域。
- 可以在输入区或笔记区使用非常轻的纸张纹理，但不能影响文字清晰度。

建议基础色：

| 用途 | 色值 |
| --- | --- |
| 页面背景 | `#F8F5EF` |
| 主要表面 | `#FFFCF7` |
| 次级表面 | `#EFE8DD` |
| 主文字 | `#20201C` |
| 次级文字 | `#6E685F` |
| 主操作/当前节点 | `#C96F45` |
| 完成状态 | `#6F8C7A` |
| 分隔线/边框 | `#DDD3C4` |

字体要求：

- 中文和英文都使用常见、清晰、略带圆润感的无衬线字体。
- 中文使用自托管的 `Noto Sans SC Variable`，英文和数字优先使用自托管的 `Nunito Sans Variable`，两者保持清晰、柔和的统一气质。
- 回退字体栈为 `"Nunito Sans Variable", "Noto Sans SC Variable", "Noto Sans SC", "PingFang SC", "Microsoft YaHei", sans-serif`。
- 字体通过采用 OFL-1.1 许可的 npm 包随前端构建，不依赖在线 CDN 或用户电脑预装字体。
- 不使用过于正式的衬线字体，也不使用花体、手写体或过度圆润的展示字体。
- Markdown 源码编辑区可以单独使用 `"Cascadia Code", "JetBrains Mono", monospace`，其他界面不使用等宽字体。
- 字重以 `400`、`500`、`600` 为主；`700` 只用于少量页面主标题。
- 字间距统一为 `0`，不随视口宽度缩放字号。

建议字号：

| 场景 | 字号 | 行高 |
| --- | --- | --- |
| 页面主标题 | `28px` 到 `32px` | `1.25` |
| 区域标题 | `20px` 到 `24px` | `1.35` |
| 模块标题 | `16px` 到 `18px` | `1.4` |
| 正文和输入内容 | `15px` 到 `16px` | `1.6` |
| 导航、按钮和表格 | `14px` 到 `15px` | `1.45` |
| 标签和辅助信息 | `13px` 到 `14px` | `1.45` |

设置中的小/标准/大三档必须形成清晰可辨且以标准档为中心的对称差异。当前正文基准为 `10px / 14px / 18px`，页面主标题为 `27px / 31px / 35px`，Markdown 编辑器为 `10px / 14px / 18px`；极小辅助文字保持 `10px` 可读下限，分段选项比同档正文小 `1px` 且不低于 `9px`。紧凑面板中的标题不得使用页面级大字号。

顶层课程、错题、笔记和设置页面在常规桌面保持统一的 `1080px` 最大工作区；逻辑视口达到 `2200px` 时扩展为 `1320px`，达到 `3200px` 时扩展为 `1520px`。每日学习流程继续使用更集中的阅读宽度，避免表单行在超宽屏上过度拉长。

桌面展开侧栏默认宽度和最大宽度均为 `236px`，用户可拖拽到最小 `144px`，也可在获得焦点后使用方向键、Home 和 End 调整；宽度保存在当前浏览器本地。宽度不大于 `168px` 时收紧品牌、导航和侧栏留白，确保最小宽度下内容仍完整显示。折叠态继续固定为 `64px`，不受展开宽度影响。拖拽期间即使指针离开窄拖拽条也必须继续响应并在结束时保存；移动端使用顶部导航，不显示拖拽条。

交互视觉：

- 当前流程节点使用陶土橙描边或侧边线突出。
- 已完成节点使用鼠尾草绿勾选状态。
- 批改结论统一使用语义色：正确为鼠尾草绿，部分正确为琥珀色，错误为砖红色；题号导航、逐题反馈和结论标签使用同一组颜色。当前题使用独立陶土橙外圈表示位置，不得覆盖原有批改结论色，也不能只依赖颜色传达结论。
- 节点完成时使用短暂、克制的勾选和进度推进动画。
- 主按钮使用陶土橙实底，次级操作使用描边或文字按钮。同一操作组中的主、次按钮必须使用一致的高度、内边距和字号；并列按钮等宽，文字和图标保持单行，空间不足时整体换行或改为纵向全宽排列，不能让单个按钮内部断行。
- 图标优先使用常见线性图标，并提供悬停提示；不要用文字胶囊替代熟悉图标。
- 所有可点击按钮、链接、折叠标题、课程条目和选择控件都应显示指针光标，并提供克制且一致的悬停、按下和键盘焦点反馈；按下反馈不得引发布局位移。
- 动效使用短时长和柔和平滑缓动；开启“减少动效”后关闭位移和非必要过渡，但保留颜色、边框和焦点等必要状态提示。
- 草稿恢复使用靠近相关内容的紧凑中性状态行，不使用贯穿主区域的成功色横幅；恢复文案短暂显示后淡出，未保存数量继续以陶土橙常驻，直到保存或放弃。保存、复制等成功反馈使用内容宽度的短暂鼠尾草绿提示，错误反馈仍使用醒目的整行错误提示。

### 11.10 导航与离开保护

- 用户从课程详情、每日学习记录或学习流程内的笔记节点切换到“设置”后，第一次点击侧栏“课程”应恢复当前浏览器会话中最后访问的课程工作区位置；恢复后或本来就在上述课程工作区页面时，再点击“课程”返回全部课程列表。位于全部课程列表时保持当前页面。该行为按当前位置逐级导航，不使用双击计时。
- 独立笔记列表和独立笔记编辑器始终归属侧栏“笔记”，不得覆盖最近课程位置；独立编辑器返回笔记列表，学习流程内的笔记编辑器返回对应每日学习记录。
- 站内未保存保护使用应用内居中对话框，不使用浏览器原生确认框；继续编辑时必须保留当前输入。每日学习记录、小节笔记、创建/编辑课程、修改章节/小节标题、课程详情中的新增章节/小节草稿，以及设置中的手动 Vault 路径均纳入统一保护。
- 未保存对话框必须由用户明确选择“继续编辑”“不保存离开”或“保存并离开”；点击遮罩不得关闭，三个操作在同一布局方向上保持等宽，窄屏时改为纵向全宽排列。创建课程、编辑课程以及修改章节/小节等数据输入对话框同样不得因点击遮罩而丢失输入。
- 用户关闭或刷新整个 Web 页面时，仅在受保护页面确实存在未保存修改期间注册浏览器原生离开确认；保存或放弃修改后立即移除。提示文字和是否展示由浏览器安全策略决定，应用不能自定义原生文案，也不能保证浏览器展示。内容已保存时关闭或刷新不得产生额外提示。
- 所有受保护文本输入在未保存期间同时写入版本化浏览器本地草稿；刷新或浏览器未展示原生确认时，只在草稿记录的服务器基线仍与当前已保存内容一致时恢复，避免旧草稿覆盖较新数据。成功保存或明确放弃后立即清除草稿；本地草稿不进入业务数据库、Obsidian 文件或开源仓库。
- 完成前的未完成内容提示使用应用内对话框，点击遮罩不得关闭，默认聚焦“继续完成”；取消后保留输入并将焦点恢复到原完成按钮。用户确认继续完成时，必须先保存成功再更新节点状态。
- 删除课程、章节、小节、参考材料、旧版练习或错题等持久化对象时，必须经过两层站内确认：第一层说明影响范围，第二层再次明确永久删除对象。两层弹窗都不得因点击遮罩关闭；只有第二层的“确认删除”可以发出删除请求，且返回第一层或取消时不得改变数据。两层底部操作保持等宽，避免危险操作因按钮尺寸差异造成误点。

### 11.11 界面参考图

第一版界面实现以 `design-references/` 中的 6 张横向参考图作为整体风格和布局依据：

1. `01-home-today.png`：首页与今日继续。
2. `02-course-detail.png`：课程详情和章节小节列表。
3. `03-daily-learning-flow.png`：每日学习流程主工作区。
4. `04-ai-practice.png`：AI 出题、作答和反馈工作区。
5. `05-mistakes-statistics.png`：错题与错误类型统计。
6. `06-obsidian-note-editor.png`：Obsidian 笔记编辑器的色彩、编辑区和预览区参考。

参考图不是逐像素实现要求。功能规格优先于图片中的局部细节，尤其要遵守以下差异：

- `06-obsidian-note-editor.png` 左侧的“文档结构”栏不实现。
- 笔记页应改为 Markdown 编辑器和实时预览双栏，编辑器获得更多横向空间。
- 参考图中的示例课程名、日期、题目和数据仅用于展示，不作为预置数据；产品内置示例必须来自单独验收并经过脱敏的静态只读内容。

## 12. 备份、恢复与内容导出

“备份”用于完整迁移或灾难恢复，“Markdown 导出”用于阅读、分享和二次处理，二者在设置页中必须使用不同名称和信息层级。

完整备份使用带清单的 ZIP，包含 SQLite 业务数据库、原始本地材料、数据库仍引用的非选择题作答附件，以及数据库已登记且由 Lumina 管理的小节 Markdown 笔记；不递归打包整个 Obsidian Vault，也不扫描收录附件目录中的孤儿文件。登录状态、模型与 OCR 缓存、全文/向量 sidecar、日志、PID、设备级界面偏好和已有备份目录不进入归档。每个文件记录大小与 SHA-256，下载响应禁止缓存。导入前必须校验归档路径、Windows 大小写与 Unicode 归一化后的路径碰撞、成员数量、单文件和总展开大小、异常压缩比、清单、数据库完整性和外键；内容校验与恢复复制均采用分块读取，不把归档成员整体读入内存。

设置页先上传并预检备份，展示创建时间、文件数、大小和包含的数据类别，再允许确认恢复。取消预览时立即删除对应暂存包；服务启动和后续预检会清理超过 24 小时的暂存包及超过 7 天的恢复结果。包含笔记时，用户必须选择新机器上的目标 Obsidian Vault；恢复后数据库中的 Vault 路径改绑到新位置。若目标 Vault 中存在同路径但内容不同的笔记，恢复中止且不覆盖。桌面启动器在替换当前数据库、材料和作答附件前先创建安全备份，失败则回滚；成功或失败结果在服务重启后返回。父级课程、章节、小节或旧版练习被删除时，数据库级联后必须清理其物理附件；即使文件清理异常，后续备份也不得重新收录没有数据库引用的文件。旧版仅含数据库和材料的归档继续兼容。

### 12.1 Markdown 内容导出

导出不是主流程入口，但需要支持。

优先导出格式：

- 包含多份 Markdown 的 ZIP 文件

导出内容：

- 小节笔记
- 每日学习记录
- AI 评阅
- 练习与 AI 批改
- 错题记录
- 课程大纲

导出交互：

- 点击设置中的导出入口后先打开站内配置框，不直接开始下载。
- 支持选择一门或多门课程。
- 支持分别选择课程大纲、每日记录、AI 评阅、练习与批改、错题和小节笔记。
- 至少选择一门课程和一种内容后才能导出。
- ZIP 内按 `课程/章节/小节` 分层；每日记录和练习继续按日期拆分为独立 Markdown。
- 未保存的笔记不创建空文件；无法读取的笔记记录在根目录的 `导出说明.md` 中。
- Web 默认下载 ZIP，不依赖兼容性有限的浏览器目录写入能力。
- 设置入口统一使用“Markdown 导出”等用户任务文案，不在入口重复强调分层和多文件 ZIP 等实现细节。

后续可增加：

- JSON 全量导出
- CSV 错题统计导出

导出入口放在设置页，不在主界面显眼展示。

## 13. 开源与本地数据边界

开源仓库不应包含：

- 本地 SQLite 数据库
- Obsidian vault 路径
- 用户学习内容
- 用户导出的 Markdown
- 个人配置

仓库应包含：

- 前端代码
- 后端代码
- 数据库 schema 或 migration
- 默认提示词模板
- 空配置示例
- README

第一版不向用户数据库写入示例课程或演示数据。可以内置一份经过真实材料全流程验收、脱敏且有明确授权来源的静态只读示例，用于展示六步流程、练习批改和最终笔记；该示例在课程首页作为特殊课程行展示，可经两次确认写入本机隐藏偏好，但不得调用 API、模型或 Obsidian，不得进入课程统计、搜索和导出。

## 14. 技术形态

技术栈正式确定为轻量的本地前后端架构。

### 14.1 核心技术栈

- 前端：Vite 8 + React 19 + TypeScript。
- 后端：FastAPI + Python 3.12。
- 数据访问：SQLAlchemy 2，同步 Session 模式。
- 数据库迁移：Alembic。
- 数据库：SQLite。
- 本地文件：Python `pathlib`，由后端负责读取和写入 Obsidian Markdown 文件。
- 前端包管理：npm，提交 `package-lock.json`。
- 后端包管理：uv，使用 `pyproject.toml` 和 `uv.lock`。

版本基线：

- Node.js 最低使用 `22.12`，项目初始环境使用 Node.js 22 LTS。
- React 使用 `19.2.x` 稳定版本。
- Vite 使用 `8.1.x` 稳定版本。
- FastAPI 使用 `0.139.x`。
- SQLAlchemy 使用 `2.0.x`。
- Alembic 使用 `1.18.x`。
- 实际补丁版本由锁文件固定，不在代码中使用不受控的浮动版本。

### 14.2 前端约束

- 使用 React Router 的普通 SPA 路由，不使用带服务端渲染能力的全栈框架。
- 使用原生 `fetch` 访问后端 API，第一版不引入 Axios。
- 使用 CSS 自定义属性和普通 CSS/CSS Modules 实现设计系统，不引入 Tailwind 或大型 UI 组件库。
- 操作图标使用 `lucide-react`；产品品牌图标使用 Lumina 自有 PNG/ICO 资源。
- 第一版不引入 Redux、Zustand 等全局状态库；服务端数据以 API 和页面级状态为主，确有跨页面复杂状态后再评估。
- 完成动画优先使用 CSS transition/animation，不引入通用动画框架。
- Markdown 预览使用 `react-markdown`、`remark-math`、`remark-gfm` 和 `rehype-katex`；这些依赖仅随 Markdown 页面按需加载，不进入不需要预览的页面组件。

### 14.3 后端约束

- API 使用 FastAPI 和 Pydantic 数据模型。
- SQLite 使用同步 SQLAlchemy Session；本地单用户场景不引入异步数据库驱动。
- 使用 Alembic 管理 schema 变化，不能依赖运行时自动改表。
- Obsidian 路径和文件操作集中在后端服务层，并校验所有目标路径都位于用户配置的 vault 和本工具管理的课程目录中。
- 服务默认只监听 `127.0.0.1`。
- 所有修改本地数据的 HTTP 请求必须拒绝跨站和 `null` 浏览器来源；同源页面以及不携带浏览器来源头的本机启动器和测试客户端可以继续使用。来源校验是本地监听边界的补充，不能替代 `127.0.0.1` 绑定。
- 第一版不使用 Docker、任务队列、缓存服务或独立搜索服务。

### 14.4 运行方式

- 开发模式：Vite 开发服务器和 FastAPI 开发服务器分别运行，Vite 将 `/api` 代理到后端。
- 本地使用：前端构建为静态文件后，由 FastAPI 同时提供 API 和 SPA 静态资源，只启动一个本地服务。
- 生产静态托管中，`index.html` 和 SPA 深层路由回退必须使用 `no-store`，带内容哈希的 `/assets/` 文件使用长期不可变缓存；不存在的静态文件必须返回真实 `404`，不得以 `200 text/html` 回退到 SPA。健康接口同时报告当前前端构建标识、入口文件和完整性状态。
- Windows 普通使用：自包含安装包内置前端、后端和 Python 运行时，标准安装向导允许选择程序安装目录并可选创建桌面快捷方式；课程、材料、设置和日志独立保存在 `%LOCALAPPDATA%\Lumina`。无窗口启动器以普通用户权限运行 FastAPI，只有健康接口中的前端构建标识与本机 `index.html` 一致且入口完整时才打开浏览器，并在课程首页 URL 附加构建标识；重复启动复用一致的现有服务，设置页和开始菜单可以请求优雅停止。
- 懒加载页面遇到旧构建 chunk 已被替换时，前端最多自动刷新一次以取得当前 `index.html`；短时间内同一地址再次失败时停止自动刷新，并显示 Lumina 中文恢复页和手动重新加载入口。普通路由错误也使用该恢复页，不显示 React Router 默认英文错误界面。
- Windows 源码使用：根目录安装/修复脚本按锁文件同步依赖、构建前端并创建指向源码目录的当前用户快捷方式，运行数据继续位于被忽略的 `runtime-data/`。源码入口保留用于开发和诊断，不是普通用户安装包的前置条件。
- Windows 安装包使用 `install_Lumina-版本号.exe` 命名，Inno Setup 的内部卸载文件收纳在 `uninstall_Lumina` 子目录，用户入口仍显示“卸载 Lumina”。安装包卸载必须先根据服务 PID 等待 Lumina 进程完全退出；停止失败时中止卸载，不能在程序仍占用文件时继续形成半卸载状态。
- 安装包卸载默认只删除程序和快捷方式并保留学习数据；交互选择删除学习数据时需要二次确认，并且必须在最终备份成功后才能删除。清理范围只包含 Lumina 明确拥有的程序文件，不能递归清空用户选择的整个安装目录。源码型卸载必须先安全停止服务，并提供保留学习数据、清理生成环境和完整清理的明确模式；不删除项目源码，也永不删除用户的 Obsidian Vault。
- 安装升级和完整卸载前的归档与设置页完整备份使用同一归档实现：包含 SQLite 数据库、本地材料、作答附件和 Lumina 已登记的小节笔记，但不递归包含整个 Obsidian Vault；登录状态、日志、PID、备份目录和可重建缓存继续排除。归档需包含版本、文件大小和 SHA-256 清单；恢复前验证路径、清单、数据库完整性和外键，恢复替换数据库、材料和附件，并按明确选择写入受管笔记，失败时回滚原数据。
- 首次安装只为全新数据库在课程首页显示一次欢迎提示；点击“开始使用”即可进入，模型连接、学习者背景和 Obsidian 均按需在设置中配置。修复和升级不重复欢迎、不创建示例数据，也不重置已有设置。
- 根目录的 CMD 启动方式作为显示日志的诊断入口保留，不作为日常入口；不注册 Windows Service，也不随系统自动启动。
- SQLite 数据库、Obsidian 路径和个人配置必须保存在仓库之外或被 `.gitignore` 明确排除。

### 14.5 测试与质量工具

- 前端单元与组件测试：Vitest + React Testing Library。
- 后端测试：pytest + HTTPX/FastAPI TestClient。
- 关键完整流程：Playwright，仅覆盖少量高价值路径，不建立庞大的端到端测试集。
- 前端执行 TypeScript 类型检查和 ESLint。
- 后端使用 Ruff 做格式与静态检查。

这套架构保留了 React 的成熟组件生态和 Python 的文件/PDF/AI 扩展能力，同时避免 SSR、桌面壳、云服务和大型前端框架带来的额外复杂度。

## 15. MVP 必须实现

第一版必须实现：

- 创建课程
- 创建章节
- 创建小节
- 小节状态：未完成、进行中、已完成
- 进入小节时自动创建或打开今日学习记录
- 每日学习记录固定六节点
- 节点完成勾选和进度条
- 今日完成按钮
- 应用内生成 12 道结构化练习，并保存为一次练习
- 单题作答、题号导航和切题保存
- 应用内整组批改并保存逐题反馈
- 错题结构化保存
- 错题错误类型预设
- 错题已理解状态
- 今日收尾生成 3 条只读衔接问题，并在同一课程的下一条已开始学习中显示、建立为闭卷回顾问题
- 小节笔记与每日流程解耦，只在完成小节时整理
- GPT 生成小节笔记初稿，Gemini 可选润色
- 课程级、章节级和小节级学习记忆
- PDF、网页 URL 和公开视频字幕参考材料的本地保存
- 课程、章节和小节材料作用域及主材料
- 设置页材料库弹窗的统一筛选和管理
- 每日记录材料选择和本次学习范围
- 材料版本快照、本地全文索引、只读材料工作区、节点锚点/fork 和稳定来源定位
- 学习者背景设置与已完成课程学习成果上下文
- 小节笔记编辑
- Obsidian vault 路径设置
- 小节笔记保存为 `课程名/章节名/小节名.md`
- 基础搜索或查看小节笔记
- Markdown 导出入口

## 16. 第一版暂不实现

第一版不做：

- 登录系统
- 云同步
- 移动端专项适配
- Electron、Tauri 等桌面壳
- LLM API 自动调用
- 复杂双向同步冲突合并
- 全 Obsidian vault 索引
- 知识图谱
- 复杂统计图表
- 间隔复习
- 写入用户数据库并参与统计的示例课程或演示数据
- 插件系统
- 多用户协作
- 云端 OCR、自动视频语音转录
- 外部向量数据库或外部 RAG 服务

## 17. 后续可能增强

后续可以考虑：

- 可编辑提示词模板
- 自动从 AI 批改中提取错题
- 更强的 Obsidian 文件索引
- 课程大纲从目录、PDF 或 URL 自动生成
- 更广泛的视频平台和无字幕视频转录
- 跨全部课程的材料全文搜索页面和可视化检索调试
- 阶段总结
- 周总结
- 错题趋势统计
- LLM API 接入
- 复习提醒

## 18. 仍需后续确认的问题

以下内容不阻塞当前第一版，后续增强前仍需确认：

1. 课程大纲自动生成第一版是否实现，还是先手动创建章节和小节。
2. AI 出题提示词和批改提示词的具体模板。
7. 首页视觉风格和完成动画强度。

## 19. 成功标准

第一版是否成功，不看功能数量，而看：

- 用户每天是否更容易开始学习。
- 当前课程和当前小节是否一眼清楚。
- 每日学习流程是否能稳定推动闭卷回忆、主动重构、AI 出题、纠错和整理笔记。
- 学习过程是否更有节奏感和完成感。
- 小节笔记是否能顺利沉淀到 Obsidian。
- 错题是否能被结构化保存并按错误类型汇总。
- 用户是否不再需要每天重新设计学习流程。
